# HS Battlegrounds Assistant — ТЗ проекта

## Цель

Десктопный помощник для Hearthstone Battlegrounds, который в реальном времени читает состояние партии из лог-файлов игры и подсказывает наилучшие ходы: оптимальную расстановку миньонов, шансы на исход боя, решения по покупкам и левелингу таверны.

**Никакого компьютерного зрения.** Источник данных — только `Power.log`, который Hearthstone пишет при включённом `log.config`. Никакого чтения памяти процесса, никакой автоматизации ввода (это бан). Оверлей только показывает информацию, все действия в игре выполняет человек.

**Цель — промышленное ПО, а не личная утилита.** Это уже повлияло на решения
и должно влиять дальше: от пользователя нельзя требовать ручных действий вроде
переподключений посреди партии, установка сводится к двум конфигам от инсталлятора,
а приложение само проверяет и чинит настройки при каждом запуске (обновление
Hearthstone может снести `client.config`, он лежит в каталоге игры).

## Стек

- **Язык:** TypeScript (strict mode), Node.js 20+
- **Симулятор боя:** пакет `@firestone-hs/simulate-bgs-battle` (репозиторий `Zero-to-Heroes/api-simulate-battlegrounds-battle`) — не писать свой симулятор, интегрировать готовый
- **Тесты:** Vitest
- **UI (поздние фазы):** Electron, прозрачное always-on-top окно
- **Данные карт:** JSON из HearthstoneJSON (https://hearthstonejson.com), файл `cards.collectible.json` + battlegrounds-теги; закоммитить снапшот в `data/`, не тянуть в рантайме

## Архитектура

```
Power.log → [Watcher] → [Parser] → GameState → [Advisors] → [Overlay/CLI]
                                        │
                          ┌─────────────┼─────────────┐
                     BattleAdvisor  PositionAdvisor  TavernAdvisor
                     (Монте-Карло)  (перебор 7!)     (эвристики)
```

Модули независимы, общаются только через типизированный `GameState`. Каждый модуль — отдельная директория в `src/` со своими тестами.

## Структура проекта

```
/
├── CLAUDE.md                  # этот файл
├── data/
│   ├── cards/                 # снапшот карт из HearthstoneJSON
│   └── fixtures/              # РЕАЛЬНЫЕ Power.log партий (см. «Правила работы с логами»)
├── src/
│   ├── watcher/               # tail Power.log, отдаёт строки парсеру
│   ├── parser/                # строки лога → события → GameState
│   ├── state/                 # типы GameState, редьюсер событий
│   ├── advisors/
│   │   ├── battle/            # обвязка симулятора, win/tie/loss %
│   │   ├── position/          # перебор перестановок борда
│   │   └── tavern/            # эвристики покупок/левелинга
│   └── ui/                    # сначала CLI-вывод, Electron позже
└── test/
```

## Фазы разработки

Работать строго по фазам. Не начинать следующую, пока не выполнен Definition of Done текущей.

### Фаза 1 — Parser + GameState (ядро проекта)

Парсер `Power.log`: tail файла, разбор строк `GameState.DebugPrintPower`, `PowerTaskList`, тегов `TAG_CHANGE` / `FULL_ENTITY` / `SHOW_ENTITY`, сборка `GameState`.

`GameState` минимально содержит:
- фаза партии (выбор героя / таверна / бой / конец)
- номер хода, тир таверны, золото
- свой борд: миньоны с cardId, atk/hp, taunt/divine shield/poisonous/reborn/windfury, golden, enchantments, порядок слева направо
- своя рука и герой (hp + armor, hero power)
- последний увиденный борд каждого оппонента + его hp/tier
- следующий оппонент, если известен

**DoD фазы 1:** для каждой fixture-партии из `data/fixtures/` есть тест, который прогоняет лог целиком и сверяет `GameState` в контрольных точках (заранее размеченных вручную) — состав борда, золото, тир, hp. Все тесты зелёные.

### Фаза 2 — BattleAdvisor

Интеграция `@firestone-hs/simulate-bgs-battle`: маппинг нашего `GameState` во входной формат симулятора, прогон N симуляций (N настраиваемый, старт — 3000), вывод win/tie/loss % и вероятности летала в обе стороны.

**DoD:** на fixture-боях с известным фактическим исходом симулятор выдаёт распределение, где фактический исход не является выбросом; время прогона 3000 симуляций ≤ 2 с на обычном железе.

### Фаза 3 — PositionAdvisor

Перебор перестановок своего борда (≤ 7! = 5040), каждую прогонять сокращённым числом симуляций (например 300), лучшие кандидаты — полным. Выдавать топ-3 расстановки с их win %.

**DoD:** на fixture-состоянии с заведомо важной позицией (клив, poisonous, taunt) советник находит расстановку не хуже той, что была сыграна фактически. Полный перебор укладывается в ≤ 10 с; если нет — beam search.

### Фаза 4 — TavernAdvisor (эвристики, без ML)

Правила: когда левелить (стандартные тайминги по золоту/хп), что покупать (синергия с текущим бордом по племенам и ключевым парам карт), когда реролл. Конфигурация правил — данными (JSON/TS-таблицы), не хардкодом в логике.

**DoD:** unit-тесты на каждое правило + сквозной тест «состояние → рекомендация» на fixture-состояниях.

### Фаза 5 — Overlay

Electron: прозрачное окно поверх игры, показывает вывод советников. До этого — CLI-режим (`npm run watch`), который печатает советы в терминал; CLI остаётся навсегда как режим отладки.

### Фаза 6 (опционально, отдельное ТЗ) — ML

Предсказание финального места по `GameState` на ходу N. Только после того, как фазы 1–5 стабильны и накоплен датасет собственных партий. В этом ТЗ не детализируется.

## Правила работы с логами — критично

1. **Не выдумывать формат `Power.log`.** Формат меняется между билдами игры. Единственный источник правды — реальные логи в `data/fixtures/`. Если в fixtures нет примера нужной конструкции — остановиться и запросить у меня свежий лог, а не угадывать теги.
2. Перед реализацией любого куска парсера — найти соответствующие строки в fixtures и сослаться на них в тесте.
3. Каждая fixture — это `partN.log` + `partN.expected.json` с вручную проверенными контрольными точками. Тесты пишутся от expected-файла.
4. Полезный референс по устоявшимся конструкциям формата — парсеры HearthSim (`hearthstone-deck-tracker`, `python-hearthstone`), но приоритет всегда у фактического содержимого fixtures.

## Правила разработки

- TDD для парсера и советников: сначала тест на fixture, потом код.
- Никаких сетевых запросов в рантайме (кроме опциональной проверки обновлений данных карт по явной команде).
- Каждая фаза завершается работающей демонстрацией: `npm run demo:phaseN` прогоняет fixture и печатает результат.
- Коммиты небольшие, по одному логическому изменению.
- Если задача требует данных, которых нет (лог, ID карты, механика миньона) — спросить, не изобретать.

## Состояние проекта

Обновлять этот раздел при завершении крупных кусков — он единственное, что
переживает смену сессии.

Поздним вечером 27.08, сразу после part30 — **двадцать третья партия
с оверлеем (part31, Йогг-Сарон, наги, 6-е место, 21:27–21:50)**, пятая
на билде 250339. Два пункта по ОДНОМУ скриншоту хода 13 — состоянию
посреди хода (борд полон, золото 9/9), — и оба про число, у которого
не было ЦЕНЫ. **Тёмный дар советовался, едва открывшись**: верхняя
строка списка и шаг плана с хода 7 (14.4 против покупки 14.0
на скриншоте), тогда как игрок все три заряда нажал на 10-м, 11-м
и 12-м ходах таверны (ходы 19/21/23, `NUM_2` кнопки 3 → 2 → 1 → 0)
и написал, что дар «становится сильнее с каждым ходом». Теперь у заряда
есть цена, посчитанная без нового веса: зарядов — живой тег кнопки
(`darkGiftCharges` в состоянии), ходов впереди — замер
`remainingTavernTurns` (part28); если ходов больше, чем зарядов, лучшие
ходы для зарядов — последние, и нажатие сейчас вытесняет самый ранний
из них (`сейчас + впереди + 1 − зарядов`); цена спешки — разница тел
этого хода и того по таблице тиров part24, той же функцией на том же
борде. На скриншоте дар 14.4 → 9.2 (тир 4 против тира 5–6 на 10-м ходу,
19.5) и уходит под покупку; на ходу 7 молчит; с десятого хода таверны
цена — ноль, и на ходах 19/21/23 дар верхний с причиной «сильнее
предложение уже не станет» — ровно там, где жал игрок. Пункт
«цена заряда дара» из «отложено» этим закрыт. **«Discover a Tier 1
minion» за 3 стоял первым шагом плана** (A New Sprout, 7.1) при семи
своих, где слабейший стоил 9.0: миньон приходит в руку, и на полном
борде жертву вычитала только ветвь модального миньона (part28) —
теперь один вычет (`handMinionVictim`) у заклинания витрины, силы героя
и ветви без племени, с планкой `sellMargin`, как у покупок. Игрок
на этом ходу продал Rodeo Performer и поднял таверну за 10 —
«продажу ради подъёма» советник не видит вовсе, вопрос записан. Тесты
part23 переписаны честно: дар на третьем и шестом ходах таверны там
больше не верхний (список стал тем ходом, который игрок и сыграл).
746 тестов зелёные. Разбор — в docs/tavern.md, «двадцать пятая
порция», фактура — в docs/power-log.md.

Вечером 27.08, тем же днём после part29 — **двадцать вторая партия
с оверлеем (part30, Крысиный король, 3-е место, 19:55–20:23)**, четвёртая
на билде 250339. Четыре пункта по четырём скриншотам, три момента —
состояния ПОСРЕДИ хода, и все четыре пункта — про число, посчитанное
не про то. **Сила «Discover a \<Tribe\>» видна и советуется**: у Крысиного
короля сила — вариант новой сущностью каждый ход (`TB_BaconShop_HP_041b`
King of Mechs и родня, `COST=2` живым тегом, 10 нажатий за партию), слова
«minion» в тексте нет, и `heroPowerRule` молчал — племя теперь читается
той же `tribeMinionRace`, что выбор сил (part26), ожидание — по пулу
племени тиров 1..своего, а у Discover оно СВОЁ: ЛУЧШИЙ из трёх порядковой
статистикой (мехи тира 1 — Lullabot 5.0 и Cord Puller 7.5: среднее 6.25
проигрывает Tusked Camper 7.0, выбор 7.5 выигрывает — игрок нажал силу,
разыграл найденного Cord Puller и купил на него банан, ровно та цепочка
вместо «просто купить существо»). **Статы «minions in the Tavern» ложатся
на витрину** (Them Apples: в логе Target=0, энчанты +1/+2 на player=10):
цель больше не называется, ценность — статы на число покупок по остатку
золота, при нуле золота молчание. **«Gain 2 Gold next turn» — отложенное
золото** (Careful Investment за 1): прежде оно доезжало до плана живым
через `grantsGold`, и «ТЁМНЫЙ ДАР за 3» становился по карману при двух
золотых — теперь поле `goldNextTurn` отдельно (тот же урок, что `maxGold`
в part28), покупка советуется со словами «СЛЕДУЮЩИМ ходом», план строит
только по живому золоту. **Сгорающее золото уходит в обновление-сток**
(ход 19: золото 1/10, борд полон, «НИЧЕГО»): когда золоту нет другого
применения (перебор: покупка, заклинание, подъём, дар, сила) и на борде
есть триггер трат (`goldSinkTriggerWords` — Dual-Wield Corsair «Whenever
you spend 5 Gold…», Enterprising Escapee «After you spend {2} Gold…»),
платное обновление советуется малыми очками с причиной-триггерами —
игрок потратил последний золотой на обновление сам; границы part27 целы
(без триггера и при бесплатном обновлении молчание остаётся). Попутно
замолчали КРОВАВЫЕ САМОЦВЕТЫ: Gem Day складывал обе ветви в «+2 статов»
разового баффа с целью (ветвей-карт в снапшоте нет), а цены самоцветов
у нас нет — граница part28 теперь в коде (`bloodGemWords` → null,
23 заклинания семьи). Фактура попутная: ивентная «Сорванная маска»
`EBG_Spell_037` меняет силу героя посреди партии (с хода 25 сила игрока —
честно Enhancification), и состояние это отражает без правок. 730 тестов
зелёные. Разбор — в docs/tavern.md, «двадцать четвёртая порция»,
фактура — в docs/power-log.md.

Ночью 27.08, сразу после part28 — **двадцать первая партия с оверлеем (part29, Scoutmaster
Tavish, 3-е место, 01:07–01:31)**, третья на билде 250339. Пять пунктов
по пяти скриншотам, четыре момента из пяти — состояния ПОСРЕДИ хода.
Разговоров три. **Бесплатная сила героя, у которой есть ЦЕЛЬ**: «Lock
and Load» `BG22_HERO_000p_Alt` («Remove a minion in the Tavern… fire it
at a random enemy minion») активна, тега `COST` не имеет вовсе и жмётся
каждый ход — игрок нажал её 13 раз, советник молчал всю партию, потому
что бесплатные силы советовались только с текстом про обновление витрины
(part13). Применение видно блоком `PLAY` с полем `Target=` (миньон
витрины), копия цели уходит в `SETASIDE` под наш контроллер, заряд
`TAG_SCRIPT_DATA_NUM_1` 0 → 1 и обратно в начале боя, а карта — в
`REMOVEDFROMGAME`. Отсюда три правила: цель судится БОЕМ (`combatValue`
— статы, ключевые слова, боевой эффект; тир, племя и копии к живущему
один размен телу не относятся), стреляем тем, чего НЕ купим (отбрасываем
`affordable` лучших, где `affordable` ограничено и золотом, и свободными
слотами), без свободного слота молчим (это сказано в тексте силы).
На ходу скриншота совет называет Tusked Camper 2/3 — ровно того, кем
игрок и выстрелил. **Два пункта из пяти закрылись ОДНИМ счётом**:
заморозка ради «предложения дешевле покупки» обещает словами «два тела
вместо одного», и это обещание надо проверять — тел без предложения
`⌊золото/3⌋`, с предложением `1 + ⌊(золото−цена)/3⌋`, золото берётся
ТОГО хода, который совет и называет. Пять золота — да, шесть и семь —
нет, восемь — снова да; ровно это игрок и написал («дальше я уже смогу
покупать два существа за 6 золота»). Тем же счётом ушла заморозка ради
Patient Scout 1/1 (ход 7) и — в прошлой партии — заморозка ради River
Skipper на part22 (ход 7), про которую тот же игрок говорил «не вижу
практического эффекта». Попутно найдена тихая ошибка в ТЕСТЕ правила:
он назывался «part17, ход 1», а брал состояние с умолчания `turn: 5`,
то есть держал поведение, которого игрок не просил. **Цена, которая
платится не золотом**: тег `BACON_COSTS_HEALTH_TO_BUY` на карте витрины
(Hasty Excavation `BG28_571`, «Gain 1 Gold. This costs Health to buy
instead of Gold», `COST=3`) — прежде цена сравнивалась с золотом,
и заклинание было невидимо целиком. Курса «здоровье → золото» у нас нет
и не будет, поэтому ветка живёт там, где курс НЕ НУЖЕН: при миньоне
«After your hero takes damage, rewind it» на борде (четыре карты пула).
Лог это подтверждает числом — `META_DATA Meta=SPEND_HEALTH Data=3`,
броня 14 → 11 → 14 триггером Soul Rewinder. **Ставка на тройку живёт
в РУКЕ**: второй Бранн 2/4 при борде из 18/16, 24/23 и 25/34 стоял
верхней строкой с 16.0, где три очка — бонус «вторая копия», и он же
стоял ПРИЧИНОЙ выставить карту. Копии руки игра считает наравне с бордом
(тег `BACON_PAIR_CANDIDATE` она ставит и при копии только в руке —
семь случаев на part22 и part29, ни одного ложного), значит розыгрыш
числа копий не меняет, а последний слот тратит навсегда: он меняет ставку
на тройку на лишнее тело в ОДНОМ бою и советуется, только когда тело
дороже ставки (`combatValue` против `copiesBonus`; при равенстве ставка
остаётся в руке — розыгрыш необратим). 721 тест зелёный. Разбор —
в docs/tavern.md, «двадцать третья порция», фактура — в docs/power-log.md.

Ночью 27.08 — **двадцатая партия с оверлеем (part28, Galakrond,
квилбоары, 8-е место, 23:11–23:27)**, вторая на билде 250339. Пункт
обратной связи один: «не предложило лучший выбор» — открытый модальный
выбор **Snare Trapper** `BG36_332` («Choose One — Get a random Quilboar;
or Increase your maximum Gold by {0}»), про который советник молчал.
Молчание оказалось двойным, и обе половины закрыты. **Экран выбора
из лога не виден вовсе**: ветви `…t`/`…t2` создаются сущностями
в `SETASIDE` с `PARENT_CARD` ещё при появлении карты в витрине
(23:19:38), а не при розыгрыше (23:20:31), и канал выборов тут
не участвует — значит ветвь называется ЗАРАНЕЕ, в совете «разыграть»,
полем `spellBranches`, которое оверлей печатает с part19. **Обе ветви
были невидимы разбору**: «Get a random Quilboar» — потому что слова
«minion» в тексте нет (его заменяет племя), «Increase your maximum
Gold» — потому что такого эффекта не существовало в правилах. Теперь
ветви судятся НА СОСТОЯНИИ (`modalBranchAdvice`), а не скаляром весов,
как ветви заклинаний: весь вопрос в борде и ходе. **Предел золота —
рента, а не монета**: он поднимается навсегда и выше десяти (в part27
тег `RESOURCES` доходит до 19), поэтому считается по золотому за каждый
ОСТАВШИЙСЯ ход таверны; сколько их осталось, отвечает таблица
`remainingTavernTurns` — замер по датасету (`npm run spike:horizon`,
25 партий, 299 точек, длина 9–15 ходов таверны при медиане 12).
На ходу скриншота: Ensnare the Target 9.8 (случайный квилбоар 20.8
минус вторая жертва 11.0 — борд ПОЛОН, карта идёт в руку) против
Collect the Bounty 15.9 (+1 × 5.3 хода × 3 очка) — совет называет
вторую, игрок сыграл первую (`SubOption=0` в блоке PLAY — так лог
и говорит, что выбрано). Не будь борд полон, выигрывал бы квилбоар.
Тем же разбором перестали быть невидимы заклинания витрины Strike Oil
`BG28_805` и Timewarped Strike Oil `BG34_Treasure_932`. Граница названа
вслух: ОЧКИ самого миньона правка не трогает — ценность ветви в них
не входит, порядок покупок прежний, сверки не устаревают; модальных
миньонов в пуле семь, и вносить их эффекты в ценность надо вместе
с перезамером. 706 тестов зелёные. Разбор — в docs/tavern.md,
«двадцать вторая порция», фактура — в docs/power-log.md.

26.08 — **девятнадцатая партия с оверлеем (part27, пираты, 3-е место,
16:15–16:42) и первая на билде 250339**; три пункта, все три — про
состояние ПОСРЕДИ хода, после трат, которого точки решения не видят,
поэтому тесты ловят моменты условиями на состояние. **Заморозка
исчезала после первого шага плана** (ход 1): план обещал «КУПИТЬ Risen
Rider → ЗАМОРОЗИТЬ Enchanted Lasso», игрок купил Crackling Cyclone —
второго из двух РАВНЫХ (6.0), — и совет стал «НИЧЕГО». Причина — счёт
планки, а не порог: цена заморозки бралась по СРЕДНЕЙ доживающей карте,
и Harmless Bonehead (4.5 при купленной нежити, 3.0 при купленном
элементале) решал всё. Теперь цена — недобор ЛУЧШИХ доживающих до свежей
по покупкам следующего хода, и только СВЕРХ свежих слотов: замороженная
витрина ДОЗАПОЛНЯЕТСЯ до размера витрины тира (фактура по 19 заморозкам
шести партий, docs/power-log.md), а подъём таверны добавляет слот сам.
Этим же закрылся спор о заморозке ради заклинания, тянувшийся с part17
через part19/22/23/24: на первом и втором тирах планка равна свежей
карте (part19 ход 3 — 6.84, part24 ход 3 — 6.81 против 8.48), на третьем
свежая карта дороже тела первого тира (part23 ход 11 — 10.70 против 9.02);
«умолкает на третьем» — ответ арифметики, а не выбор. Тест part24 при
этом оказался пустым (селектор брал состояние ПОСЛЕ ручной заморозки
игрока) и переписан. Замер частоты: заморозка в плане на 25 → 37 точках
из 292. **Провокация уходила с крупнейшего тела на отработавший
генератор** (ход 7): Molten Rock 4/4 («After you play an Elemental…»)
числился движком словом «After», а его триггер ТАВЕРННЫЙ — в бою он
тело. Тавернные триггеры движком больше не считаются (`tavernTriggerWords`,
проверка по позиции головы): из 129 миньонов пула с триггером 83 —
только тавернные, движками остаются 46 (Deathstrider среди них, а также
слушатели заклинаний и добычи в руку — Rally кастует и добывает в бою).
Оба Slimy Shield теперь идут на Molten Rock. **«ОБНОВИТЬ» при нуле золота
обещало покупку** (ход 19, полный борд, обновление бесплатно): когда
найденное не на что купить, обновление советуется только под НАЗВАННУЮ
цель заморозки — третью копию к паре или соплеменника на неполном борде,
— иначе молчит; на самом ходу цели были (пары Bigwig Bandit и Dual-Wield
Corsair, назван старший по тиру), и совет остался с честной причиной.
Состязательная проверка (три опровергателя) внесла ещё пять правок,
из них одна блокирующая — заклинание ПО ЦЕНЕ покупки вставало верхней
строкой с нулевым запасом; две находки приняты как есть и записаны
(асимметрия планки, пул лассо без свежих слотов) — docs/tavern.md. Билд 250339 объявлен одной игрой с 248348
(`src/data/builds.ts`, параллельная сессия); part27 в `CURRENT_BUILD_PARTS`
не добавлена — это отдельный шаг с перезапуском всех замеров. 685 тестов
зелёные. Разбор — в docs/tavern.md, «двадцать первая порция».

19.08 (день) — **замер 2 фазы 6: имитация покупок, вердикт
НЕ ДОКАЗАНО** (docs/ml.md). Запрос игрока «пусть учится лучшим
действиям» получил первый действующий замер на журнале действий:
предсказуем ли ЕГО выбор покупки сверх советника. Парная гребневая
регрессия на шести признаках-кирпичах советника (`src/ml/imitation.ts`),
LOGO по 23 партиям, hit с честными ничьими (твины признаков в 47
инстансах из 154, раскол меткой в 11 — целый hit решал бы тай-брейк),
контроль годности на десяти реализациях случайных меток против шанса
собственного выбора (пройден: +0.2 п.п. при полосе реализаций
−5.4…+4.9 — одна реализация могла выпасть за порог). Числа: модель
66.2% против советника 70.3%, D̄ = −4.1 п.п. внутри полосы ±8.6
при МРЭ 8.6 — различия не показано, и предпрогонная достижимость
предупредила об этом дословно (прокси-МРЭ 9 п.п. выше порога 5).
Показательное: 66% предсказуемости покупок дают ДВА веса —
соплеменники на борде (0.617) и тир (0.190), статы — ноль: игрок
покупает племя, а не тело. В расхождениях, где прав оказался выбор
модели, — сплошь экономика (River Skipper, Sellemental, Fleeing
Fugitive) — тот же портрет, что жалобы part18/22/25. Состязательная
проверка внесла девять правок до прогона; главная оговорка записана:
советник ДОРАБАТЫВАЛСЯ по этим же партиям (бейзлайн видел тест-сет),
ОТВЕРГНУТЬ отсюда не переносился бы, но его и нет. В советник ничего
не внесено; перезамер при +10 партиях (`npm run ml:imitation`).
667 тестов зелёные.

19.08 (ночь) — **журнал действий игрока: в состоянии, в датасете
и задним числом по всем фикстурам**. Запрос игрока — «пусть ML на основе
победы/поражения учится лучшим действиям и высчитывает лучший ход» —
разложен дорожной картой в docs/ml.md: классический RL невозможен
(среды нет, партия целиком не симулируется), путь — V(состояние) →
ΔV действий на гипотетических состояниях плана (`spend.ts` их уже
строит), блокер — сам V (первый замер: хуже таблицы). Сделан первый
шаг пути: «какие действия ведут к победе» не выучить, не записывая
действий, а записи их не несли. Фактура — блоки `PLAY` канала-источника:
покупка и продажа — перетаскиватели `TB_BaconShop_DragBuy[_Spell]`/
`DragSell` с картой-целью в `Target=[…]` (BlockContext теперь несёт
`target`), обновление/подъём/заморозка — кнопки, розыгрыш — карта, чья
зона в дескрипторе строки BLOCK_START ещё `HAND` (к следующим событиям
блока карта уже сменила зону), сила/дар/активация — прежними ветками.
Запись одноразовая через WeakSet по объекту блока — блок стоит в стеке
многих событий подряд. `GameState.actions` → запись датасета
(`recorder`), досбор дописал журнал во все 28 лежащих записей —
**4771 действие по 23 партиям** — и ничего не перетёр; живой путь
получает журнал через то же состояние, эквивалентность живого
и пакетного путей закрывает и его. Следующий замер по карте — имитация
покупок из выигранных партий (парное ранжирование против советника,
предрегистрация до прогона). 653 теста зелёные; тест журнала —
от независимого счёта по строкам part17 (жадный `sed` там однажды
соврал: две «продажи TB-карт» были целями продаж, а не действиями).

18.08 (день) — **начата фаза 6: первый замер предсказания места,
вердикт НЕ ДОКАЗАНО** (docs/ml.md). По ТЗ фазы 6 написано отдельное
мини-ТЗ с предрегистрацией, закоммиченной ДО прогона; предрегистрация
прошла состязательную проверку (восемь находок внесено до прогона,
в том числе полоса вместо нуля у ОТВЕРГНУТЬ и индикатор информативного
пропуска «ещё без побед» — тег BACON_WON_LAST_COMBAT пишется только
при смене значения, и null значит «ещё не выигрывал», а не «до первого
боя»). Прибор: `src/ml/` — загрузка датасета с дедупом по отпечатку
(23 партии, 277 точек; дубли part17–21 отброшены вслух), семь «своих»
признаков, гребневая регрессия закрытой формы (λ=1), LOGO по партиям,
бейзлайны B0 (среднее место), B1 (текущее место таблицы), B2 (сжатое
место — ridge на одном признаке таблицы), полоса sign-flip,
отрицательный контроль конвейера на перемешанных местах (пройден:
−0.100 при пороге утечки +0.1), два прогона побайтно равны. Числа:
модель 1.874 ПРОТИВ B0 1.834 и B2 1.823 — лучший предиктор места
на этом датасете НЕ модель, а сжатое к среднему текущее место; выигрыш
у таблицы (D̄ = +0.254) взял продуктовый порог ровно, но ниже МРЭ (0.400)
и полосы (0.406), живёт только в ранних ходах (D̄_late = −0.152)
и целиком создан сжатием, а не признаками борда (D̄₂ = −0.052). Причина
названа: место — величина ОТНОСИТЕЛЬНАЯ, а все признаки датасета —
абсолютные (`lobby` вошёл в состояние ПОСЛЕ всех записанных партий).
Строки прогноза в оверлее НЕТ; перезамер — при +10 новых партиях,
когда записи принесут `lobby` (`npm run ml:eval`). 645 тестов зелёные.

18.08 — **разбор кода без новой партии**: ревью нашло восемь мест, где
советник считал тихо неверное число, и все они закрыты правками с тестами,
ПАДАЮЩИМИ на прежнем коде (это проверено откатом каждой правки по очереди).
Девятая находка ревью проверки НЕ прошла и правкой не стала — отвергнутое
записано там же, в docs/tavern.md. (Нумерация там своя и до десяти:
пункты docs считают вместе с местами ВНЕ советника и со снятой лишней
работой, поэтому «восемь» здесь и «десять» там — про разное.) Крупнейшее — **кэш ожидания по пулу**: он
ключевался ссылкой на борд, тирами и ДЛИНОЙ руки, а зависит ответ ещё
от справочника, таблицы правил, СОСТАВА руки, заклинаний руки и силы
героя. Больнее всего последнее: розыгрыш заклинания руки не меняет
ни ссылки на борд, ни длины руки — и следующий шаг плана получал планку
заморозки и тёмный дар, посчитанные с уже разыгранным заклинанием.
**Заряд магнита-хранителя** считался дважды с обеих сторон: ценность
покупки складывала выгоду по ВСЕМ заклинаниям руки (два трезубца — +4
статов вместо +2), а расход заряда в плане писался через `map` по массиву
тегов и на пустом массиве не делал ничего — ровно там, где обе стороны
читают «заряд есть». **`grantsGold`** заполнялось чистым золотом
у заклинания руки и валовым у заклинания витрины, при том что цену
`applyRecommendation` вычитает само, — то есть симптом part24 («откроется
покупка», а покупка не открывается) жил дальше для карт руки. Ещё:
продажа ради траты подставляла УЖЕ доступную покупку, когда витрина
дополнительной не предлагала, и считала удерживаемую ценность на борде
из одного кандидата (карта числила соплеменником саму себя); прокрутка
отбирала кандидата по бампу порядка, а не по своей ценности; ставка
на чужой бой не сводила скин героя к базовой карте и половину вариантов
теряла в «оценить не берёмся». Попутно убрана лишняя работа: разбор
заклинания и разбор удвоителей механик повторялись на КАЖДОГО кандидата
(при усреднении по пулу их 382) — теперь оба под тем же `memoByCard`,
что и текстовый разбор, и ход советника стал быстрее на 9–31%
(part19/21/22/25, числа в docs/tavern.md). Правки структурные, ни одного
веса не тронуто; 599 прежних тестов зелены, добавлено 21 новый — всего 620.

Тем же разбором закрыты три места вне советника. **Список партий текущего
билда жил семью копиями и разъехался** (`validate`/`validateSpend`/
`spike:level` дошли до part26, замеры `buff`/`taunt`/`hand` и досбор
датасета остались на part21–22) — теперь он один, `CURRENT_BUILD_PARTS`.
**Досбор датасета проверял имя файла, а не партию**, и оказалось, что
в `data/dataset/` лежит **28 записей на 23 партии: part17–part21 записаны
дважды** — живым режимом и досбором. Идемпотентность теперь по отпечатку
партии (`gameSignature`: билд, герой, место, ход и витрина первой точки
решения), досбор называет задвоенное вслух, но сам ничего не удаляет.
Следствие, которое надо помнить: фраза «по датасету 25 партий» из разбора
part25 считана по ЗАПИСЯМ, а не по партиям.

Третье место — в самих тестах. Снимок состояния дорог, поэтому тесты
снимают его не на каждом событии, а по фильтру строк — и фильтр у каждого
был свой. У part17 селектор ловит момент условием `!shop.some(frozen)`,
а строки `tag=FROZEN` его фильтр не пропускал вовсе: снимок мог быть взят
ДО заморозки, и тест проверял бы состояние, которого в партии не было.
Фильтр теперь один (`test/snapshots.ts`) и перечисляет всё, из чего
состоит вопрос советника; статы вынесены отдельно — они стоят трети строк
сверху и нужны только part22. Тесты part23–part26 фильтра не имеют вовсе
и оставлены как есть: «снимать всегда» — самый строгий вариант.

Ночью 18.08 — восемнадцатая партия с оверлеем (part26, Мастер Нгуен,
4-е место, 00:28–00:54), два пункта, и оба про ВЫБОР, на котором советник
говорил «оценить не берёмся». **Сила героя в выборе читается двумя
шкалами**: своими очками из текста (даёт миньона — в том числе племенем,
как «Discover a Naga»; обновляет витрину; даёт заклинание таверны; меняет
своего миньона) и средним местом ГЕРОЯ, чья это сила, по статистике
Firestone — связь «сила → герой» лежит в идентификаторе (69 сил пула
из 169, из них 63 со статистикой). Шкалы не смешиваются: ранжируется та,
что есть у всех вариантов, известное стоит выше неизвестного. Две тихие
ошибки закрыты сразу: замена читается РАНЬШЕ «даёт миньона» (Rune
of Damnation — 3.0 очка вместо 7.6), а триггер в тексте выключает разбор
разового усиления (Tavern Lighting оценивался в одно очко вместо вечной
прибавки ко всем заклинаниям). **Ставка на чужой бой получила фактуру**:
«Дружеская ставка» предлагает угадать победителя чужого боя, и для этого
в состояние вошла таблица `lobby` — герой, тир, здоровье и место ВСЕХ
восьми игроков (требование ТЗ, висевшее незакрытым). Ловушка одна:
сущностей героя у игрока несколько, и копии для модальных выборов остаются
с `PLAYER_TECH_LEVEL=0` — берётся сущность с наименьшим id, не выбывшая
из игры. Совет называет тир, hp, место и давность нашей картинки борда,
порядок — по тиру; симулятор не зовётся намеренно (картинке 5–17 ходов).
Игрок поставил на лидера таблицы и проиграл; выиграл тот, у кого выше тир.
Разбор — в docs/tavern.md, «двадцатая порция».

Ночью 17.08 — семнадцатая партия с оверлеем (part25, Murozond,
Unbounded, 3-е место, 22:44–23:15), два пункта, и оба про ОДНУ механику:
**карту, чьё обещание отдаёт ПРОДАЖА**. **Прокрутка научилась продажным
генераторам**: цепочка «купить-разыграть-продать» с part16 знала только
боевой клич, а у Patient Scout («When you sell this, Discover a Tier 1
minion») платит продажа — то же действие, та же чистая цена, и считается
той же функцией, что заклинание витрины, дающее миньона (9.3 очка при
чистой цене 2). Запрет «генератора-лучшую-покупку не прокручивают»
на продажного не распространяется: держать его телом значит не получить
обещанного никогда. **Заморозка получила ветку для такой карты**: River
Skipper при нулевом золоте — это покупка дешевле трёх на СЛЕДУЮЩИЙ ход,
и судится она планкой заклинания витрины (8.48 против 7.01 на ходу 3,
на пятом тире молчит сама). Игрок заморозил витрину сам и разменял пять
золотых на два тела — советник молчал. **Спор о подъёме на шести золотых
закрыт РЕШЕНИЕМ ИГРОКА** — третья подряд жалоба на один ход (part23,
part24, part25) была вынесена ему с числами (подъём верхний на 4-м ходу
таверны в 21 партии из 22; по датасету 25 партий он там ВСЕГДА тир 2,
а тир 3 берёт на 5–6-м), и он выбрал «пусть план сравнивает подъём
с цепочкой». Двойной счёт снят явно: у подъёма появилось поле
`standaloneScore` (одна срочность, без лучшей покупки внутри), список
советов не изменился, изменился план. Замер частоты новых советов
(22 партии, 267 точек решения): продажный генератор в витрине на 31,
прокрутка верхним советом на 11, в плане на 17, заморозка ради неё
на 6 — навязчивым это не выглядит. Сверка плана после правки:
внутри формы 65% при цене 14.07 п.п., а **сгорающего золота у плана
1.49 против 2.21 у «лучшего набора»** (было 1.56/2.82). Разбор —
в docs/tavern.md, «девятнадцатая порция».

Ночью 17.08 — шестнадцатая партия с оверлеем (part24, Тесс Златопряха,
демоны, 5-е место, 21:03–21:28), четыре пункта, и вместе с ними игрок
прислал **ТАБЛИЦУ ТИРОВ ТЁМНОГО ДАРА** — то самое число, которого
не хватало часом раньше. Плоский вес ушёл: тир предложения читается
таблицей по ходу ТАВЕРНЫ (3-й ход — тир 2, дальше 2–3, 3, 3–4, 4, 4–5,
4–5–6, 5–6), а ценность считается ожиданием миньона этих тиров той же
функцией, что и всё остальное. Замер частоты: дар верхним советом на 16%
точек решения, первым шагом плана на 12% — не доминирует. **Золото
заклинания стало доезжать до следующего шага плана**: совет обещал
«откроется покупка Ominous Seer», а гипотетическое состояние обещанного
золота не получало, и план брал заклинание за 2 вместо миньона за 3.
**Поглощение витрины научилось считаться числом**: «Your Demons each
consume a random minion in the Tavern to gain its stats» — племя едоков
из текста, их число с борда, статы съедаемого из витрины: на ходу 15 это
+97 статов за 2 золота против совета «обновить за 1». Попутно исправлена
неточность вчерашней правки: витрина тира N предлагает миньонов тиров
1..N, а «свежая карта» считалась по одному своему тиру. **Два пункта
НЕ закрыты, и по каждому написаны и ОТКАЧЕНЫ две правки** — заморозка
на втором тире (третий разворот правила за сутки, нужен арбитраж игрока)
и подъём на шести золотых с горящей монетой (обе попытки убирали
канонический подъём четвёртого хода таверны). Разбор — в docs/tavern.md,
«восемнадцатая порция».

Вечером 17.08 — пятнадцатая партия с оверлеем (part23, Nightmare Lord
Xavius, мехи с пиратами, 7-е место, 19:21–19:42) и первая обратная связь
из ПРОИГРАННОЙ партии. Три пункта, и все три — про **цену действия
в золоте**. **Жадный первый шаг запирал остаток**: пять золота, тёмный
дар за 3 (8.0 очков) и два сгоревших — при том что прокрутка Oozeling
за чистых 2 (7.5) стояла ВТОРОЙ строкой и оставляла ровно три
на покупку. Игрок сыграл её сам и написал: «мне подсвечивает ход, где
я должен потерять золото». Теперь шаг, после которого золото сгорает,
соревнуется не сам с собой, а с ЦЕПОЧКОЙ; подъём таверны в развилку
не входит с обеих сторон. **Тир, названный в тексте, прочитан** — дыра
была найдена ещё при разборе part22 и отложена там как «не из пунктов
игрока», а теперь стала пунктом: Recruit a Trainee («Get a random Tier 1
minion») на третьем тире стоил 14.5 очков, как карта витрины, вместо
9.0 по пулу первого тира. **Наценка стала считаться так же, как скидка**
(прежнее `Math.max(0, …)` делало курс односторонним), и заодно
**заклинание бесплатных обновлений** перестало быть невидимым: «Gain 2
free Refreshes» за 1 — это живая цена кнопки, помноженная на число
обновлений. Главное же — **закрыт спор про заморозку ради заклинания,
тянувшийся с part17**: правило part19 ОТМЕНЕНО по словам того же игрока
(«работает для ранней игры»), и не подгонкой, а починкой планки, которая
сравнивала числа с разной начинкой. Тёмный дар опознан по логу: это
раскопка ТРЁХ МИНЬОНОВ с даром, а не усиление своего, — причина совета
исправлена, вес оставлен плоским и вынесен игроку. Разбор —
в docs/tavern.md, «семнадцатая порция».

Вечером 17.08 — четырнадцатая партия с оверлеем (part22, Грибомант
Флургл, мурлоки, **1-е место**, 17:23–17:53), пять пунктов, и три из них
об одном: **рука — это позиция, а не склад тел**. План каждый ход
предлагал выставить Flighty Scout («Start of Combat: If this minion is
in your hand, summon a copy of it» — тел в бою поровну, а слот тратится
навсегда) и Bream Counter («While this is in your hand… gain +{0}/+{1}»
— розыгрыш останавливает рост); игрок держал обе в руке и пришёл первым,
счетовод дорос до 670/668. В пуле 14 карт с «in your hand», и ВСЕ
мурлоки: трое работают из руки, одиннадцать её едят. **Сила героя вошла
в ценность покупки** — прежде та читала только борд и текст карты,
а «Рыбалка» Флургла («After you sell 5 minions, get a random Murloc»)
называет и продажу, и племя, ради которых игрок и купил River Skipper
вместо предложенного дракона. **Удвоитель механики перестал быть рядовой
связью**: Бранн множит клич, и «Battlecry and Deathrattle: Get
a Deepwater Clan» при нём приносит две карты (21.0 → 27.0, выше эффекта
конца хода). Замер «что даёт розыгрыш карты из руки» (`npm run
spike:hand`) объявлен НЕГОДНЫМ по собственному критерию — контроль порога
не взял, — и правило внесено на структурном доводе; это записано, а не
стёрто. **Два пункта закрыты не полностью**: жалобы на заморозку ради
заклинания (ходы 1 и 7) упираются в правила, которые тот же игрок сам
одобрил в part17 и part19, — обе правки написаны, обе откачены по падению
тестов тех партий, вопрос вынесен игроку. Разбор — в docs/tavern.md,
«шестнадцатая порция».

Вечером 17.08 — тринадцатая партия с оверлеем (part21, наги
на заклинаниях, 2-е место, 15:47–16:12), два пункта, и оба про ОДНО
действие: заклинание, применённое к миньону. Разбор развалил их на три
разных вопроса с разными ответами. **Магнит заклинаний бьёт «крупнейшее
тело»**: план покупал Lava Lurker («первое чародейское заклинание на нём
за ход становится постоянным») и тут же играл Mini-Trident мимо него —
в самое большое тело борда, где «+2 Attack until next turn» выветрится.
Выгода считается числом из тегов: +2 у хранителя против +1 у растущего
беглеца («gain +{0} Health», {0}=1 живым тегом) — цель сменилась счётом,
а не мнением; попутно временность усиления научилась искаться в СВОЁМ
предложении (у Undersea Mount временна только вихревая половина).
**Заклинания руки вошли в ценность покупки магнита** из витрины, но
верхней покупкой беглеца это не сделало (9.0 против 10.0), и ответ игроку
дан числами, а не «советник был прав»: ближайший бой даёт 55.1% против
97.0%. **Провокация на носителе ралли — замерена и правилом не стала**:
`npm run spike:taunt` на 78 точках 23 партий дал −0.241 п.п.
при пороге 0.359 (знак в сторону опасения игрока, порога не берёт).
Попутно найдена и закрыта дыра, без которой этот замер был бы
бессмысленным: **рука не доходила до симулятора**, и ралли-призыв
Expert Aviator считался нулём — мурлок давал 77.4% вместо 97.0%.
Разбор — в docs/tavern.md, «пятнадцатая порция».

**Фазы 1–5 закрыты и проверены игроком в игре**: оверлей запущен на его
машине (Electron скачал сам игрок), советы идут по живой партии, обратная
связь с одиннадцати партий патча 248348 внесена в правила. В среде
разработки GUI не поднять — окно проверяется только игроком, см.
docs/live.md. 620 тестов, `npm run typecheck`, `lint`, `test`, `build`
зелёные.

Днём 17.08 — двенадцатая партия с оверлеем (part20, пираты, **1-е место**
при 3 hp, 14:34–15:02), пункт обратной связи один и **впервые про частоту
совета, а не про совет**: «очень часто советует улучшить таверну».
Впечатление проверялось замером, а не скриншотом (`npm run spike:level`):
на 202 точках решения семнадцати партий советник числил игрока отстающим
от кривой в **87%** ходов и ставил подъём верхним советом в **50%**.
Причина — **шкала ходов**: таблица кривой написана в ходах ТАВЕРНЫ («тир 6
к одиннадцатому»), а сравнивалась с сырым `GameState.turn`, который растёт
и на переходе в бой. Ход таверны N — это `turn = 2N − 1`, и золота на нём
`min(2 + N, 10)`: ход 15 со скриншота — восьмой ход таверны, где игрок шёл
на тир ВПЕРЕДИ графика, а советник требовал тир 6. После перевода шкалы
(`tavernTurnOf`) — 45% и 30%, а на точке жалобы первой строкой встала
покупка. Порог здоровья и место в таблице правка не трогала, и обратного
эта партия не показывает: игрок стоял седьмым при 22 hp, таверну не поднял
и пришёл первым. Разбор — в docs/tavern.md, «четырнадцатая порция».

Ночью 17.08 — одиннадцатая партия с оверлеем (part19, Nightmare Lord
Xavius, демоны, **1-е место**, 23:51–00:20), четыре пункта. **Заморозка
ради заклинания переживает подъём таверны**: запрет «в ход подъёма
заморозка молчит» верен для миньонов (свежая витрина будет нового тира),
а заклинания в свежей витрине не будет вовсе — игрок морозил лассо
по совету, поднял таверну, и совет исчез, хотя заморозку каждый ход надо
продлевать. Порог этой ветки стал маржинальным: заморозка отнимает
не покупку, а разницу между свежей картой и лучшей из доживающих.
**Модальное «Choose One» разобрано по ветвям** — они лежат в снапшоте
отдельными картами (`…t`/`…t2`), совет называет ветвь и её числа;
попутно исчез тихий просчёт, складывавший ОБЕ ветви (+8 статов вместо
+4). Какую ветвь брать при равной сумме, решал замер, а не мнение:
`npm run spike:buff` на 252 точках решения дал −1.530 п.п. при пороге
1.547 — направления нет, и совет честно называет обе. **Аура на чужих
не идёт в жертву продажи**: Бранн Бронзобород 27/29 при борде в сотни
статов был слабейшим по шкале, которая меряет тела, а его ценность —
удвоенные кличи будущих покупок; клич добавлен в таблицу механик, связь
двусторонняя, и Mind Muck поднялся на первое место покупок. Четвёртый
пункт (ралли вторым номером) кода не потребовал: перебор всех 24
расстановок подтвердил совет — 92.9% против 91.1% у лучшей «камп первым».
Разбор — в docs/tavern.md, «тринадцатая порция», и docs/position.md.

Вечером 16.08 — десятая партия с оверлеем (part18, наги на заклинаниях,
4-е место, 21:47–22:11) и первая с планом трат хода: **все три пункта
обратной связи — про план**, и это ожидаемо, ведь он показывает весь ход
одной строкой. **Продажа вошла в план**: карта, чья ценность реализуется
продажей (River Skipper), продаётся, когда золото открывает ещё одну трату
— игрок сыграл это сам, а советник продажу не предлагал вовсе. **Раннее
платное обновление** больше не съедает остаток после подъёма: оно имеет
смысл, только если найденное будет на что купить (прежний запрет был
привязан к доступному подъёму и в плане после подъёма испарялся).
**Синергия по механике стала двусторонней**: нага со Spellcraft на борде,
живущем заклинаниями, обошла демона на два тира выше — прежде связь
читалась только в одну сторону. Разбор — в docs/tavern.md, «двенадцатая
порция».

16.08 закрыты три пункта из «отложено», и все три оказались об одном:
не моделировать то, что игра уже посчитала. **Экономика читается живыми
ценами**: недостающей была цена обновления витрины (жила константой), а она
падает в ноль на целые ходы — «Leaf Through the Pages», напарник Magnus
Manastorm, экономические тринкеты; теперь `rerollCost` живой, и бесплатное
обновление не уступает подъёму. Выбор тринкета своей шкалы экономики не
получил: статистика мест есть у 241 тринкета пула из 247. **План трат хода**
(`spend.ts`) собирает весь ход одной строкой — цепочка тех же правил
на гипотетических состояниях, без второй шкалы очков; на part17 (ход 1)
он выходит ровно тем ходом, который сыграл игрок. Перебор корзин
(`npm run validate:spend`) показал, что полного оптимизатора данные
не оправдывают. **Чистка склеенного золотого текста** применена и на стороне
симулятора (`endOfTurn.ts`) — сегодня безопасно, со следующим снапшотом было
бы тихо неверно. Разбор — в docs/tavern.md.

Днём 15.08 — девятая партия с оверлеем (part17, элементали, **1-е место**,
15:57–16:22), четыре пункта, из них ДВА ВОПРОСА, а не жалобы. Вопрос
«верна ли логика заморозки ради заклинания» вскрыл дыру: заклинание
витрины, ДАЮЩЕЕ МИНЬОНА (Enchanted Lasso, «Steal a random minion from
the Tavern» за 2), советнику было невидимо целиком — ни купить, ни
заморозить. Вопрос «почему расстановка советуется в начале хода и молчит
в конце» кода не потребовал: за ход борд дорос с 97.7% до 100% побед
по полю, и «менять нечего» — это потолок (прогон обоих состояний
в тесте). Две жалобы закрыты структурно: **провокация уходила на токен**
(триггер о себе — боец, а не движок; усиление навсегда — не на кандидата
в продажу) и **заморозка мусора при полном борде** (заморозка проходит
ту же планку, что покупка). Попутно найден тихо неверный счёт: в снапшоте
к тексту шести заклинаний приклеен золотой вариант, и числа складывались
(Fortify «+6 статов» вместо +3). Разбор — в docs/tavern.md,
«одиннадцатая порция».

15.08 закрыт вопрос, висевший в «отложено» с самого начала: **помогает ли
глобальная статистика мест миньонов оценивать покупку**. Ответ — нет,
и это записанный результат, а не отказ от работы (docs/cardstats.md).
Предрегистрация закоммичена ДО прогона; замер парный (слагаемое входит
только в ранжирование покупок, поэтому борды от веса не зависят и любой
вес проверяется по одному дампу без новых симуляций); 108 точек решения,
405 кандидатов, 4000 симуляций с зерном. Сигнала сверх наших одиннадцати
слагаемых нет (V2 = −0.078 при пороге +0.15), кросс-валидация выбрала
λ=0 в 12 фолдах из 13. Состязательная проверка поправила ФОРМУЛИРОВКУ:
не «вредит», а «польза не показана» — у цены выбора нулевая точка сама
отрицательна, и её ветка не разрешала собственного порога. Три
методических урока внесены в «не переоткрывать».

Той же ночью 15.08 закрыт частный случай «ключевых пар карт», отложенный
с ТЗ: **связь по имени карты в тексте** (`perNamedCardMate`). Automaton
Portrait называет Ancestral Automaton — связь читается из опубликованных
текстов в обе стороны (и портрет ценнее при автоматонах, и автоматон
при портрете), копии исключены, короткие имена не ищутся. В пуле таких
пар «миньон ↔ миньон» десятки (Auto Assembler, Eternal Summoner,
Cousin Errgl…). Перезамер: 80% / 3.02 / 62% / 8.6 против 79% / 3.03 /
58% / 8.9 — сдвиг вверх в пределах шума, регресса нет.

Той же ночью 15.08 закрыты два продуктовых пункта. **Накопление датасета
для фазы 6** (`src/dataset/`): живой режим на конце каждой партии пишет
в `data/dataset/` (вне git) JSON с точками решения всех ходов таверны
(то же определение, что у `readTavernTurns`), героем, билдом и финальным
местом; `npm run dataset:backfill` досыпал 15 партий текущего билда
из фикстур. **Предупреждение «снапшот отстал от патча»**
(`src/live/freshness.ts`): номер билда теперь читается из лога
(`BuildNumber=…`, канал DebugPrintGame → `GameState.buildNumber`),
а сигнал — уникальные незнакомые карты в зонах советов; фон замерен
на всех 13 партиях со свежим снапшотом — ровно ноль, порог 3.
Предупреждение называет билд и обе команды обновления, в оверлее —
первой строкой, раз за партию.

Ночью 15.08 — восьмая партия с оверлеем (part16, АПМ-пираты, 4-е место),
первая проверка строки «по бою», четыре пункта — все про непокрытые места,
все закрыты структурно: **прокрутка** батлкрай-генератора карт
(купить-разыграть-продать до основной покупки, `spinRule`), **бафф
соседям конца хода в симуляцию** (Surfing Sylvar: таверна → конец хода →
бой, бафф применяется к каждому кандидату расстановки и досчёта покупок,
`battle/endOfTurn.ts`), **числительное перед friendly** — нецелевое
заклинание (Healthy Bounty), **смертность Badsong-карт — по тексту
создателя** (`CREATOR_DBID`), а не по энчанту: энчант значит лишь
«бесплатно», и правило part11 пряталo розыгрыш всей руки. Урок разбора:
тексты снапшота переносят строки посреди предложения — многословные
шаблоны пишут `\s+` вместо пробела. Разбор — в docs/tavern.md,
«десятая порция».

Ночью 14.08 закрыт «следующий уровень» качества покупок: **досчёт
решающих покупок симулятором в живом режиме**. Верхние покупки (до трёх
разных карт, борд «как выйдет в бой») прогоняются через симулятор в том
же воркере, что расстановка, против той же цели (следующий противник
или поле виденных бордов); к эвристике добавляется строка «по бою» —
«подтверждено» / «ПО БОЮ ЛУЧШЕ X» / «покупки неразличимы» (разброс ниже
порога шума, выведенного из числа симуляций). Досчёт дополняет эвристику,
не подменяя её порядок: экономику ближайший бой не видит. У воркера
теперь два слота отмены (расстановка и покупки не бросают друг друга),
покупки шлются перед расстановкой — короткий счёт не ждёт длинного.
Подробности — в [docs/tavern.md](docs/tavern.md).

Поздним вечером 14.08 игрок принёс **базу знаний JeefHS** (конспект
геймплея топ-игрока) как внешний источник правил. Разбор — в
[docs/jeefhs.md](docs/jeefhs.md): четыре пункта вошли структурными
правилами (витрина из мусора — довод подняться; ранний реролл
не соревнуется с доступным подъёмом; напоминание о тьюторинге тринкетов
за ход до предложения; сильный нейтральный тринкет выше слабого
племенного — «эффективное место» с курсом σ за миньона; последние два —
после подтверждения игроком), большая часть конспекта уже покрыта
правилами или симулятором, картоспецифика (таблица активаторов —
по словам игрока частично устарела) отложена до списка актуальных карт.
Disconnect tech и APM-советы отвергнуты как противоречащие продукту.

Ночью 14.08 подключена **статистика мест** — по предложению игрока
(он прислал ссылки hsreplay; тот закрыт от выгрузки, вместо него — открытые
данные Firestone, та же экосистема, что симулятор и снапшот карт).
`npm run update:bgstats` кладёт снапшот в `data/bgstats/` (сеть только
по явной команде, как у карт); советник получил два применения: **выбор
героя** (канал MULLIGAN теперь в состоянии — `heroChoice`, варианты
ранжируются средним местом, скины приводятся к базовой карте) и **тринкеты**
(среднее место в reason и как разводка при равных своих племенах; голое
«оценить не берёмся» осталось только там, где нет и статистики). Малые
выборки (<500 партий) отфильтрованы. Статистика привязана к патчу —
обновлять вместе со снапшотом карт.

Поздним вечером 14.08 — шестая партия с оверлеем (part14, Сильвана,
5-е место), три пункта: **заклинание-замена** от тринкета («Destroy a
friendly Undead to get a random Undead» — ни статов, ни золота, разбор
возвращал null и совет молчал) распознаётся и целится в наименьшую нежить;
**активации миньонов** («Activate (N): …») советуются — активируемость
тегом `HAS_ACTIVATE_POWER`, цена живым тегом `INTERACTABLE_OBJECT_COST`,
применение блоком PLAY на сущности, стоящей в PLAY, повторно в тот же ход
не советуется; **боевой бонус за племенное ралли** («Rally: Your Undead
have +1 Attack») без своих того племени снят — дул ценность Dustbone
Devastator на борде из элементалей.

Вечером 14.08 закрыты три хвоста с готовой фактурой: **скидка на покупку** читается
с миньона витрины и попадает в цену совета; **состав племён партии**
копится по витрине (`seenShopCardIds` + `lobbyRaces`), фантомные племена
от двуплеменных карт отсеяны, тринкет для невиданного племени говорит
об этом вслух; **тесты состояния part1** нашли и закрыли настоящую дыру —
дамп переподключения не разбирался, и после реконнекта парсер не знал
ни героя, ни тира. Попутно уточнена семантика `finalPlace`: это текущее
место в таблице, финальным оно становится на FINAL_GAMEOVER.

14.08 закрыт «следующий шаг» фазы 5: **расстановка считается против поля** —
когда следующего противника не видели, целью берутся все виденные борды
и средний исход по ним. Советник перестал молчать полпартии (по замеру
16 из 27 точек решения были немыми при виденных бордах). Подробности,
цена и honest-оговорка про гладкий ландшафт — в docs/position.md,
замер — `npm run spike:field`.

Тем же вечером — пятая партия с оверлеем (part13, Хроми, 4-е место):
пять пунктов обратной связи, все закрыты тестами. Бесплатная активная
сила героя советуется (прежде отсекалась условием `cost > 0`), открытие
модального выбора вошло в ключ положения (оверлей показывал «НИЧЕГО»
поверх раскопки), носитель магнита называется и на неполном борде
и выбирается без дубля дара (перерождение на перерождённого — впустую),
заклинание «Destroy a friendly X» целится в жертву, а не «на крупнейшего».

Седьмая партия с оверлеем — part15 (Холли'дэй, **1-е место**, 20:19–20:48
того же 14.08): пять пунктов по пяти скриншотам, все закрыты структурными
правилами с тестами. Пара первого тира больше не держит витрину (порог
тира у второй копии), платная сила «даёт заклинание таверны» советуется
при остатке золота, механика из текста стала синергией (Titus Rivendare
на борде хрипов больше не продаётся), нецелевое заклинание («of each
type») цели не называет, провокация обходит миньонов-«движков»
(Deathstrider). Разбор — в docs/tavern.md, «восьмая порция».

Рабочий цикл, который сложился и работает: игрок играет с оверлеем →
называет неверные советы → его логи становятся фикстурами → правки правил
закрепляются тестами на этих логах. Сырые транскрипты сессий разработки —
в `chat-history/` (не в git).

Три последние итерации цикла — партии 13–14.08 (part9 за Зиреллу, 2-е место,
шесть пунктов; part10 сразу после правок, 6-е место, восемь пунктов;
part11 за Сильвану, 3-е место, семь пунктов — уже без скриншотов, со слов
игрока, моменты найдены в логе по описаниям), все пункты закрыты
и закреплены тестами. По part11: тёмный дар умолкает после трёх зарядов
(счётчик на кнопке), карты-смертники из «Восстания из гробницы» не
советуются в ход получения без хрипа/перерождения, заклинания витрины
видны и покупаются (у них другой CARDTYPE), заморозка ради племени молчит
в ход подъёма, «делать нечего с золотом» превратилось в обновление
от безделья, хвост подъёма стал стадийным. По part9: сила героя с местоимением
в тексте советуется, тринкеты видят племя `MECH`, магнитные мехи не требуют
продажи и называют носителя, модальные выборы (раскопка, тройка, лавка)
разобраны и ранжированы, миньон с племенем в тексте карты держит место
в композиции, несколько розыгрышей складываются в план одной строкой,
советы в оверлее переносятся, а не обрезаются. По part10: заморозка перестала
верить амальгамам и золотым копиям и получила порог от тира, на полном борде
кандидат больше не получает бонусов от собственной жертвы, тройка и вторая
копия покупаются без продажи, заклинания руки (монетка, баффы) видны
и советуются, выбор из заклинаний-сокровищ показан и ранжирован, подъём
называет судьбу остатка золота. Подробности — в [docs/tavern.md](docs/tavern.md),
фактура каналов — в [docs/power-log.md](docs/power-log.md).

### Модули

| модуль | что делает |
|---|---|
| `src/watcher/logPaths.ts` | поиск свежайшей сессии логов Hearthstone |
| `src/watcher/clientConfig.ts` | снятие предела размера логов, проверка и починка |
| `src/watcher/tail.ts` | чтение дописываемого файла по байтовому офсету |
| `src/parser/logLine.ts` | строка лога → уровень, время, источник, отступ |
| `src/parser/entity.ts` | дескрипторы и ссылки на сущности |
| `src/parser/blocks.ts` | сборка блоков, поток событий с контекстом |
| `src/state/players.ts` | определение «кто я» |
| `src/state/reducer.ts` | свёртка событий в `GameState` |
| `src/advisors/battle/episodes.ts` | извлечение боёв с известным исходом |
| `src/advisors/battle/mapper.ts` | `GameState` → входной формат симулятора |
| `src/advisors/battle/simulator.ts` | загрузка карт и прогон боя, общие на всех |
| `src/advisors/battle/calibrate.ts` | сверка предсказаний с фактом |
| `src/advisors/battle/spike.ts` | замеры симулятора, воспроизводимые |
| `src/advisors/position/arrangements.ts` | перестановки борда, дедуп по сигнатуре |
| `src/advisors/position/score.ts` | оценки счётчиками, цели сравнения, значимость |
| `src/advisors/position/search.ts` | поиск с накоплением симуляций у выживших |
| `src/advisors/position/opponent.ts` | против кого считать и насколько картинка стара |
| `src/advisors/position/advisor.ts` | публичный вход советника расстановки |
| `src/advisors/position/rng.ts` | детерминированный ГПСЧ, подмена `Math.random` |
| `src/advisors/position/spike.ts` | замеры фазы 3, воспроизводимые |
| `src/advisors/position/spikeField.ts` | замеры цели-поля: молчание, цена K бордов, качество |
| `src/data/cards.ts` | справочник карт: племя, тир, статы по cardId; пул миньонов тира |
| `src/data/bgStats.ts` | статистика мест героев и тринкетов (снапшот Firestone) |
| `src/data/fixtureGames.ts` | партии текущего билда — один список на все скрипты |
| `src/data/cardStats.ts` | статистика мест миньонов — только для замера, вне рантайма |
| `src/advisors/tavern/measureCardStats.ts` | дамп «кандидат покупки → исход боя» |
| `src/advisors/tavern/statAnalysis.ts` | арифметика замера: ранжирование, связь, перестановки |
| `src/advisors/tavern/rules.ts` | таблицы правил: тайминги, веса, пороги |
| `src/advisors/tavern/turns.ts` | точки решения — состояния до первой траты |
| `src/advisors/tavern/advisor.ts` | правила подъёма, покупки, продажи, реролла |
| `src/advisors/tavern/spend.ts` | план трат хода: цепочка советов на всё золото |
| `src/advisors/tavern/spendQuality.ts` | сверка плана трат с перебором корзин |
| `src/advisors/tavern/spikeBuff.ts` | замер: атака или здоровье при равной сумме |
| `src/advisors/tavern/spikeLevel.ts` | замер частоты: отставание от кривой и подъём первым |
| `src/advisors/tavern/spikeTaunt.ts` | замер: вредит ли провокация носителю ралли |
| `src/advisors/tavern/spikeHand.ts` | замер: что даёт розыгрыш карты, живущей в руке |
| `src/advisors/tavern/spikeHorizon.ts` | замер: сколько ходов таверны остаётся впереди |
| `src/advisors/tavern/buyQuality.ts` | сверка эвристик покупки с симулятором |
| `src/advisors/tavern/simulated.ts` | досчёт верхних покупок боем в живом режиме |
| `src/live/freshness.ts` | предупреждение «снапшот карт отстал от патча» |
| `src/dataset/recorder.ts` | накопление датасета партий для фазы 6 |
| `src/dataset/backfill.ts` | досбор датасета из фикстур, без повторов по отпечатку |
| `src/ml/dataset.ts` | загрузка датасета: дедуп по отпечатку, фильтр билда |
| `src/ml/features.ts` | признаки точки решения для предсказания места |
| `src/ml/ridge.ts` | гребневая регрессия закрытой формы, вся «модель» фазы 6 |
| `src/ml/evaluate.ts` | LOGO по партиям, бейзлайны, полоса, вердикт |
| `src/ml/report.ts` | отчёт замера фазы 6 по предрегистрации docs/ml.md |
| `src/ml/imitation.ts` | замер 2: имитация покупок игрока против советника |
| `src/ml/imitationReport.ts` | отчёт замера 2 по предрегистрации docs/ml.md |
| `src/ml/track.ts` | мониторинг датасета между замерами, без вердикта |
| `src/live/lines.ts` | байтовые порции → строки |
| `src/live/feed.ts` | строки → `GameState`, партия за партией |
| `src/live/watcher.ts` | опрос лога, догон, смена сессии клиента |
| `src/live/position/*` | советник расстановки в отдельном потоке |
| `src/live/advisor.ts` | когда звать советников и когда бросать начатое |
| `src/live/session.ts` | склейка слежения и советников, общая на CLI и оверлей |
| `src/watcher/logConfig.ts` | включение записи Power.log, проверка и починка |
| `src/ui/setup.ts` | проверка обеих настроек игры при запуске |
| `src/live/replay.ts` | проигрывание записи через живой путь |
| `src/ui/format.ts` | как советы выглядят словами, общее на CLI и оверлей |
| `src/ui/watch.ts` | живой режим в терминале |
| `src/overlay/view.ts` | что показывать в оверлее, без DOM и Electron |
| `src/overlay/main.ts` | окно поверх игры |
| `src/ui/demo-phase1.ts` | печать состояния по ходам |
| `src/ui/demo-phase3.ts` | советы по расстановке на боях фикстуры |
| `src/ui/demo-phase4.ts` | советы по таверне на ходах фикстуры |
| `src/ui/capture.ts` | архивация логов поверх лимита клиента |

### Команды

```
npm run watch                       живой режим в терминале
npm run watch -- --replay <лог>     проиграть запись через живой путь
npm run overlay                     сборка и окно поверх игры
npm run build                       сборка в dist/
npm run demo:phase1                 состояние по ходам эталонной партии
npm run demo:phase1 -- <путь>       то же на другом логе
npm run demo:phase3                 советы по расстановке на боях фикстуры
npm run demo:phase3 -- <лог> <ход>  один бой
npm run demo:phase4                 советы по таверне на ходах фикстуры
npm run demo:phase4 -- <лог> <ход>  один ход
npm run calibrate                   предсказания против фактических исходов
npm run calibrate -- <лог>          то же на другой партии
npm run update:cards                свежий снапшот карт (сеть по команде)
npm run update:bgstats              свежая статистика мест (сеть по команде)
npm run dataset:backfill            досбор датасета из фикстур текущего билда
npm run ml:eval                     замер фазы 6: предсказание места против таблицы
npm run ml:imitation                замер 2 фазы 6: имитация покупок против советника
npm run ml:track                    мониторинг накопления: что прибавилось и куда движутся числа
npm run validate:tavern             эвристики покупок против симулятора
npm run validate:spend              план трат хода против перебора корзин
npm run spike:battle                перезамер производительности симулятора
npm run spike:position              перезамер всего, на чём стоит фаза 3
npm run spike:field                 перезамер расстановки против поля бордов
npm run spike:buff                  замер «+3/+1 против +1/+3» на фикстурах
npm run spike:level                 как часто советуется подъём таверны
npm run spike:taunt                 вредит ли провокация носителю ралли
npm run spike:hand                  что даёт розыгрыш карты, живущей в руке
npm run spike:horizon               сколько ходов таверны остаётся впереди
npm run capture                     архивация растущего Power.log
```

### Что в GameState

Фаза, ход, тир, золото (остаток и всего), цена и цель подъёма таверны, живая
цена обновления витрины (`rerollCost` — тег кнопки, бывает нулевой), предел
тира, герой с силой, свой борд и рука с полными признаками и энчантами,
магазин с признаком заморозки, борд противника, аномалия, счётчики
`globalInfo`, следующий и текущий противник, накопленные борды соперников
с ходом, на котором каждый снят, таблица игроков лобби (`lobby`:
герой, тир, hp с бронёй и место каждого из восьми), открытое предложение тринкетов, открытый
модальный выбор (`openChoice`: источник и варианты — раскопка, тройка,
лавка), заклинания руки (`handSpells`: цена и значения плейсхолдеров; тем же
типом описаны и заклинания витрины — у них есть признак `costsHealth`,
тег `BACON_COSTS_HEALTH_TO_BUY`: цена такой карты платится здоровьем,
а не золотом),
цена и остаток зарядов тёмного дара (`darkGiftCost`, `darkGiftCharges` —
теги `COST` и `TAG_SCRIPT_DATA_NUM_2` кнопки),
взятые тринкеты всех игроков (dbfId на героях), карты, виденные в витрине
за партию (`seenShopCardIds` — сырьё состава племён), текущее место
в таблице (`finalPlace`, финальное — на конце партии), номер билда игры
(`buildNumber` — строка `BuildNumber=…` после CREATE_GAME), журнал своих
действий за партию (`actions`: покупки и продажи с картой-целью,
обновления, подъёмы, заморозки, розыгрыши, сила/дар/активации — сырьё
фазы 6).

### Фикстуры

`part1` — партия из 4 кусков через реконнекты, есть дампы переподключения.
`part2` — эталонная, полная партия одним файлом, 41 МБ.
`part3` — сверена по скриншотам игрока.
`part4` — партия 13.08.2026, билд 248348: первая после патча, аномалий нет,
вместо них тёмные дары. Ею и `part5`–`part7` калибруются предсказания.
`part5`–`part7` — три партии одним заходом клиента 13.08, разрезаны
по CREATE_GAME.
`part8` — Скаббс Маслорез, сыграна игроком под фактуру активной силы героя:
10 нажатий силы, кнопка тёмного дара, карта с замком в руке.
`part9` — Зирелла, 13.08, первая партия с работающим оверлеем: пять
скриншотов советов из игры стали обратной связью, каждый пункт — тестом.
Фактура модальных выборов (`DebugPrintEntityChoices`/`SendChoices`),
магнитных мехов, силы героя «даёт миньона местоимением». Итог партии —
2-е место.
`part10` — 14.08, вторая партия с оверлеем, сыграна сразу после правок
по part9: восемь скриншотов — восемь пунктов, каждый тестом. Фактура
заклинаний руки (CARDTYPE=SPELL, живой COST монетки, плейсхолдеры `{N}`
и `TAG_SCRIPT_DATA_NUM`), сокровищ каналом выборов, золотого миньона
в витрине. Итог — 6-е место.
`part11` — 14.08, третья партия (Сильвана): семь пунктов СО СЛОВ игрока,
без скриншотов — моменты найдены в логе по описаниям. Фактура зарядов
тёмного дара, карт-смертников (энчант Badsong), заклинаний витрины
(BATTLEGROUND_SPELL с ценой), тега CARDRACE строками. Итог — 3-е место.
`part12` — 14.08, четвёртая партия (E.T.C.): три пункта по трём скриншотам.
Фактура силы «Discover a Buddy», племени тринкета тегом
`BACON_SUBSET_<RACE>` (в тексте — плейсхолдер `{0}`), заклинания витрины
с целью. Итог — 2-е место.
`part13` — 14.08, пятая партия (Хроми), первая после цели-поля: пять
пунктов — четыре скриншота из чата плюс жалоба словами. Фактура активной
бесплатной силы героя (`HAS_ACTIVATE_POWER` без `COST`, нажатие кладёт
в витрину заклинания), выбора, не меняющего ничего кроме себя, магнита
с даром («Рука-протез»: Magnetic+Reborn, к мехам и нежити), заклинания-
жертвы («Разделка туши»). Итог — 4-е место.
`part14` — 14.08, шестая партия (Сильвана): три пункта (скриншот хода 21
плюс два словами). Фактура заклинания-замены от тринкета (наклейка
Тюремщика: SPELL без COST, ни статов, ни золота в тексте), активаций
миньонов (`HAS_ACTIVATE_POWER` + живой `INTERACTABLE_OBJECT_COST`,
применение — блок PLAY на сущности, стоящей в PLAY), племенного ралли
без своих племени. Итог — 5-е место.
`part15` — 14.08, седьмая партия (Доктор Холли'дэй): пять пунктов по пяти
скриншотам из чата. Фактура платной силы «даёт заклинание таверны»
(«Get a random Tavern spell», COST=1), миньонов-усилителей механик
(Titus Rivendare — AURA, Deathstrider — TRIGGER_VISUAL), заклинания
без выбора цели («of each type»), заклинания с провокацией на борде
с движками. Итог — 1-е место.
`part16` — 14.08 (23:00–23:30), восьмая партия (АПМ-пираты): четыре
пункта — три скриншота из чата плюс словами. Фактура батлкрай-генератора
карт (Oozeling Gladiator), тавернного баффа соседям (Surfing Sylvar,
END_OF_TURN + adjacent), нецелевого заклинания с числительным (Healthy
Bounty, «four friendly»), энчанта Badsong от БЕЗОБИДНЫХ источников
(Friendly Bounty, Chef's Choice, награда за тройку — смертность несёт
только текст создателя), заклинания-замка Lockbox. Итог — 4-е место.
`part17` — 15.08 (15:57–16:22), девятая партия (элементали, **1-е место**):
четыре пункта по пяти скриншотам, два из них — вопросы. Фактура заклинания
витрины, дающего миньона (Enchanted Lasso, `BG28_512`, «Steal a random
minion from the Tavern», COST=2), склеенного золотого варианта в тексте
снапшота (Fortify: «+{1} Health and Taunt.**3[x]**Give a minion +{0}/+{1}
and Taunt.»), миньона с триггером о себе (Wildfire Elemental, «After this
attacks…»), полного борда поздней партии (ход 25: семь миньонов
по 130–780 статов при витрине в 38/43). Итог — 1-е место.
`part18` — 16.08 (21:47–22:11), десятая партия (наги на заклинаниях,
4-е место) и ПЕРВАЯ с планом трат хода: все три пункта — про него. Фактура
карты, чья ценность реализуется продажей (River Skipper, `BG33_140`, «When
you sell this, get a random Tier 1 minion» — продажа даёт золото и обещанное,
и шестое золото открывает вторую покупку), обратной стороны синергии
по механике (борд наг живёт заклинаниями, а в витрине нага со Spellcraft —
источник этих заклинаний), платного обновления на остаток после подъёма.
Итог — 4-е место.
`part19` — 16–17.08 (23:51–00:20), одиннадцатая партия (Nightmare Lord
Xavius, демоны, **1-е место**): четыре пункта по четырём скриншотам.
Фактура заморозки, которую надо продлевать (заклинание витрины пережило
подъём таверны), модального заклинания «Choose One» (Alliance Flag
`BG31_880` → ветви-карты `BG31_880t` Allied Mace «+{0}/+{1}»
и `BG31_880t2` Allied Buckler «+{2}/+{3}», теги NUM_1..4 = 3,1,1,3,
в логе ветви приходят сущностями с `PARENT_CARD`), миньона с ралли
вторым номером расстановки (Tusked Camper), ауры на чужих в жертвах
продажи (Brann Bronzebeard 27/29 при борде в сотни статов).
`part20` — 17.08 (14:34–15:02), двенадцатая партия (пираты с квилбоарами,
**1-е место** при 3 hp): один пункт, и он про ЧАСТОТУ совета. Фактура
ШКАЛЫ ХОДОВ: ход 15 — восьмой ход таверны (десять золота, тир 5, hp 22,
седьмое место, подъём за все десять), где кривая, сравниваемая с сырым
`turn`, требовала тир 6 и объявляла отставание в два тира. Партия же
показывает обратное опасению игрока: таверну он там не поднял, тир 6 взял
на четырнадцатом ходу таверны и с седьмого места пришёл первым.
`part21` — 17.08 (15:47–16:12), тринадцатая партия (наги на заклинаниях,
2-е место): два пункта по двум скриншотам, оба про ЗАКЛИНАНИЕ НА МИНЬОНА.
Фактура магнитов заклинаний (Lava Lurker `BG23_009` — «The first Spellcraft
spell played from hand on this each turn is permanent», дневной заряд
в живом теге `TAG_SCRIPT_DATA_NUM_1`; Fleeing Fugitive `BG36_921` —
«Whenever you cast a spell on this, gain +{0} Health», у нас {0}=1 тем же
тегом), ВРЕМЕННОГО усиления («+2 Attack until next turn» у чародейского
токена Mini-Trident `BG23_000t` — к следующему ходу атака возвращается),
носителя ралли с призывом ИЗ РУКИ (Expert Aviator `BG34_140` — из-за него
в симулятор поехала рука).
`part22` — 17.08 (17:23–17:53), четырнадцатая партия (Грибомант Флургл,
мурлоки, **1-е место**): пять пунктов по пяти скриншотам, три из них
об одном — РУКА ЭТО ПОЗИЦИЯ. Фактура: миньоны, работающие ИЗ руки
(Flighty Scout `BG32_330` — «Start of Combat: If this minion is in your
hand, summon a copy of it»; Bream Counter `BG26_137` — «While this is in
your hand, after you play a Murloc, gain +{0}/+{1}», к ходу 23 вырос
до 670/668) и миньоны, которые рукой ПИТАЮТСЯ (Costume Enthusiast
`BG34_142`). Все 14 карт пула с «in your hand» — мурлоки. Здесь же сила
героя, называющая продажу и племя («Рыбалка» `TB_BaconShop_HP_056`),
и Бранн `BG_LOE_077` при выборе из кличевых карт (`BG35_143` — «Battlecry
and Deathrattle: Get a Deepwater Clan»).
`part23` — 17.08 (19:21–19:42), пятнадцатая партия (Nightmare Lord Xavius,
мехи с пиратами, 7-е место) и ПЕРВАЯ обратная связь из проигранной партии.
Три пункта по двум скриншотам, и все три про ЦЕНУ ДЕЙСТВИЯ В ЗОЛОТЕ.
Фактура: план, запирающий остаток (ход 5 — пять золота, тёмный дар за 3
и два сгоревших при прокрутке Oozeling Gladiator `BG27_002` за чистых 2,
стоявшей второй строкой); заклинание витрины, называющее ТИР (Recruit
a Trainee `BG28_504`, «Get a random Tier 1 minion» за 2, на третьем тире
оценивалось средним по витрине); заклинание витрины ДОРОЖЕ покупки
(Planar Telescope `BG28_521`, «Discover a minion of your most common
type» за 4, держало витрину при нулевом золоте); заклинание бесплатных
обновлений (Leaf Through the Pages `BG28_827`, «Gain 2 free Refreshes»
за 1, советнику было невидимо целиком). Здесь же лог отвечает, ЧТО такое
тёмный дар: блок PLAY на кнопке `BG36_Button_DarkGift` открывает
`ChoiceType=GENERAL` с источником `BG36_MidGameEffect_010` и ТРЕМЯ
МИНЬОНАМИ в `Entities[0..2]`.
`part24` — 17.08 (21:03–21:28), шестнадцатая партия (Тесс Златопряха,
демоны, 5-е место). Четыре пункта по четырём скриншотам; вместе с ней
пришла ТАБЛИЦА ТИРОВ ТЁМНОГО ДАРА. Фактура: заклинание витрины, дающее
золото (Hasty Excavation `BG28_571`, «Gain 1 Gold» — план обещал
«откроется покупка», а золото до следующего шага не доезжало);
активация-поглощение (Soulkeeping Jailer `BG36_503`, «Activate ({0}):
Your Demons each consume a random minion in the Tavern to gain its
stats» — семь демонов при витрине по 24 стата); канонический подъём
на шести золотых (ход 7, четвёртый ход таверны, цена 5, остаток 1
сгорает); витрина ВТОРОГО тира, целиком набранная картами ПЕРВОГО
(ход 3) — на ней и видно, что «свежая карта» считалась не по тем тирам.
`part25` — 17.08 (22:44–23:15), семнадцатая партия (Murozond, Unbounded
`BG34_HERO_000`, 3-е место). Два пункта по одному скриншоту и словам,
и оба про КАРТУ, КОТОРАЯ ПЛАТИТ ПРОДАЖЕЙ. Фактура: River Skipper
`BG33_140` в витрине хода 3 при НУЛЕВОМ золоте (игрок заморозил витрину
сам в 22:46:41 и на ходу 5 разменял пять золотых на два тела: купил
за 3, продал за 1 — пришёл Tusked Camper, — и купил ещё одного);
Patient Scout `BG24_715` («When you sell this, Discover a Tier 1 minion»)
на ходу 7 — та же цепочка за чистых 2, а следом Ominous Seer `BG31_330`
(«Battlecry: The next Tavern spell you buy costs (1) less») удешевил
Enchanted Lasso `BG28_512` до одного золота, и лассо украло из витрины
Decoy Conjurer 3/4: шесть золота превратились в три тела. Здесь же третий
случай подряд «подъём за 5 из 6 с горящей монетой» — тот, на котором спор
и закрылся решением игрока.
`part26` — 18.08 (00:28–00:54), восемнадцатая партия (Мастер Нгуен
`BG20_HERO_202`, 4-е место). Два пункта, и оба про ВЫБОР, который советник
не брался оценивать. Фактура: смена силы героя КАЖДЫЙ ход
(`ChoiceType=GENERAL`, источник `BG20_HERO_202pt`, в вариантах чужие силы
— двенадцать выборов за партию, от `TB_BaconShop_HP_020` до
`BG34_HERO_001p`); «Дружеская ставка» `TB_BaconShop_HP_081` («Guess which
player will win their next combat») — выбор из двух ГЕРОЕВ, то есть
из двух игроков лобби (ход 13: Торим тир 3 / Смотритель тир 4, ставка
на Торима проиграна); «Наемный детектив» `BG23_HERO_303p2`, угадывающий
миньона следующего противника (ход 7, угадан — в логе монета `BG28_810`),
причём следующего противника мы к тому ходу НИ РАЗУ не видели. Здесь же
ловушка лишних сущностей героя: копии для выборов остаются
с `PLAYER_TECH_LEVEL=0`.
`part27` — 26.08 (16:15–16:42), девятнадцатая партия (пираты,
`TB_BaconShop_HERO_36_SKIN_H`, 3-е место), ПЕРВАЯ на билде 250339
(балансный патч поверх 248348, снапшот от 13.08 её знает — предупреждение
о свежести не сработало). Три пункта по трём скриншотам, все три —
про состояние ПОСРЕДИ хода, после трат. Фактура: заморозка ради Enchanted
Lasso `BG28_512`, обещанная планом вторым шагом и исчезнувшая после
покупки второго из двух РАВНЫХ миньонов (ход 1); ДОЗАПОЛНЕНИЕ замороженной
витрины свежей картой в купленном слоте (ход 3: Molten Rock рядом
с Risen Rider и Harmless Bonehead — те же карты новыми сущностями);
Slimy Shield `BG27_002t` на Oozeling Gladiator 3/3 при Molten Rock 4/4
`BGS_127` («After you play an Elemental…» — тавернный триггер) на борде
(ход 7); «ОБНОВИТЬ» при нулевом золоте, полном борде и бесплатном
обновлении от Air Baller (ход 19, пара Bigwig Bandit `BG33_822` на борде
и в руке, пара Dual-Wield Corsair `BG31_824` на борде).
`part28` — 26.08 (23:11–23:27), двадцатая партия (Galakrond
`TB_BaconShop_HERO_02`, квилбоары, 8-е место), вторая на билде 250339.
Пункт один и про МОДАЛЬНЫЙ ВЫБОР миньона. Фактура: Snare Trapper
`BG36_332` (тир 4, квилбоар 4/4, `CHOOSE_ONE`) с ветвями-картами
`BG36_332t` Ensnare the Target («Get a random Quilboar») и `BG36_332t2`
Collect the Bounty («Increase your maximum Gold by {0}», {0}=1 тегом
`TAG_SCRIPT_DATA_NUM_1`); ветви приходят сущностями `SETASIDE`
с `PARENT_CARD` ещё при появлении карты в витрине (23:19:38), а не при
розыгрыше (23:20:31) — канал выборов молчит; выбранная ветвь читается
полем `SubOption` блока PLAY (`SubOption=0` — первая; ею пришёл Roadboar
`BG20_101`, тир 2 при таверне 4, то есть пул тиров 1..N). Здесь же —
подтверждение, что предел золота бывает выше десяти: `RESOURCES` = 11
у соперника (в part27 — до 19).
`part29` — 27.08 (01:07–01:31), двадцать первая партия (Scoutmaster Tavish
`BG22_HERO_000_SKIN_A`, 3-е место), третья на билде 250339. Пять пунктов
по пяти скриншотам; четыре момента из пяти — состояния ПОСРЕДИ хода.
Фактура: бесплатная активная сила героя с ЦЕЛЬЮ В ВИТРИНЕ («Lock and Load»
`BG22_HERO_000p_Alt` — блок `PLAY` с полем `Target=`, копия цели в
`SETASIDE` под нашим контроллером, заряд `TAG_SCRIPT_DATA_NUM_1` 0 → 1
и обратно в начале боя, сам миньон в `REMOVEDFROMGAME`; 13 нажатий
за партию); цена В ЗДОРОВЬЕ (тег `BACON_COSTS_HEALTH_TO_BUY=1` на Hasty
Excavation `BG28_571` при `COST=3`, покупка — `META_DATA
Meta=SPEND_HEALTH Data=3`, броня 14 → 11 и обратно 14 триггером Soul
Rewinder `BG26_174` «After your hero takes damage, rewind it»);
`BACON_PAIR_CANDIDATE` при копии ТОЛЬКО В РУКЕ (ход 23, `BG31_330`);
заморозки ходов 5 и 7, где следующий ход даёт шесть и семь золота
и лишнего тела уже не выходит; второй Бранн `BG_LOE_077` в руке при
Бранне на борде и шести миньонах (ход 19).
`part30` — 27.08 (19:55–20:23), двадцать вторая партия (Крысиный король
`TB_BaconShop_HERO_12`, 3-е место), четвёртая на билде 250339. Четыре
пункта по четырём скриншотам, три момента — ПОСРЕДИ хода. Фактура: сила-
вариант новой сущностью каждый ход (`TB_BaconShop_HP_041b/g/h/i/j`,
«Discover a \<Tribe\>. Swaps type each turn», `COST=2` живым тегом,
10 нажатий; на ходу 1 игрок нажал силу, разыграл найденного Cord Puller
`BG29_611` и купил на него Tavern Dish Banana `BG28_897`); заклинание
ПО ВИТРИНЕ (Them Apples `BG28_966`, «Give minions in the Tavern
+{0}/+{1}» — блок PLAY с `Target=0`, энчанты +1/+2 на `player=10`);
золото СЛЕДУЮЩЕГО хода (Careful Investment `BG28_800`, «Gain 2 Gold next
turn» за 1); сгорающий золотой хода 19 (борд полон, витрина от трёх,
обновление за 1), потраченный игроком на обновление под триггеры трат
борда (Dual-Wield Corsair `BG31_824`, Enterprising Escapee `BG36_523`);
Gem Day `BG31_893` без ветвей-карт в снапшоте; ивентная смена силы героя
(«Сорванная маска» `EBG_Spell_037` → Enhancification `BG24_HERO_204p`
с хода 25).
`part31` — 27.08 (21:27–21:50), двадцать третья партия (Йогг-Сарон
`TB_BaconShop_HERO_35_SKIN_E`, наги, 6-е место), пятая на билде 250339.
Два пункта по ОДНОМУ скриншоту хода 13 — состоянию ПОСРЕДИ хода (борд
полон, золото 9/9). Фактура: кнопка дара `BG36_Button_DarkGift`
с первого хода (`COST=3`, `NUM_2=3`), три нажатия на ходах 19/21/23
(`NUM_2` 3 → 2 → 1 → 0 через ~10 с после каждого — выбор из трёх открыт
между нажатием и списанием); заклинание витрины A New Sprout `BG33_101`
(«Discover a Tier 1 minion», `COST=3`) при таверне 4 и полном борде;
продажа Rodeo Performer `BG28_550` ради подъёма за 10 при девяти
золотых (`DragSell` → `TechUp05` за секунду) — из-за неё точка решения
хода видит борд из шести, а скриншот снят при семи.
Скриншоты в `data/screenshots/` не коммитятся, выводы из них — в `expected.json`;
скриншоты part9 пришли в чат, пометка в expected — «скриншот из чата».

### Качество предсказаний

На 35 боях четырёх партий своего патча (`part4`–`part7`, билд 248348):
расхождение калибровки **5.1 п.п.**, Brier **0.034** против 0.25 у «всегда
50%», выбросов нет. Передача тринкетов симулятору улучшила расхождение
с 5.8 до 5.1 п.п.; передача РУКИ (17.08, part21) расхождения не сдвинула,
а Brier чуть улучшила (0.035 → 0.034) и убрала единственный пограничный
выброс — на этих партиях носителей ралли мало, цена правки видна не здесь,
а в советах по покупке (part21, ход 5: 77.4% → 97.0%). Числа
перепроверяются командой `npm run calibrate` (по умолчанию только part4,
остальные — аргументами), пороги закреплены тестом
`test/advisors/calibration.test.ts`.

**Калибровать можно только на партиях текущего патча.** 13.08 после
обновления карт и симулятора расхождение на партиях билда 246003 выросло
с 3.9 до 9.0 п.п., хотя ни парсер, ни маппер не менялись: поменялась игра,
а записанный исход относится к своему билду. Старые фикстуры остаются
годными для проверки разбора лога — там они и работают.

### Качество советов по расстановке

Проверено против полного перебора 720 расстановок (part3, ход 8), эталон
по 2000 симуляций на вариант: рекомендация занимает **2.4 место из 720**
в среднем за 3.3 секунды. Выигрыш относительно фактически сыгранного
на боях part3 — до **+14.6 п.п.** доли побед. Подробности и способ
перепроверки — в [docs/position.md](docs/position.md).

### Качество советов по таверне

Эвристики покупок сверены с симулятором на 98 ходах ВСЕХ двенадцати партий
текущего патча (`part4`–`part15`, выборка расширена поздним вечером 14.08):
совпадение с лучшим по бою **82%**, средняя цена расхождения **2.60 п.п.**,
на решающих ходах совпадение **67%** при цене промаха **7.6 п.п.**
Перезамер после правил part15 (порог пары, perTextMechMate) регресса
не показал: до правок на десяти партиях было 83% / 2.25 / 67% / 7.3 —
сдвиги в пределах шума расширенной выборки. Перезамер после правил
JeefHS (подъём при мусорной витрине, ранний реролл) — 81% / 2.63 /
64% / 7.7: покупочные очки правки не трогали, ранжирование покупок
прежнее, расхождение — шум несеяного Монте-Карло на почти-ничейных
ходах (цены 0.1–0.3 п.п.). Перезамер после правок part16 на прежних
двенадцати партиях — 82% / 2.64 / 66% / 7.8, регресса нет; с добавлением
part16 в выборку (13 партий, 108 ходов) — 79% / 3.03 / 58% / 8.9:
просадка целиком от самой партии — АПМ-пираты живут экономическими
цепочками, которые ближайший бой честно наказывает (оговорка методики).
Правило имени карты (`perNamedCardMate`, 15.08) на той же выборке —
80% / 3.02 / 62% / 8.6: сдвиг вверх в пределах шума, регресса нет.
Перезамер после правок part17 на тех же тринадцати партиях — 80% / 2.98 /
61% / 8.7, регресса нет (и не ожидалось: ранжирование покупок правки
не трогали — заморозка, цель заклинания и заклинания витрины в сверку
не входят, она отбирает только советы `buy` с миньоном). С добавлением
part17 в выборку (14 партий, 121 ход) — 79% / 2.89 / 60% / 8.0:
у самой part17 одиннадцать совпадений из двенадцати, единственный промах
(ход 9, Sellemental против Very Hungry Winterfinner) — снова экономическая
карта, наказанная ближайшим боем. Перезамер после правок part18
(двусторонняя синергия по механике) на тех же 14 партиях — 80% / 3.15 /
57% / 9.0: регресса нет, сдвиги — шум несеяного Монте-Карло на почти
ничейных ходах. С добавлением part18 (15 партий, 130 ходов) —
78% / 2.98 / 57% / 8.7. Перезамер после правок part19 (аура не в жертвы,
клич в таблице механик) на тех же 15 партиях — 78% / 2.98 / 56% / 8.8:
регресса нет, разошёлся один решающий ход из 43. С добавлением part19
(16 партий, 141 ход) — **80% / 2.71 / 56% / 7.9**: сама part19 совпала
почти везде. Перезамер после правки шкалы ходов (part20) на тех же
16 партиях — 81% / 2.75 / 60% / 8.0: ранжирование покупок правка
не трогает вовсе (сверка отбирает советы `buy`), и сдвиги — шум
несеяного Монте-Карло, включая состав «решающих» ходов (48 против 43).
С добавлением part20 (17 партий, 153 хода) — **78% / 2.58 / 59% / 7.8**.
Перезамер после правок part21 (магнит заклинаний в ценности покупки,
рука в симуляторе) на тех же 17 партиях — 79% / 2.57 / 56% / 7.7:
регресса нет. Здесь правка трогает обе стороны сверки сразу — и порядок
покупок (у магнита появилось слагаемое), и сам эталон (рука меняет исходы
боёв с ралли), поэтому состав «решающих» ходов сдвинулся (50 против 48).
С добавлением part21 (18 партий, 163 хода) — **79% / 2.54 / 59% / 7.5**;
у самой part21 семь совпадений из десяти, и оба крупных расхождения —
ходы 7 и 17, где ближайший бой предпочёл тело экономике и связи по племени.
Перезамер после правок part22 (сила героя, удвоитель механики) на тех же
18 партиях — 77% / 2.53 / 54% / 7.4: совпадение просело на 2 п.п., а ЦЕНА
расхождения не изменилась вовсе (2.53 против 2.54, 7.4 против 7.5) —
разошлись почти ничейные ходы. Причина названа заранее: слагаемое силы
героя тянет к экономическим картам, а ближайший бой экономику наказывает
(part22, ход 1: River Skipper 1/1 против Glim Guardian 1/4 — по бою дракон
лучше на 50 п.п., игрок попросил обратного и партию выиграл).
С добавлением part22 (19 партий, 177 ходов) — **76% / 3.20 / 52% / 9.1**:
все четыре крупных расхождения самой part22 — там, где ближайший бой
не видит, ради чего карта берётся (экономика; разведчик, воюющий ИЗ РУКИ).
Перезамер после правок part23 (названный тир, наценка, развилка плана,
планка заморозки) на тех же 19 партиях — 76% / 3.14 / 54% / 8.9: регресса
нет, три числа из четырёх чуть лучше. Иного и не ожидалось — ранжирование
ПОКУПОК трогают только две правки, и обе про заклинания витрины, которые
в эту сверку не входят вовсе. С добавлением part23 (20 партий, 187 ходов)
— **76% / 3.09 / 52% / 9.0**; сама part23 совпала на восьми ходах
из десяти, оба расхождения дешёвые (6.1 и 3.7 п.п.) и оба — ставки
на следующие ходы (тройка и магнит), которые игрок сыграл сам.
С добавлением part26 (23 партии, 214 ходов) — **76% / 3.52 / 53% / 9.3**;
правки part26 живут в разборе ВЫБОРА (сила героя, ставка) и покупок
не касаются вовсе, так что это чистое влияние самой партии.
С добавлением part25 (22 партии, 206 ходов) — **76% / 3.32 / 51% / 9.0**;
правки part25 ранжирование покупок не трогают вовсе (прокрутка
и заморозка советов `buy` с миньоном не порождают, а развилка живёт
в плане), так что это чистое влияние самой партии.
С добавлением part24 (21 партия, 194 хода) — **76% / 3.54 / 53% / 9.7**;
правки part24 ранжирование покупок не трогают вовсе (дар, активация
и золото заклинания советов `buy` с миньоном не порождают, правка тиров
живёт в заморозке), так что весь сдвиг — от самой партии: четыре
расхождения из семи, и одно дорогое — ход 3, 75.7 п.п. при разбросе
ровно 75.7, то есть ранний насыщенный бой, а не выброс.
Правки 14.08 — структурные, по диагнозу промахов: боевой
эффект из текста («Rally:», «Summon») и кап щита/вихря/перерождения телом —
разгромные промахи в 50–100 п.п. исчезли, цена ошибок упала вдвое.
Совпадение на решающих по-прежнему скромное — решающий ход и есть тот,
где нужен симулятор; остаток расхождений — экономические карты, которые
ближайший бой честно наказывает (оговорка методики). Случайный выбор дал бы
20–33%. Веса по числам не подгонялись — только структурные правила
с перезамером. Подробности — в [docs/tavern.md](docs/tavern.md).

13.08 правила дважды переработаны по обратной связи игрока: подъём таверны
при отставании ставится выше лучшей покупки (шкалы прежде были несравнимы),
кривая сдвинута на стандартную («тир 2 вторым ходом»), подъём на чётный тир
обещает расширение витрины (замерено: 3/4/4/5/5), появились «разыграть
из руки» и экономическое слагаемое ценности по тексту карты (River Skipper
и родня), заморозка советуется только ради копии под тройку или собираемого
племени — незамороженная витрина обновляется бесплатно.

### Живой режим

Состояние в конце догона — это «сейчас», а не история: обновление приходит
один раз на чтение, после применения всех строк. Правило «на догоне
не советуем» выбрасывало ровно его, и помощник, запущенный посреди партии,
молчал. Закреплено тестом на дописываемом файле.

Живой путь даёт ровно то же состояние, что и пакетный, — это проверяется
тестом на порциях случайного размера, и это единственная защита от их
молчаливого расхождения. Догон файла с начала стоит 234 мс на 41 МБ,
поэтому отдельного быстрого пути не понадобилось. Подробности —
в [docs/live.md](docs/live.md).

**Расстановку в живом режиме почти всегда считать не против кого.** Замер
по всем ходам таверны обеих фикстур: до 13-го хода включительно следующий
противник ни разу не оказался из числа виденных, дальше картинке 7–17 ходов.
Это не дефект реализации — соперников семеро, бой с каждым случается однажды.
Следствие для продукта: советник расстановки в живой игре молчит большую
часть партии, и об этом сказано вслух, а не скрыто.

### Решения, которые НЕ надо переоткрывать

Каждое подтверждено замером или фактурой, ссылки — в `docs/`.

- **Ключ кэша обязан покрывать ВСЁ, от чего зависит ответ, — иначе первый
  спросивший отвечает за всех.** Советник кэширует дорогое дважды:
  разбор текста карты (`memoByCard`) и ожидание по пулу тиров
  (`averagePoolValue`, сотня `minionValue` на вызов). Первый ключуется
  правилами, справочником и картой; второй — теми же правилами
  и справочником, ссылкой на массив борда и СТРОКОЙ из всего остального:
  состав руки (копии считаются и по ней), заклинания руки (слагаемое
  магнита), сила героя (её текст входит в ценность покупки с part22).
  Прежний ключ знал только тиры и ДЛИНУ руки, и опаснее всего был третий
  случай: розыгрыш заклинания руки не меняет НИ ссылки на борд
  (`withoutMagnetCharge` возвращает тот же массив, когда заряд не тратится),
  НИ длины руки — ключ выходил буквально тот же, и следующий шаг плана
  считал планку заморозки и тёмный дар по состоянию, которого уже нет.
  Правило общее: кэш — это утверждение «ответ зависит только от ключа»,
  и проверять его надо списком аргументов той функции, что под кэшем,
  а не тем, что менялось в голове при написании.
- **Счётчик, который читают ПО ЗАПРОСУ, обязан считать тот, кто
  спрашивает.** `spellMagnetGain` читает дневной заряд хранителя
  из `scriptData[0]` самого миньона и потому на всех вопросах отвечает
  одинаково. Ценность покупки складывала выгоду по всем заклинаниям руки
  — два трезубца давали +4 стата вместо +2, — а план списывал заряд
  через `scriptData.map`, который на ПУСТОМ массиве не делает ничего:
  ровно в том случае, который обе стороны читают как «тег не проставлен,
  значит заряд есть». Обе половины чинятся одинаково — вести счёт
  на стороне спрашивающего и писать заряд по индексу, а не отображением.
- **`grantsGold` — ВАЛОВОЕ золото из текста, потому что цену
  `applyRecommendation` вычитает само.** Общая для всех шагов строка
  `gold − rec.cost` не знает, что за действие она обслуживает, и «чистое»
  значение вычитает цену дважды. Поле заполнялось чистым у заклинания
  руки и валовым у заклинания витрины: симптом part24 («откроется покупка
  X», а следующим шагом покупка не открывается) для карт руки не был
  вылечен вовсе. Правило шире одного поля: у величины, которую складывают
  с результатом общей формулы, договорённость «валовое или чистое» одна
  на все точки заполнения, и записывать её надо у ПОЛЯ, а не у случая.
- **Число, которым СОРТИРУЮТ список, и число, которым ОТБИРАЮТ кандидата,
  — разные числа.** У прокрутки бамп `max(base, лучшая покупка + 0.5)`
  отвечает на вопрос «идти ли ей впереди покупки», а отбор лучшей
  прокрутки — на вопрос «какая из них ценнее». Пока оба жили в одном
  поле, слабый генератор выигрывал отбор просто потому, что помещался
  в один ход с дорогой покупкой, и совет называл его число, посчитанное
  не про него.
- **Ценность СВОЕГО миньона всегда считается против ОСТАЛЬНОГО борда.**
  Это записано у `ownValue` с part9, но `sellForGoldRule` завёл свою
  базу — борд из одного кандидата, — и на ней карта числила соплеменником
  саму себя (`tribeMates` себя не исключает), теряла связи по тексту
  и имени, а боевой эффект «вашим мурлокам» обнулялся вместе с бордом.
  Скипер на борде из пяти мурлоков выходил дешевле, чем его же оценивают
  все прочие правила, и продавался тем охотнее, чем лучше синергия,
  которую он теряет. Общая функция теперь одна — `ownBreakdown`.
- **Герой сводится к базовой карте везде, где его сравнивают, а не только
  в статистике.** Варианты «Дружеской ставки» приходят БАЗОВЫМИ картами,
  а в таблице лобби тот же игрок стоит со скином (`_SKIN_E`, `_SKIN_C4` —
  в фикстурах их полно). Сырое сравнение их не сводит, и половина ставки
  уходила в «оценить не берёмся». `baseHeroCardId` живёт в `bgStats.ts`
  и экспортируется: второе определение того же правила рядом — ровно
  тот способ, которым расходятся списки.
- **Список партий текущего билда — ОДИН (`CURRENT_BUILD_PARTS`).** Он жил
  семью копиями и разъехался: сверки и `spike:level` дошли до part26,
  а замеры `buff`/`taunt`/`hand` и досбор датасета остались на part21–22.
  Увидеть это прогоном нельзя — каждый скрипт зелен на том подмножестве,
  которое сам же и перечислил. Цена общего списка названа вслух:
  добавление фикстуры делает записанные числа замеров устаревшими
  до перезапуска, и перезапускать надо все замеры сразу.
- **Идемпотентность датасета — ПО СОДЕРЖАНИЮ партии, а не по имени
  файла.** Путей записи два: живой режим называет файл временем, досбор —
  номером фикстуры, и партия, сыгранная с оверлеем и потом положенная
  в фикстуры, проходит обоими. Так в датасете и оказались 28 записей
  на 23 партии: part17–part21 лежат дважды. Отпечаток (`gameSignature`)
  — билд, герой, место, ход и витрина ПЕРВОЙ точки решения; билда с героем
  и местом мало (две партии одним героем с одним местом в датасете уже
  есть), а витрина первого хода тождественна у обеих записей одной партии,
  потому что живой и пакетный пути дают одно состояние. Чистить уже
  задвоенное скрипт не берётся — он их называет, решает владелец данных.
- **Канал-источник — `GameState.DebugPrintPower`.** Приходит раньше
  (20 677 против 131) и именует сущности числовыми id.
- **Неизвестные счётчики `globalInfo` нулями подставлять нельзя.**
  Замерено: калибровка ухудшается вдвое (3.9 → 9.1 п.п.). Отсутствие
  лучше выдуманного нуля.
- **Золотой миньон — тег `PREMIUM`, не суффикс `_G`.** 25 золотых
  в эталонной партии суффикса не имеют.
- **`cards.collectible.json` из ТЗ не годится** — нужен полный набор
  Firestone с `techLevel`, `isBaconPool`, `races`.
- **Билд и ПАТЧ — разные вещи, и отбор партий идёт по патчу**
  (`src/data/builds.ts`, решение игрока 26.08). Правило «учиться можно
  только на партиях текущего билда» выведено из контентного патча
  246003 → 248348, где калибровка ухудшилась с 3.9 до 9.0 п.п. без единой
  правки кода. Но 250339 оказался балансом и багфиксами поверх 248348:
  игрок прочитал патчноут, а число подтвердило — в его первой партии
  на новом билде 73 карты в зонах советов и НИ ОДНОЙ незнакомой снапшоту
  (у контентного патча их было 41 из 216). Выбрасывать из-за такого
  23 накопленные партии — терять данные на формальности, поэтому билды
  объединены в группу таблицей, а не отключением фильтра: контентный патч
  группы не получит и обнулит накопление честно. Цена допущения названа
  вслух: баланс МЕНЯЕТ силу отдельных карт, а веса ценности покупки этого
  не знают — если замеры начнут вести себя странно, первый подозреваемый
  эта таблица.
- **Снапшот карт надо обновлять с патчем.** Замер 13.08: снапшот от 04.08
  не знал 41 карту из 216 в партии нового билда, почти все — набор BG36.
  Незнакомая карта это не «чуть хуже совет», а совет вслепую.
  Команда `npm run update:cards`.
- **Воспроизводимость симулятора держится на его собственном ГПСЧ, а не
  на подмене `Math.random`.** С версии 1.1.737 пакет захватывает
  `Math.random` в замыкание при загрузке модуля, и поздняя подмена глобали
  на него не действует: три прогона с одним зерном дали 57.0, 58.3 и 52.3%
  побед. Используется пакетный `withRng(createSeededRng(seed), …)`.
- **Фаза 3 выполнима.** Моя ранняя оценка «17 минут на перебор» была
  ошибкой: я принял бюджет ТЗ за факт производительности. Полный перебор
  5040 расстановок — около четырёх минут в поток (замер с прогревом:
  120 мкс на симуляцию плюс 8.5 мс постоянных на вызов), локальный поиск
  укладывается в 3–4 с.
- **Общие случайные числа для сравнения расстановок НЕ работают.** Это было
  предписано ТЗ и моими же заметками, но замер показал 0.9× на близких
  кандидатах, то есть выигрыша нет вовсе: два порядка миньонов расходятся
  по числу бросков с первого размена. Работает другое — накопление симуляций
  у выживших кандидатов. Числа в `docs/position.md`.
- **Опции прогона симулятора надо задавать явно.** Умолчания пакета доигрывают
  бой ради образцов, пересчитывают перцентили каждые 200 симуляций и молча
  обрывают счёт на 8 секундах. Всё выключено в `advisors/battle/simulator.ts`.
- **Значимость считается по величине сравнения, а не по доле побед.** Бывают
  бои, где все расстановки выигрывают в 0% случаев, а решается всё долей
  ничьих; и бывают бои с нулевой дисперсией, где ошибка равна нулю и
  «значимым» становится любой разводящий довесок. Оба случая стоили тихо
  неверного совета и закрыты тестами.
- **Кривая подъёма написана в ходах ТАВЕРНЫ, а `GameState.turn` считает
  и бой.** Ход таверны N — это `turn = 2N − 1`, и золота на нём
  `min(2 + N, 10)`: наш `turn = 15` — восьмой ход таверны с десятью
  золотыми, а не пятнадцатый ход партии. Сравнение таблицы `levelling`
  с сырым `turn` разгоняло кривую вдвое: «тир 6 с хода 11» читалось
  как «с ШЕСТОГО хода таверны», где восемь золота и тир 6 недостижим
  в принципе, — и советник числил отставание в 87% точек решения, ставя
  подъём верхним советом в 50% (жалоба part20 «очень часто советует
  улучшить таверну»; после `tavernTurnOf` — 45% и 30%, замер
  `npm run spike:level`). Правило общее: числа, взятые из разговоров
  об игре и чужих таблиц, живут в ходах таверны и переводятся; числа,
  ЗАМЕРЕННЫЕ у нас (`trinketOfferTurns: [11, 17]`), живут в нашей
  нумерации и перевода не хотят. Совет обязан называть ход в той шкале,
  в которой считает, — иначе игрок не может сверить причину.
- **РУКА идёт в симулятор вместе с бордом.** Часть карт пула играет рукой
  прямо в бою: «Rally: Summon the highest-Attack minion from your hand for
  this combat only» (Expert Aviator), «Whenever this takes damage, give
  a minion in your hand +2/+1» (Very Hungry Winterfinner). Симулятор это
  УМЕЕТ (`expert-aviator.js` читает `attackingHero.hand`), а маппер руку
  не передавал вовсе — и строка «по бою» оценивала носителя ралли как голое
  тело: на part21 (ход 5) мурлок давал 77.4% вместо 97.0%. Миньон руки
  обязан нести `maxHealth`: кандидатов на призыв симулятор отбирает
  условием `!!e.maxHealth`, а тег в руке бывает пустым. Поле `playerHand`
  необязательное — у старых эпизодов руки нет, и отсутствие честнее пустого
  списка. Пороги калибровки правка не сдвинула.
- **«Временное» усиление ищется в СВОЁМ предложении, а не по всему тексту.**
  У Undersea Mount «Give a minion +{0}/+{1}. If it's a Naga, also give it
  Windfury until next turn» временна только вихревая половина, статы
  постоянны. Поиск слов по всей карте объявил бы временными и их — тот же
  класс тихой ошибки, что склеенный золотой текст (part17) и сложенные
  ветви «Choose One» (part19). Временных чародейских токенов в пуле пять
  из 22, среди 206 заклинаний витрины — три со статами.
- **Магнит заклинаний бьёт «крупнейшее тело» при выборе цели** (part21,
  ход 9). Магнит — миньон, чей текст говорит о заклинании, применённом
  К НЕМУ; таких в пуле семь. Выгода считается ЧИСЛОМ и только там, где
  число есть: у ХРАНИТЕЛЯ (Lava Lurker, «The first Spellcraft spell played
  from hand on this each turn is permanent») это временная часть усиления,
  которая станет постоянной (+2 у Mini-Trident), у РАСТУЩЕГО (Fleeing
  Fugitive, «gain +{0} Health») — его собственный плейсхолдер (+1). Двойка
  против единицы и сменила цель. Остальным пяти магнитам выгода честно
  равна нулю, и цель у них выбирается как прежде: «не берёмся судить»
  лучше выдуманного веса. Чародейское заклинание опознаётся соглашением
  идентификаторов `<id миньона>t` (проверено на всех 22 чародеях пула),
  дневной заряд хранителя — живым тегом `TAG_SCRIPT_DATA_NUM_1`
  («({0} left!)»), и план тратит его один раз за ход.
- **Заклинания РУКИ идут в ценность покупки магнита из витрины** (part21,
  ход 5). Три заклинания в руке при беглеце в витрине — это +3 стата
  навсегда, и связь читается из состояния. Своего веса у слагаемого нет
  намеренно: выгода измерена в статах, а у статов цена уже есть. Верхней
  покупкой беглеца это не сделало (9.0 против 10.0 у мурлока, и по бою
  55.1% против 97.0%) — слагаемое добавлено потому, что оно ЕСТЬ, а не
  потому, что должно было перевернуть совет.
- **Провокация на носителе ралли — ЗАМЕРЕНО 17.08, разницы не видно.**
  Довод игрока (ралли срабатывает при АТАКЕ носителя, а провокация зовёт
  на него удары — можно не дожить) проверялся замером, а не мнением:
  78 точек решения 23 партий билда, предрегистрация закоммичена
  до прогона. Основная пара (двигается только провокация) — **−0.241 п.п.
  при пороге приёмки 0.359**; знак в сторону опасения, но порога не берёт.
  Правила нет. Читать как «польза не показана»: у 60 точек из 78 обе ветви
  дали тождественный исход, эффективная выборка мала. (Перезамер 18.08
  на общем списке партий; прежние числа — −0.332 при пороге 0.448
  на 62 точках, вердикт тот же.) Замер стал возможен
  только вместе с передачей руки — до неё ралли-призыв не срабатывал вовсе.
  Перезапуск — `npm run spike:taunt`.
- **Цена подъёма таверны читается из лога, таблицей её не заменить.** Тег
  `COST` кнопки `TB_BaconShopTechUp0N_Button`. Цена падает на единицу за
  каждый ход без подъёма, то есть зависит от всей истории партии.
- **Цена ОБНОВЛЕНИЯ витрины — тоже живой тег, и ноль там значащий.** Тег
  `COST` кнопки `TB_BaconShop_8p_Reroll_Button`; правилом игры это единица,
  но обновление бывает бесплатным целый ход, и это не редкость — шесть партий
  из четырнадцати: заклинание «Leaf Through the Pages» («Gain 2 free
  Refreshes» — part6 ходы 17–21, part7, part8, part13, part17), напарник
  Magnus Manastorm («Two Refreshes each turn are free» — part12 с хода 15),
  экономические тринкеты. Советник считал по таблице и в такие
  ходы был честно неправ: реролл уступал подъёму, которому не отнимал ни
  монеты. Сброс кнопки в начале хода нулями отсеян зоной — на это время
  сущность лежит в `REMOVEDFROMGAME`, а не в `PLAY` (замерено на всех
  14 партиях билда). Это же закрывает и «экономические эффекты тринкетов»:
  цену покупки (`BACON_REDUCE_BUY_COST`), подъёма, обновления и заклинаний
  игра считает сама, а мы читаем результат.
- **Заморозка соревнуется с бесплатным обновлением, а не с пустотой.**
  Незамороженная витрина обновляется в начале хода даром, поэтому морозить
  можно только то, чего свежая витрина не обещает: копию под тройку или
  собираемое племя. Абсолютный порог ценности это правило уже ломал.
- **Порог «выбор что-то решал» в сверках обязан превышать шум Монте-Карло.**
  Порог 1 п.п. при ошибке разности ~1.6 п.п. давал множество «решающих»
  ходов, состав которого зависел от зерна: 33% совпадений при 800 симуляций
  против 62% при 2000 на одних и тех же партиях.
- **Точка решения в таверне — последнее состояние до первой траты золота.**
  Не первое: витрина наполняется не мгновенно, и снимок заставал в ней одного
  миньона вместо пяти. Не конец фазы: там золото всегда 0 из 10.
- **Применение силы героя и тёмного дара видно только блоком `BlockType=PLAY`
  на их сущностях.** `EXHAUSTED` в фикстурах не встречается ни разу (part8:
  10 нажатий силы — 10 блоков). Сброс «нажато в этом ходу» — по смене `TURN`.
  Недоступность силы — тег `LITERALLY_UNPLAYABLE`; он же блокирует карты
  в руке (замок тринкета), и «разыграть» такие не советуется.
- **Порог реролла обязан расти с тиром.** Плоский порог к пятому тиру
  не срабатывал никогда: миньон там дороже порога одним тиром.
- **Взятые тринкеты читаются с героев, а не из зоны.** Теги
  `BACON_FIRST/SECOND_TRINKET_DATABASE_ID` (значение — dbfId) видны у всех
  восьми игроков. Маркер открытого предложения — `BACON_TRINKET=1`
  в `SETASIDE`; `USE_DISCOVER_VISUALS` не годится, он лишь у части
  вариантов. На выборе все варианты уходят в `REMOVEDFROMGAME`.
- **Цель-поле не трогает поиск: симуляции кандидата делятся между бордами
  поровну, счётчики складываются.** Это послойная выборка среднего по полю;
  накопление, финал и значимость работают на слитых счётчиках как на одном
  борде. Замерено (`npm run spike:field`): постоянная часть вызова множится
  на K (13 → 82 мс на кандидата при K=1 → 7), охват отбора падает до ~30
  кандидатов — и это принято, потому что ландшафт среднего исхода глаже
  одиночного (размах 0–2.5 п.п. против десятков). Следствие заявлено:
  против поля советник чаще честно говорит «менять нечего». Взвешивать
  борды по свежести не стали — данных о том, кто вероятнее выпадет, нет.
- **Сверять покупки против последнего увиденного борда противника нельзя.**
  Он устаревает на 7–17 ходов, все сравнения выходят разгромом 100:0,
  а разброс между кандидатами — тождественный ноль. Для сверки берётся борд,
  который вышел в бою следующего хода на самом деле.
- **Модальные выборы — канал `DebugPrintEntityChoices`/`SendChoices`,
  не зоны.** Раскопка, награда за тройку, лавка аксессуаров — всё приходит
  заголовком с id и строками `Entities[i]` с cardId; закрытие — `SendChoices`
  с тем же id. Id вариантов НЕ совпадают с id зонных копий тех же карт,
  зонный путь тринкетов опаздывает (3 из 4 вариантов в точке решения part9),
  выбор героя идёт тем же каналом с `ChoiceType=MULLIGAN`. Строки каналов
  выбора нельзя пропускать через стек блоков — их отступ чужой. Фактура
  в docs/power-log.md.
- **Ключи племён в таблицах правил — строки `races` снапшота.** Племя
  механизмов называется `MECH`; ключ `MECHANICAL` не совпадал ни с чем,
  и тринкет про мехов при пяти мехах на борде получал «своих таких нет»
  (part9, скриншот). Список рас пула: ALL, BEAST, DEMON, DRAGON, ELEMENTAL,
  MECH, MURLOC, NAGA, PIRATE, QUILBOAR, UNDEAD.
- **Шаблоны «сила даёт миньона» обязаны терпеть местоимение и синонимы.**
  У Зиреллы «Choose a minion in the Tavern. … and add it to your hand» —
  «minion» и глагол в разных предложениях (part9, ход 3). У E.T.C.
  «Discover a Buddy» — слова «minion» нет вовсе (part12). Словарь
  пополняется по фактам таких промахов.
- **Племя тринкета — тег `BACON_SUBSET_<RACE>` на его сущности, текст
  вторичен.** У «Разноцветного компаса» племя в тексте — плейсхолдер `{0}`,
  и текстовый разбор его не видел (part12). Имена тегов совпадают
  со строками `races` снапшота (`MECH`).
- **Цель заклинания-усиления — поле рекомендации, а не слова в reason.**
  Оверлей показывает короткую строку действия, и совет без цели
  перекладывал выбор на игрока (part12).
- **Магнитный мех на полном борде не требует продажи.** `mechanics: MODULAR`
  в снапшоте; совет называет носителя — самый крупный свой мех. Прежний
  совет «разыграть магнита, продав N» отдавал миньона даром (part9, ход 13).
- **Тип карты в снапшоте пишется смешанным регистром** («Minion»,
  «Battleground_trinket») — сравнение без нормализации молча не совпадает
  никогда. Справочник приводит к верхнему регистру.
- **На полном борде ценность кандидата считается против борда БЕЗ жертвы,
  и превосходство обязано превышать `sellMargin`.** Иначе кандидат получает
  бонусы от миньона, которого сам вытесняет: дракончик из руки числил
  бордового дракончика «второй копией» и предлагал продать его же (part10,
  ход 11). Тройка с копией на борде и вторая копия в руку продажи не требуют
  вовсе.
- **Заморозка не верит амальгамам и одноимённым.** «Собираемое племя»
  для заморозки считается без `ALL` и без карт с тем же базовым cardId
  (суффикс `_G` снимается — золотая копия та же карта). Порог заморозки
  растёт с тиром, как у реролла: плоский порог на тире 4 держал витрину
  ради дешёвки второго тира (part10, ходы 3 и 13).
- **Плейсхолдеры `{0}`/`{1}` в текстах снапшота — индексы
  `TAG_SCRIPT_DATA_NUM_1..2` на сущности.** «+{0} Attack» у сокровища —
  это NUM_1=10; у Тавматургии единица улучшения в NUM_2 и растёт по партии.
  Парсить литеральные числа из «{1}» — тихо неверно.
- **Заклинания руки видны только белым списком `CARDTYPE=SPELL`.** Монетка
  таверны создаётся с `COST=1`, живой тег падает до нуля — цену читать
  из тега. Экономическое заклинание советуется, лишь когда его золото
  открывает недоступное действие, иначе честно копится (part10, ходы 5 и 9).
- **У заклинаний ДВА типа: в руке SPELL, в витрине BATTLEGROUND_SPELL.**
  Белый список одного типа молча терял витринные — единственные карты
  магазина с ценой в логе (живой `COST`; part11).
- **Заряды тёмного дара — `TAG_SCRIPT_DATA_NUM_2` на кнопке.** Три на
  партию, 3→2→1→0 по нажатиям; после нуля кнопка остаётся в PLAY с ценой,
  и совет по ней был тихо неверным (part11).
- **Энчант `TB_BaconShopBadsongE` значит лишь «розыгрыш бесплатен»,
  смертность несёт ТЕКСТ СОЗДАТЕЛЯ** (part16, уточнение part11). Энчант
  висит на любой добытой карте — от наград за тройку до пиратской
  экономики; смертник — только карта, чей создатель (тег `CREATOR_DBID` →
  dbfId) пишет «It dies if you play it this turn» («Восстание
  из гробницы»). Прежнее чтение «энчант = смертник» прятало розыгрыш
  всей руки. Ход получения отличает `NUM_TURNS_IN_HAND=1`; розыгрыш
  свежего смертника советуется только ради хрипа или перерождения.
- **Бафф «соседям» конца хода применяется к кандидатам расстановки
  ПЕРЕД симуляцией** (`battle/endOfTurn.ts`, part16). Порядок фаз:
  таверна → конец хода → бой, и расстановка решает, кому достанется
  «give adjacent minions +{0} Attack» (Surfing Sylvar) — прежде совет
  двигал носителя из центра краем ради +2.2 п.п. одного боя, теряя бафф
  второму соседу. Величина — живой плейсхолдер, повторы — единица плюс
  свои золотые. Носители ищутся по тексту в снапшоте симулятора — оба
  пути (пакетный и воркер) читают один источник.
- **Батлкрай-генератор карт можно прокрутить: купить-разыграть-продать
  до основной покупки** (`spinRule`, part16). «Battlecry: Get two Slimy
  Shields…» (Oozeling) даёт карты за чистую цену «покупка минус золото
  продажи»; пока золота хватает и на цепочку, и на лучшую покупку,
  прокрутка идёт первой и называет, что купить следом. Не прокручиваются:
  генератор-лучшая-покупка, копия под тройку, при полном борде.
- **Прокручивается и ПРОДАЖНЫЙ генератор — тот, у которого платит
  не клич, а продажа** (part25, ход 7). «When you sell this, Discover
  a Tier 1 minion» (Patient Scout), «…get a random Tier 1 minion» (River
  Skipper): цепочка та же самая, чистая цена та же (покупка минус золото
  продажи), и ценность считается ТОЙ ЖЕ функцией, что у заклинания
  витрины, дающего миньона, — ожидание по пулу НАЗВАННОГО тира плюс
  разница с ценой покупки по курсу золота. Обещать миньона обязательно:
  «get a 3/3 Elemental» (Sellemental) и «give your minions +{0} Attack»
  (Ballers) пулом тира не мерятся. В пуле девять карт с «when you sell
  this», миньона обещают три, и все три называют тир. Запрет «генератора,
  который сам лучшая покупка, не прокручивают» на продажного НЕ
  распространяется: его обещание отдаёт только продажа, и «оставить
  телом» значит не получить обещанного никогда (part18).
- **Витрина держится ради продажного генератора по планке заклинания**
  (part25, ход 3). River Skipper при нулевом золоте — это покупка дешевле
  трёх на следующий ход: пять золотых дают ДВА тела (купить за 3, продать
  за 1 — придёт миньон, — и купить ещё одного) вместо одного и двух
  сгоревших. Планка та же, что у заклинания витрины, и это не совпадение,
  а один и тот же вопрос: стоит ли витрина того, чтобы держать её ради
  предложения дешевле покупки. Числа part25: 8.48 против 7.01. Ветка
  умолкает сама с ростом тира (планка растёт по тирам витрины, обещанный
  текстом тир остаётся первым) — то же «работает для ранней игры», что
  у заклинания. Что по карману сегодня — сегодня и прокручивается,
  витрины это не стоит; полный борд ветку выключает.

  **Запрета `justLevelled` у этой ветки нет НАМЕРЕННО, и переоткрывать
  его не надо** (проверено разбором кода 18.08). Ветке ПЛЕМЕНИ запрет
  нужен потому, что её порог — число от тира из таблицы правил
  (`2 × тир + запас`), и подъём его не двигает: без явного `mates = 0`
  витрина держалась бы ради соплеменника, которого свежая витрина нового
  тира предложит и так. У прокрутки и заклинания порог другой —
  `spellThresholdOf`, то есть ожидание по тирам ВИТРИНЫ
  (`averagePoolValue(shopTiers(state.techLevel))`), а `techLevel`
  в ход подъёма уже НОВЫЙ (редьюсер ставит `techLevelUpTurn = turn`
  тем же тегом, которым меняет тир). Планка поднимается сама, ценность
  прокрутки остаётся на обещанном текстом тире — и ветка гаснет
  арифметикой, а не запретом. Второй запрет поверх этого был бы
  правилом без фактуры.
- **Тексты снапшота переносят строки посреди предложения** — многословные
  шаблоны обязаны писать `\s+` вместо пробела, иначе не совпадают МОЛЧА
  (part16: два тихих промаха подряд — смертники и бафф соседям).
- **У «цены выбора» (Δcost) нулевая точка ОТРИЦАТЕЛЬНА.** Советник угадывает
  лучшую покупку в 80% ходов, и возмущение такого выбора ЛЮБЫМ
  неинформативным довеском в среднем его портит: полоса перемешанных
  данных — от −1.20 до +0.73 при медиане −0.07. Сравнивать Δcost с нулём
  нельзя, только с этой полосой, и печатать её обязательно рядом
  (замер 15.08 без неё читался как «антисигнал»).
- **Порог приёмки замера обязан сверяться с МРЭ ДО прогона.** У ветки
  «цены выбора» минимальный различимый эффект 2.17 п.п. при
  предрегистрированном пороге 0.5 — она не могла сказать «принять» ни при
  какой правде. Причина: эффективных наблюдений 10, а не 108 ходов
  (у 58 ходов исходы всех кандидатов тождественны).
- **Ход 1 в сверках — другой режим, а не выброс.** Бой идёт 1×1, исходы
  насыщены (0/25/50/100), разброс 37.5 п.п. против 6.7 на остальных ходах:
  11% ходов несут почти всю сумму квадратов. Обращение с ним объявлять
  заранее.
- **Совпадение с симулятором честно считать «попал в ЛЮБОЙ максимум».**
  Ничьих за первое место 66 из 108: счёт «по первому максимуму» бесплатно
  награждает исходный порядок и вдвое преувеличивает падение
  (64% против честных 78%).
- **В ход подъёма таверны заморозка ради ПЛЕМЕНИ молчит.** Свежая витрина
  будет уже нового тира; копия под тройку держит витрину по-прежнему
  (part11, ход 9). Заклинания это НЕ касается (part19, ход 3): его
  в свежей витрине не будет вовсе — ни нового тира, ни старого, — и подъём
  не повод его терять. Заморозку каждый ход надо продлевать заново, поэтому
  молчание советника тут не нейтрально: оно отменяет вчерашний совет.
- **Порог заморозки ради заклинания был маржинальным — ПРАВИЛО ОТМЕНЕНО
  17.08 (part23).** Читать надо пункт «Заморозка ради заклинания судится
  ЦЕНОЙ» ниже: маржинальная форма (`threshold − bestKept`) сравнивала
  числа с разной начинкой и на собранном борде падала в ноль. Запись
  оставлена, чтобы довод «заморозка — ДОПОЛНИТЕЛЬНОЕ действие, а не замена
  покупке» не переоткрывался как новый: он верен, но планку из него
  вывести нельзя.
- **Активность силы героя — тег `HAS_ACTIVATE_POWER` на её сущности; цены
  у бесплатной силы нет вовсе (нет тега COST).** Прежнее правило отсекало
  бесплатные условием `cost > 0`, и сила Хроми «Refresh the Tavern» молчала
  всю партию (part13). Советуется только бесплатная И активная сила
  с Refresh-текстом; пассивным «нажать» советовать нельзя.
- **Открытие модального выбора обязано входить в ключ положения.** Раскопка
  открывается, не меняя ни золота, ни бордов, ни руки, — без выбора в ключе
  советник не пересчитывается, и оверлей показывает старые советы поверх
  модального экрана (part13, ход 5). Тот же урок, что с тринкетами в part9.
- **Дар и племена носителя магнита читаются с его карты.** `mechanics`
  минус MODULAR — что дарит (перерождение, щит…); `races` — к кому
  магнитится (рука-протез: к мехам И нежити). Бинарные ключевые слова
  второй раз не дарятся: носитель — крупнейший из тех, у кого дара ещё нет
  (part13, ход 19: перерождение советовалось уже перерождённому). Носитель
  называется и на неполном борде (ход 15). При виденном у соперников яде
  (по накопленным бордам — читаемый факт) носитель со щитом предпочтительнее
  крупного: яд убивает любым касанием, щит его поглощает, магнит со щитом
  в даре снимает угрозу сам. Указано игроком после part13.
- **Заклинание «Destroy a friendly X» целится в жертву, а не «на
  крупнейшего».** Жертва — наименьший свой племени X; без подходящей
  жертвы совет молчит. «Разделка туши» советовалась «на» голема 200/122 —
  уничтожить главную карту борда, к тому же не-нежить (part13, ход 21).
- **Статистика мест — открытые данные Firestone, не hsreplay.** hsreplay
  закрыт от выгрузки (403); Firestone публикует средние места героев
  и тринкетов открытыми JSON (`static.zerotoheroes.com/api/bgs/…`,
  срез last-patch / mmr-100), идентификаторы карт совпадают с нашими.
  Это ЧУЖИЕ агрегированные данные: в советах они всегда подписаны
  «по статистике», малые выборки (<500 партий) отфильтрованы, снапшот
  живёт в `data/bgstats/` и устаревает с патчем. Своих правил ценности
  героев нет и не выдумывается — статистика и есть их свёртка.
- **Выбор героя — тот же канал выборов с `ChoiceType=MULLIGAN`, отдельным
  полем `heroChoice`.** Сущность героя-заготовки при этом уже существует
  (part14) — экран выбора включается по самому выбору, не по отсутствию
  героя. Среди вариантов бывают скины (`_SKIN_A`) — статистика приводится
  к базовой карте.
- **Активация миньона — тег `HAS_ACTIVATE_POWER`, цена — живой тег
  `INTERACTABLE_OBJECT_COST` на нём же.** Цена сходится с плейсхолдером
  «Activate ({N})» → `TAG_SCRIPT_DATA_NUM`. Применение — блок PLAY
  на сущности, СТОЯЩЕЙ в PLAY: розыгрыш из руки отличается зоной (part14,
  Suspicious Prisonguard). Снимается ли доступность тегом после нажатия,
  в фикстурах не видно — «нажато в этом ходу» считается блоками, как
  у силы героя.
- **Заклинание-замена («Destroy a friendly X to get a random X») — свой
  распознанный эффект.** В тексте ни статов, ни золота, и разбор «только
  статы/золото/щит» возвращал null — заклинание наклейки Тюремщика молчало
  всю партию (part14). Цель — наименьший свой подходящего племени.
- **Боевой эффект «вашим \<племени\>» пуст без своих того племени.**
  «Rally: Your Undead have +1 Attack» на борде элементалей не делает
  ничего, а бонус дул ценность (part14, ход 21, Dustbone Devastator).
  Призывов это не касается: «Summon a Beast» приносит тело независимо
  от борда.
- **Скидка на покупку — тег `BACON_REDUCE_BUY_COST` на самом миньоне
  витрины.** part3: 9999 — миньоны даром (кламп в ноль), part4: 2 — цена 1
  у части витрины при трёх у остальных. Цена совета и «по карману ли»
  считаются через `buyCostOf` по каждому миньону отдельно.
- **Состав племён партии — по однoплеменным миньонам витрины, не по
  `CARDRACE`.** Строковый `CARDRACE` показывает одно племя даже
  у двуплеменной карты и создаёт фантомы: MECHANICAL и DRAGON в part11 —
  от «Руки-протеза» (MECH/UNDEAD) и Firescale Hoarder (DRAGON/NAGA),
  партий без мехов и драконов. Витрина предлагает только пул, поэтому
  однoплеменный миньон в ней доказывает племя; двуплеменные и амальгамы —
  нет. `seenShopCardIds` копится в step, а не в snapshot: живой и пакетный
  пути обязаны давать одно состояние.
- **Пара под тройку морозится только картой не ниже тира таверны.** Вторая
  копия — ставка на будущую тройку, а не тройка, и бонус копии сам же
  пробивал порог ценности: заморозка Buzzing Vermin 1/1 первого тира при
  таверне 2 (part15, ход 5). Третья копия собирает тройку немедленно
  и от тира не зависит. **Тот же порог тира — и у ветки ПЛЕМЕНИ** (part22,
  ход 5): снова Buzzing Vermin 1/1 при таверне 2, только держали его как
  «своего по племени» — на борде два зверя. Довод тот же: витрина второго
  тира предложит зверя не хуже. Племя тем и отличается от копии, что
  заменимо — конкретную третью копию ждут, а соплеменника нет.
- **Платная сила «даёт заклинание таверны» советуется как экономика.**
  «Get a random Tavern spell» за 1 (Холли'дэй, part15): ценность —
  примерно цена заклинания в витрине (два золота по курсу), очки за вычетом
  цены силы; сила за 2+ честно молчит. При остатке в 1 золото совет молчал,
  и золото сгорало.
- **Механика, названная словами в тексте, — синергия** (`perTextMechMate`,
  таблица `mechanicTextWords`: DEATHRATTLE, BACON_RALLY,
  BACON_SPELLCRAFT_ID, BATTLECRY). Titus Rivendare
  («Your Deathrattles trigger an extra time», AURA, без племени) на борде
  хрипов был слабейшим по статам и предлагался в продажу (part15, ход 17).
  Собственная механика синергией не считается («Deathrattle:» о себе).
  «Taunt» словами не разбирается: в текстах он описывает призываемых.
- **У заклинания без выбора цели цель не называется.** «Give a friendly
  minion of each type +2/+2» (Misplaced Tea Set, part15) раздаёт сама,
  а совет писал «→ на Deathstrider». Шаблоны `untargetedSpellWords`:
  of each type, random, left-/right-most, all friendly, your minions.
- **Провокация не вешается на миньона-«движка»** (AURA или текст-триггер
  «After/Whenever/At the start…», `engineTextWords`): она зовёт удары,
  а ценность движка — эффект. Slimy Shield советовался на Deathstrider —
  игрок не хочет его в приоритете ударов (part15, ход 19). Цель —
  крупнейший из остальных; борд целиком из движков возвращает крупнейшего.
- **Триггер О СЕБЕ («After this attacks…») движком НЕ считается**
  (`selfTriggerWords`, part17). Правило движка — про эффект от чужих
  действий, которому нужно, чтобы носитель дожил; собственный размен
  провокации не боится. Из семи миньонов пула с «After/Whenever this»
  четыре срабатывают от полученного урона, а один носит провокацию
  в своём же тексте. Прежнее чтение уводило Fortify с Wildfire Elemental
  8/3 на токен Water Droplet 3/3 (ход 11).
- **Усиление не вешается на кандидата в продажу.** Бафф остаётся
  на миньоне навсегда, а слабейшего своего правила сами назовут жертвой
  при первой покупке на полный борд (part17, ход 11: игрок сказал про
  цель «скорее всего продам»). Цель — крупнейший из ОСТАЛЬНЫХ.
- **Заморозка — отложенная покупка и судится правилами покупки.**
  На полном борде кандидат обязан перебивать жертву с запасом
  `sellMargin`, как в `buyRules` (копия и магнит слота не требуют).
  Порог от тира этого не ловит: к концу партии статы витрины растут
  вместе с бордом, и на part17 (ход 25) морозился Crackling Cyclone 38/43
  — 56 очков против порога 14 — при семи своих в 130–780 статов,
  слабейший из которых стоит 150.
- **Заклинание витрины, ДАЮЩЕЕ МИНЬОНА, — это покупка дешевле трёх.**
  «Steal a random minion from the Tavern» (Enchanted Lasso за 2, part17):
  ни статов, ни золота в тексте, и разбор возвращал `null` — заклинание
  было невидимо целиком. Шаблоны общие с силой героя (`givesMinionWords`:
  факт записан в ТЕКСТЕ, а не в том, кто его произносит), ценность — та же
  формула: средний миньон витрины плюс сэкономленное золото по курсу
  (у лассо среднее по витрине — точное ожидание, миньон берётся из неё же).
  Пока не по карману — держит витрину; на полном борде ветка молчит.
  Скидка засчитывается не всегда: у действия ПРЯМО СЕЙЧАС — только когда
  остатка хватает ещё на покупку (при трёх золотых и заклинании за два
  остаётся золотой, который сгорит, и «дешевле покупки» на деле означает
  «слабее покупки»); у заморозки — всегда, она и есть ставка на ход,
  где золота хватит на оба действия.
- **Ветви модального «Choose One» — отдельные карты снапшота `…t` и `…t2`.**
  `BG31_880` (Alliance Flag, «Choose One — Give a minion +{0}/+{1}; or
  +{2}/+{3}») — это `BG31_880t` (Allied Mace, «+{0}/+{1}») и `BG31_880t2`
  (Allied Buckler, «+{2}/+{3}»); плейсхолдеры у ветвей те же, что
  у родителя, и читаются из тегов сущности. Соглашение проверено на всех
  пяти модальных заклинаниях пула; порядок ветвей в тексте родителя
  с порядком карт НЕ совпадает (у Boundless Potential обратный), поэтому
  читается текст каждой ветви, а не позиция. Разбор по ветвям убрал и тихое
  завышение: сумма статов складывала обе ветви (+8 вместо +4) — тот же
  класс ошибки, что склеенный золотой текст (part19, ход 7).
- **При равной сумме статов «+3/+1 против +1/+3» РАЗНИЦЫ НЕ НАЙДЕНО —
  замерено 17.08, перезамерено 18.08.** 252 точки решения 23 партий билда,
  по 2000 симуляций на сторону, предрегистрация до прогона: среднее −1.530 п.п.
  в пользу здоровья при пороге приёмки 1.547 (было −1.27 при 2.03 на 169
  точках шестнадцати партий; вердикт тот же, но запас до порога сузился
  почти в ноль — этот замер стоит перезапускать первым при росте выборки). Читать как «польза
  не показана»: разброс между точками огромен (у ранних бордов четыре
  стата решают бой целиком). Поэтому `buffSplitPreference: null`, и совет
  честно называет ОБЕ ветви — это лучше и молчания, и уверенного выбора,
  за которым ничего нет. Перезапуск — `npm run spike:buff`.
- **Аура на ЧУЖИХ не назначается жертвой продажи, пока есть обычное тело.**
  Признак — механика `AURA` в снапшоте, кроме аур О СЕБЕ («Has +{0}/+{1}
  for each…»), у которых эффект и есть статы. В пуле 17 карт с AURA, слова
  «Has +» делят их начисто: восемь пумпят себя, девять усиливают чужое
  (Brann «Your Battlecries trigger twice», Titus, Drakkari Enchanter…).
  Наша шкала меряет ТЕЛА, и продавать по числу, про которое сами знаем,
  что оно не про эту карту, — ошибка, а не осторожность (part19, ход 27:
  Бранн 27/29 первым уходил в продажу при борде в сотни статов). Борд
  целиком из таких аур возвращает слабейшую из них — место откуда-то
  брать надо.
- **Миньоны бьют слева направо, и «Rally» срабатывает при атаке носителя.**
  Значит, риск не дожить до своего удара растёт к ПРАВОМУ краю борда,
  а не ко второму слоту (part19, ход 9: игрок опасался за Tusked Camper
  вторым номером). Перебор всех 24 расстановок подтвердил совет: 92.9%
  против 91.1% у лучшей расстановки с кампом первым и 90.0% у сыгранной.
- **В снапшоте к тексту части заклинаний приклеен ЗОЛОТОЙ вариант** —
  «…+{1} Health and Taunt.**3[x]**Give a minion +{0}/+{1} and Taunt.»
  (Fortify, part17). Числа обеих версий складывались: +3 здоровья читались
  как «+6 статов». Таких карт шесть, и все шесть — заклинания-усиления,
  то есть ровно те, чьи числа мы читаем. Режется в справочнике
  (`normalizeCardText`) по первому «цифры + `[x]`»; ведущий `[x]` —
  обычная разметка переносов и остаётся.
- **Платное обновление имеет смысл, только если найденное будет на что
  купить** (`paidRerollIsUseful`, part18 ход 7). После подъёма за 5 из 6
  остаётся золотой: витрину нового тира на него не купишь, а заморозить
  найденное значит отдать бесплатное обновление следующего хода. Прежний
  запрет раннего реролла был привязан к ДОСТУПНОМУ подъёму — и в плане,
  где подъём уже сделан, испарялся. С `lateRerollTier` правило снимается:
  там обновление — поиск карты под заморозку.
- **Синергия по механике считается в ОБЕ стороны** (part18, ход 17).
  Прямая: текст кандидата называет механику, свои её несут (Titus
  Rivendare на борде хрипов). Обратная: кандидат НЕСЁТ механику, а тексты
  своих её называют — нага со Spellcraft на борде, где Abyssal Bruiser
  считает «Tavern spells you've cast», а Fleeing Fugitive растёт от «cast
  a spell on this». Без обратной стороны советник вёл к демону на два тира
  выше, «который совсем не подходит композиции». Механика заклинаний —
  `BACON_SPELLCRAFT_ID`, таких текстов в пуле 97. Производитель, который
  сам же механику называет («Spellcraft: …»), в обратную сторону
  не считается.
- **Карта, чья ценность в ПРОДАЖЕ, продаётся, когда золото открывает ещё
  одну трату** (`sellForGoldRule`, `sellValueWords`, part18 ход 5). River
  Skipper обещает отдачу «when you sell this», и держать его ради этого
  слагаемого — не получить его никогда. Условие жёсткое: продажа обязана
  менять ЧИСЛО доступных покупок (пять золотых — один миньон, шесть — два),
  иначе она даёт монету, которой некуда деться. Удерживаемая ценность
  считается без слагаемого экономики; копия под тройку не продаётся.
  Это единственная продажа, которую план предлагает сам, — остальные живут
  внутри покупки (`sellFirst`).
- **Мусорная витрина — довод подняться, а не покупать и не крутить.**
  Правило из базы знаний JeefHS (docs/jeefhs.md), внесено по указанию
  игрока: когда лучший кандидат ниже порога «покупать нечего» (тот же
  порог, что у реролла), подъём получает очки выше слабых покупок —
  прежде подъём «по графику» имел ноль очков и проигрывал любому телу.
  Туда же: до `lateRerollTier` реролл молчит, пока подъём доступен
  и по карману; при недоступном подъёме (здоровье, тир, золото) реролл
  живёт — «золото иначе сгорит».
- **Предложения тринкетов открываются на ходах 11 и 17** (наша нумерация;
  6-й и 9-й ходы таверны) — замерено по всем двенадцати партиям билда
  248348, ходы в `rules.trinketOfferTurns`. Игра подбирает предложение
  под борд (тьюторинг, подтверждён игроком по базе JeefHS) — за ход
  до предложения советник напоминает держать 2+ миньонов желаемого
  племени (`trinketForecast`); амальгамы сигналом не считаются.
- **Тринкеты ранжируются «эффективным местом»: статистика минус
  0.3 места за своего миньона племени** (`trinketPlacePerTribeMinion`,
  курс — σ выборки снапшота). Правило JeefHS, подтверждено игроком:
  сильный нейтральный/экономический тринкет лучше слабого племенного;
  сильная синергия (4+ своих) статистикой не перебивается. Без
  статистики порядок прежний — свои племена первыми.
- **Карта, работающая ИЗ РУКИ, к розыгрышу не советуется.** Признак —
  собственный текст (`handWorkerWords`), и пород две: «Start of Combat:
  If this minion is in your hand, summon a copy of it» (Flighty Scout) —
  тел в бою поровну, копия приходит и без розыгрыша, а слот тратится
  НАВСЕГДА; «While this is in your hand… gain +{0}/+{1}» (Bream Counter,
  Timewarped Astrogill) — розыгрыш останавливает рост, и цена остановки
  ближайшему бою невидима, как у экономики. Купить такую по-прежнему
  советуется: из руки она и работает. В пуле 14 карт с «in your hand»,
  и ВСЕ мурлоки — трое работают из руки, одиннадцать её едят (Costume
  Enthusiast: «Gain the Attack of the highest-Attack minion in your
  hand»). Замер (`npm run spike:hand`) правило НЕ доказывает: он объявлен
  негодным по собственному критерию — контроль порога не взял (0.546 при
  0.937 на перезамере 18.08; было 0.648 при 1.170), потому что лишнее
  тело ближайший бой чаще всего не меняет вовсе (из 85 точек
  контроля разошлись 10). Согласие он показал: у «играет из руки»
  разность розыгрыша осталась нулём (−0.11) при +0.55 у обычной карты
  (part22, ходы 5 и 17).
- **Порог тройки — НЕ константа: его меняет сила героя** (part7, 26.08).
  `Double Time` (`BG34_HERO_002p`) обещает «You only need 2 copies to make
  minions Golden», и лог это подтверждает: все три золотых той партии
  собрались при ДВУХ простых копиях, при контроле в три на обычных героях
  (part17, part19). Число читается из текста силы (`tripleCopies`,
  `tripleCopiesWords`, `copiesForTriple`), а ценность копий считается
  по РАССТОЯНИЮ до тройки: последний вес таблицы `copiesBonus` достаётся
  тому, кого покупка делает золотым. При обычных трёх копиях выходят ровно
  прежние числа. Пока порог был константой, двенадцать очков «собирает
  тройку» стояли на второй копии — на ход позже, чем надо, — и целую
  партию советник недооценивал первую. Сравнений `copiesOwned >= 2`
  по местам больше нет: их было семь, и разъехались бы они молча.
- **Сила, дающая статы за РОЗЫГРЫШ, считается — и ровно один раз**
  (part27, 26.08). `Hat Trick` (`TB_BaconShop_HP_042`): «When you play
  a minion, give it a +1/+1 hat that passes to a friendly minion when
  sold». Число читаемое, шкала наша (`perStatPoint`), своего веса
  у слагаемого нет намеренно. Прибавляется на стороне ПРИОБРЕТЕНИЯ,
  а у своего миньона вычитается (`ownValue`, `sellForGoldRule`): шляпа —
  настоящий энчант, её +1/+1 уже сидят в `attack`/`health`, и второе
  слагаемое было бы той же шляпой, посчитанной дважды. Слагаемое ОДИНАКОВО
  у всех кандидатов, поэтому порядок покупок оно не меняет — меняет
  покупку против обновления и розыгрыш против «ничего». Цикл
  «купить-разыграть-продать» ради шляпы (при продаже она переходит
  на другого) НЕ считается: это правило про лишнее золото на полном борде,
  и вносить его надо с замером — именно его игрок крутил в part27 сотню
  раз (69 покупок, 98 продаж, 199 розыгрышей).
- **Сила героя — такой же читаемый текст, как текст миньона борда.**
  Прежде ценность покупки смотрела только на борд и на саму карту, и герой
  не влиял ни на что, хотя лежит в состоянии с part13. У Флургла сила
  «Рыбалка» («After you sell 5 minions, get a random Murloc») называет
  и ПРОДАЖУ, и ПЛЕМЯ, а советник предлагал дракона вместо River Skipper
  («When you sell this, get a random Tier 1 minion») — карты, чьё обещание
  реализуется ровно тем действием, которое сила вознаграждает (part22,
  ход 1). Слагаемых два, оба ОДНОКРАТНЫЕ (факт о кандидате, а не о числе
  своих): племя из силы — весом `perTextTribeMate`, продажа — весом
  `economy`. **Размер этого механизма пересчитан 26.08 и оказался куда
  меньше, чем здесь стояло:** в пуле Battlegrounds племя называют
  **24 силы из 174**, продажу — **три**. Прежние «140» были посчитаны
  по всем 2147 картам типа HERO_POWER снапшота, то есть по всему
  Hearthstone вместе с Дуэлями и Наёмниками, — фильтра пула в том счёте
  не было. Читать запись надо так: слагаемые верны, но покрывают восьмую
  часть пула, а не большинство сил. Все числа покрытия закреплены тестом
  (`test/advisors/tavern/heroPowers.test.ts`) — в прозе они разъезжаются
  и от патча, и от наших же правок.
- **Удвоитель механики на борде — множитель, а не рядовая связь.** В пуле
  их ровно ТРОЕ, и они делят три механики: Бранн Бронзобород (кличи),
  Titus Rivendare (хрипы), Drakkari Enchanter (конец хода); все трое —
  AURA. `perTextMechMate` считал Бранна за полторы очка, а при нём
  «Battlecry and Deathrattle: Get a Deepwater Clan» приносит ДВЕ карты
  (part22, ход 23: кличевой мурлок стоял вторым после эффекта конца хода,
  удваивать который некому). Очки начисляются ТОЛЬКО там, где известно
  что удваивается: у триггера, приносящего карту, курс уже есть — тот же
  `heroPowerSpellValue`, что у прокрутки генератора. Удвоение эффекта
  без добычи остаётся обычной связью: множитель реален, а цены у него нет.
- **Заклинание, крадущее ИЗ ВИТРИНЫ, оценивается по ОСТАТКУ витрины.**
  «Steal a random minion from the Tavern» (Enchanted Lasso) берёт из той
  же витрины, которую мы оцениваем, а к моменту применения лучшие карты
  будут куплены нами. Среднее по ВСЕЙ витрине считало лучшую карту дважды
  — и «мы её купим», и «лассо может её дать» (part22, ход 7). Признак
  «из витрины» (`givesMinionFromShopWords`) отличается от «даёт миньона»:
  «Get a random Tier 1 minion» берёт из пула, и витрина там ни при чём.
- **Шаг, после которого золото СГОРАЕТ, соревнуется не сам с собой,
  а с цепочкой** (part23, ход 5). Жадный план берёт верхний совет и живёт
  остатком; верхний совет умеет ЗАПЕРЕТЬ остаток — оставить золото,
  которого не хватает ни на что. Пять золота: тёмный дар за 3 (8.0 очков)
  и два сгоревших против прокрутки за чистых 2 (7.5), оставлявшей ровно
  три на покупку (7.0). Разрыв первого шага — полбалла, цена запертого
  остатка — шесть очков по курсу `goldPointValue`. Поэтому при сгорании
  план пробует начать с ближайших соперников верхнего совета и берёт
  цепочку с бо́льшим «сумма очков минус сгоревшее золото». Полного
  перебора корзин это не вводит: развилка ОДНА, на первом шаге, и только
  после признания, что золото сгорает. Порядок самих советов правка
  не трогает — список ранжирует ОТДЕЛЬНЫЕ действия.
- **ПОДЪЁМ ТАВЕРНЫ входит в развилку плана — решение игрока 17.08
  (part25).** Жалоба на «подъём за 5 из 6 с горящей монетой» пришла
  третий раз подряд (part23 ход 5, part24 ход 7, part25 ход 7), и две
  правки по ней были написаны и откачены. Вопрос вынесен игроку
  с числами: подъём — верхний совет на 4-м ходу таверны в 21 партии
  из 22 (`npm run spike:level`), а по датасету 25 партий он на этом ходу
  ВСЕГДА тир 2 и берёт тир 3 на 5-м (8 партий) или 6-м (17). Выбран
  вариант «пусть план сравнивает подъём с цепочкой». Прежнее возражение
  снято ЯВНО, а не спрятано: у подъёма появилось поле `standaloneScore`
  — своя ценность, то есть одна срочность, без лучшей покупки внутри;
  в очках СПИСКА лучшая покупка стоит ради места над покупками,
  а в цепочке она делается по-настоящему отдельным шагом, и считать её
  дважды нельзя. Две границы: альтернатива — это ход БЕЗ подъёма ЦЕЛИКОМ
  (иначе после вынужденного первого шага подъём возвращается вторым,
  part24 ход 7), и **мусорная витрина подъём в развилку не отдаёт**
  (`standaloneScore` там не заполняется: довод ветки JeefHS — «покупать
  нечего», и цепочка из тех же покупок ему не возражение). Следствие
  названо игроку заранее и принято: на шести золотых цепочка почти всегда
  перевешивает один тир, и канонический подъём part20 (ход 7) исчезает.
- **Тир, НАЗВАННЫЙ в тексте «даёт миньона», читается — витрина ему
  не мерка** (part23, ход 11). «Get a random Tier 1 minion» (Recruit
  a Trainee за 2) на третьем тире оценивался средним по витрине третьего
  тира — 11.5 очков за тело первого. Тир пишется двумя способами, оба
  из текста: числом (`Tier N`) и словами «своего тира» (`of your Tier` —
  Search Through Time, Boundless Potential); таких заклинаний в пуле 21.
  Ожидание считается ПУЛОМ того тира (`cards.poolOfTier`), той же шкалой
  и на том же борде: свои по племени, ключевые слова и копии у пришедшего
  миньона будут настоящие, и усреднение по пулу — их честное ожидание,
  а не поправочный коэффициент. Тир, не названный вовсе («of your most
  common type»), по-прежнему меряется витриной — она и есть выборка
  своего тира. Жалоба игрока дословно: «это работает для ранней игры».
- **Наценка считается тем же курсом, что скидка.** Прежнее
  `Math.max(0, minionCost − cost)` делало разницу односторонней:
  заклинание дешевле покупки получало прибавку, а ДОРОЖЕ покупки
  не платило ничего. Это не осторожность, а ошибка знака — золото давно
  переведено в очки, и курс обязан работать в обе стороны (part23,
  ход 15: Planar Telescope за 4 стоил 11.3 очка вместо 8.3).
- **Заморозка ради заклинания судится ЦЕНОЙ, и спор об этой ветке
  закрыт 17.08 (part23) — правило part19 ОТМЕНЕНО.** Два условия, оба
  структурные. Первое: витрину держат ради заклинания, только если оно
  ДЕШЕВЛЕ покупки — в этом весь смысл ветки (part17, ход 1: тело за два
  золота там, где покупка стоит три). Заклинание дороже покупки такой
  ставкой не является: то же тело в тот же ход просто покупается,
  и дешевле (Planar Telescope за 4). Второе: планка = «свежая карта
  своего тира» ПЛЮС цена самой заморозки, и оба слагаемых — ценности
  карты, посчитанные одной функцией на одном борде (пул своего тира
  через `cards.poolOfTier` против среднего по доживающей витрине).
  Прежняя планка была маржинальной (`threshold − bestKept`) и сравнивала
  ЧИСЛА С РАЗНОЙ НАЧИНКОЙ: голое `2×тир+4` против полной ценности карты
  на борде. На собранном борде разность падала в ноль (part23, ход 15:
  13.0 против 14 — порог ЕДИНИЦА), и витрину держало любое заклинание,
  дающее миньона. Новая планка растёт с тиром сама: лассо берёт её
  на первом тире (5.3/4.3 → 6.4 при лассо 7.0) и не берёт со второго
  (7.6/5.0 → 10.3 при 8.0). Это дословно то, что игрок сформулировал
  в part23: «работает для ранней игры». Тем самым разрешено расхождение
  part19 («совет исчез после подъёма» — просил вернуть) против part22
  («практического эффекта не вижу» — просил убрать): обе жалобы были
  про одну карту на одном тире, и правильный ответ оказался не про
  подъём, а про цену. Тесты part19 и part22 переписаны, а не удалены —
  они держат новое поведение и хранят историю спора.
- **Тёмный дар — это РАСКОПКА ЧУЖОГО ТЕЛА с даром, а не усиление
  своего.** Читается из лога (part23, три нажатия): блок PLAY на кнопке
  `BG36_Button_DarkGift`, следом `DebugPrintEntityChoices`
  с `ChoiceType=GENERAL`, источником «Battlegrounds Dark Gift [DNT]»
  (`BG36_MidGameEffect_010`) и ТРЕМЯ МИНЬОНАМИ в `Entities[0..2]`.
- **Тир предложения дара растёт по ходам ТАВЕРНЫ, и таблица у нас есть**
  (прислана игроком, part24): 3-й ход таверны — тир 2, дальше 2–3, 3,
  3–4, 4, 4–5, 4–5–6, 5–6; после 10-го не растёт. Ключ — ход таверны,
  тот же урок, что с кривой подъёма (part20). Лог подтверждает нижнюю
  границу сам: `error=REQ_MINIMUM_GAME_TURN errorParam=5`. Ценность дара
  — ожидание миньона названных тиров (`averagePoolValue`), той же
  функцией и на том же борде, что всё остальное; надбавки за сам дар
  и за выбор из трёх нет (`bonus: 0`), то есть оценка НИЖНЯЯ. Прежний
  ПЛОСКИЙ вес вёл себя наоборот правде: обгонял покупку на втором тире
  и проигрывал ей на пятом, подталкивая жать РАНО. Замер частоты после
  правки (21 партия, 253 точки решения): дар по карману на 217, верхним
  советом на 35 (16%), первым шагом плана на 31 (12%).
- **«Свежая карта витрины» — это тиры ОТ ПЕРВОГО ДО СВОЕГО, а не один
  свой.** Витрина второго тира на part24 (ход 3) состояла целиком
  из карт ПЕРВОГО тира. Считать ожидание по одному своему тиру значило
  завышать планку заморозки тем сильнее, чем выше таверна. Взвешивания
  по числу копий в пуле у нас нет — снапшот его не несёт, карты
  усредняются поровну; настоящий пул смещён к низким тирам, то есть
  оценка скорее завышена, чем занижена.
- **Золото, которое действие ПРИНОСИТ, обязано доезжать до следующего
  шага плана** (`grantsGold`, part24 ход 9). У золотого заклинания эффект
  известен числом прямо из текста («Gain 1 Gold»), и прятать его
  за пометкой «непрозрачный шаг» нечестно: совет обещал «откроется
  покупка Ominous Seer», а следующим шагом плана покупка не открывалась
  — золото не доезжало, и план брал заклинание за 2 вместо миньона за 3.
- **Поглощение витрины считается ЧИСЛОМ, а не откладывается** (part24,
  ход 15). «Activate ({0}): Your Demons each consume a random minion
  in the Tavern to gain its stats» — все три множителя читаемы: племя
  едоков названо в тексте (таблица `tribeTextWords`), их число на борде,
  средние статы съедаемого в витрине; золотая версия удваивает («gain
  double its stats»). Едоков считаем не больше числа карт витрины —
  съесть можно только то, что есть. Семь демонов при витрине из четырёх
  по 24 стата дали **+97 статов за 2 золота (42.5 очка)** там, где совет
  предлагал обновить витрину за 1. Пропажу съеденных карт не считаем:
  при полном борде и двух золотых покупать было нечего.
- **Заклинание бесплатных обновлений — экономика с ЖИВОЙ ценой кнопки.**
  «Gain 2 free Refreshes» (Leaf Through the Pages за 1): ни статов,
  ни золота, ни миньона в тексте, и разбор возвращал `null` — заклинание
  было невидимо целиком (part23, ход 5: два золота сгорели при нём
  в витрине). Ценность — `count × rerollCost − cost` по курсу
  `goldPointValue`; при уже бесплатных обновлениях дарить нечего,
  и совет молчит. Текст пула несёт разметку посреди предложения
  («Gain 2 free **Refreshes**»), поэтому шаблон пишет `[^.]*`, а не
  пробел, — тот же урок, что с переносами строк в part16.
- **Сила героя, предложенная В ВЫБОРЕ, судится ДВУМЯ шкалами, и смешивать
  их нельзя** (part26, Мастер Нгуен `BG20_HERO_202` меняет силу каждый
  ход). Первая — наши очки из ТЕКСТА силы на нашем состоянии (даёт
  миньона, обновляет витрину, даёт заклинание таверны, меняет своего
  миньона). Вторая — среднее место ГЕРОЯ, чья это сила, по статистике
  Firestone: связь лежит в идентификаторе (`BG25_HERO_103p`,
  `BG23_HERO_303p2`, `BG22_HERO_000p_Alt`) и проверяется по справочнику,
  а не регуляркой на веру; из 169 сил пула героя находят 69, из них 63
  со статистикой. Ранжируется та шкала, которая есть у ВСЕХ вариантов;
  общей нет — порядок не выдумывается, но известное стоит выше
  неизвестного. Число статистики всегда подписано «по статистике»: оно
  про целую партию того героя, а Нгуену сила достаётся на один ход.
- **ЗАМЕНА читается раньше «даёт миньона», а триггер в тексте выключает
  разбор разового усиления** (part26). «Destroy a friendly Undead to get
  a random Undead» отдаёт миньона ВЗАМЕН своего — прочитанная как «даёт
  миньона», она стоит 7.6 очка вместо 3.0. «Your Tavern spells give
  an extra +{1}/+{1}. At the start of every 3 turns, improve this» — это
  не «+2 статов»: вечная прибавка ко всем будущим заклинаниям оценивалась
  в одно очко. Оба случая дают не падение, а тихо неверное число.
- **Сущностей ГЕРОЯ у игрока несколько, и лишние врут** (part26). Живые
  теги (`PLAYER_TECH_LEVEL`, `PLAYER_LEADERBOARD_PLACE`, `DAMAGE`,
  `ARMOR`) приходят на сущность, созданную в начале партии; копии для
  модальных выборов (варианты «Дружеской ставки», кандидаты муллигана)
  создаются позже и остаются с тегами момента создания — с тиром 0
  и чужим местом. Отработавшие копии отсеиваются зоной `REMOVEDFROMGAME`,
  из оставшихся берётся сущность с НАИМЕНЬШИМ id. Без этого тир половины
  лобби читается нулём, а свой герой — кандидатом муллигана.
- **Ставка на чужой бой судится ФАКТАМИ, а не симулятором** (part26,
  «Дружеская ставка» `TB_BaconShop_HP_081`: «Guess which player will win
  their next combat»). Совет называет тир таверны, здоровье с бронёй,
  место в таблице и давность нашей картинки его борда; порядок — по тиру,
  при равенстве по здоровью (тир говорит о силе доступных миньонов, то
  есть о самом бое, здоровье — о прошлых боях). Симулятор не зовётся
  намеренно: чужой борд виден только в бою с ним, к моменту ставки
  картинке 5–17 ходов, и сверять по ней уже записано как ошибка. Один
  случай part26 порядок «по тиру» подтвердил, а «по месту» опроверг —
  игрок поставил на лидера таблицы и проиграл.
- **Замороженная витрина ДОЗАПОЛНЯЕТСЯ до размера витрины своего тира**
  (part27, проверено по 19 заморозкам шести партий — таблица
  в docs/power-log.md). Замороженные миньоны возвращаются НОВЫМИ
  сущностями с теми же cardId, а пустые слоты — купленные в этот ход
  и добавленные подъёмом таверны (3 → 4 на втором тире) — приходят
  свежими. Держать витрину ради одной карты не значит отказаться
  от свежей витрины целиком.
- **Цена заморозки ради «предложения дешевле покупки» — недобор ЛУЧШИХ
  доживающих до свежей карты, по покупкам следующего хода и только СВЕРХ
  свежих слотов дозаполнения** (part27, ход 1). Прежняя планка брала
  СРЕДНЮЮ по остатку: дешёвка рядом с лучшей картой роняла её, хотя игрок
  дешёвку не купит, — и один ход давал разные ответы в зависимости от того,
  какой из двух РАВНЫХ миньонов купили (Bonehead 4.5 при нежити на борде
  против 3.0 при элементале: планка 5.70 против 6.45 при лассо 7.5 и 6.0).
  Формула: `fresh + Σ_{i < покупок − refills} max(0, fresh − kept_i)`,
  `refills = shopSizeByTier[тир] − доживающих`, покупок —
  `floor((золото следующего хода − цена предложения) / 3)`; дешевле
  покупки — СТРОГО, сравнение с планкой — строгое. Следствие для спора
  о заморозке ради заклинания (part17 → part19 → part22 → part23 → part24):
  на первом и втором тирах свежий слот есть почти всегда и планка равна
  свежей карте (part24 ход 3: 6.81 против 8.48 — совет есть), на третьем
  свежая карта дороже тела первого тира (part23 ход 11: 10.70 против 9.02
  — молчит). «Работает для ранней игры» стало следствием
  размера витрины, а не порогом по тиру. Оговорка: свежая сторона —
  среднее, а не лучшее-из-N, планка мягкая; если заморозок в плане станет
  много (замер: 25 → 37 из 292 точек), крутить надо свежую сторону.
  Пул лассо — одни доживающие, без свежих слотов: несогласованность
  в строгую сторону, принята сознательно (part22, ход 7).
- **Тавернный триггер — не движок** (part27, ход 7). Правило движка
  (part15) бережёт эффект, срабатывающий В БОЮ и требующий живого носителя
  (Deathstrider: «After a friendly Rally minion attacks…»). «After you play
  an Elemental, gain +{1} Health» (Molten Rock) срабатывает в таверне,
  в бою это тело, а смерть в бою ничего не стоит — провокацию на нём
  терять нечего. Прежнее чтение уводило Slimy Shield с крупнейшего тела
  4/4 на отработавший генератор 3/3, которого игрок собирался продать.
  Проверка по ПОЗИЦИИ: голова триггера из `engineTextWords`, на той же
  позиции sticky-шаблон таверны из `tavernTriggerWords` («after you
  play/buy/sell/refresh/upgrade/spend/discover/magnetize/pass…»,
  «at the start/end of your turn», «whenever it is refreshed», «whenever
  a minion is consumed»). «Whenever you cast a Tavern spell» и «whenever
  a card is added to your hand» тавернными НЕ считаются: Rally кастует
  заклинания и добывает карты посреди боя, симулятор рассылает
  `onTavernSpellCast`/`onCardAddedToHand` всему борду (Hooktail, Peggy).
  По пулу: 129 миньонов с головой триггера, 83 — только тавернные, 46
  остаются движками. Аура — движок без разбора текста.
- **Обновление, после которого не на что купить, советуется только под
  НАЗВАННУЮ цель заморозки** (part27, ход 19: золото 0, борд полон,
  обновление бесплатно, совет «покупать нечего» обещал покупку, которой
  быть не могло). `paidRerollIsUseful` пропускал бесплатное обновление
  и лейт с доводом «поиск карты под заморозку», не спрашивая, какой.
  Цели — из веток самой заморозки: третья копия к паре (незолотые копии
  на борде и в руке карты, которую витрина может дать — пул, миньон, тир
  не выше таверны; тройка места не просит, годится на полном борде)
  и соплеменник собираемого племени на НЕПОЛНОМ борде, не в ход подъёма
  и только если племя есть в пуле своего тира. «Не на что купить» — по
  цене заклинания витрины (1), а не миньона. Вторая копия целью
  не считается нарочно. Нет цели — обновление молчит, и «обновление
  от безделья» тоже. Совет с целью остаётся: крутить бесплатные обновления
  ради тройки при нуле золота — настоящая игра.
- **Дамп переподключения — блоки `GameEntity/Player EntityID=…` со
  строками-продолжениями `tag=`.** `HERO_ENTITY` там приходит НЕ как
  TAG_CHANGE, а тегом блока Player; разбор без этой ветки оставлял партию
  после реконнекта без героя и с тиром 1 (part1, сегменты 2–4).
  `PLAYER_LEADERBOARD_PLACE` живёт всю партию как текущее место —
  финальным становится только на FINAL_GAMEOVER.
- **Модальный «Choose One» У МИНЬОНА каналом выборов НЕ приходит, и ловить
  открытый экран нечем** (part28). Ветви — те же карты `…t`/`…t2`, что
  у заклинаний (part19), и приходят они сущностями в `SETASIDE` с тегом
  `PARENT_CARD`, но создаются ЕЩЁ ПРИ ПОЯВЛЕНИИ карты в витрине
  (23:19:38), а розыгрыш случился в 23:20:31 — то есть ни зона, ни наличие
  ветвей признаком «выбор открыт» не являются, а `DebugPrintEntityChoices`
  для такого выбора не пишется вовсе и `openChoice` остаётся `null`.
  Следствие: ветвь называется ЗАРАНЕЕ, в совете «разыграть», полем
  `spellBranches` (оверлей печатает его с part19). Что выбрал игрок,
  читается полем `SubOption` блока PLAY: 0 — первая ветвь, 1 — вторая,
  −1 — выбора не было. Тест держит утверждение про `openChoice === null`:
  если игра начнёт слать такой выбор каналом, он упадёт, и правило будет
  пересмотрено, а не унаследовано молча.
- **Ветви модального МИНЬОНА судятся на СОСТОЯНИИ, а не скаляром весов**
  (`modalBranchAdvice`, part28). У ветвей заклинаний скаляр (`branchScore`)
  правомерен: их считает `spellEffect`, у которого состояния нет и кэш
  ключуется одной картой. У миньона весь вопрос именно в борде и ходе —
  «случайный квилбоар» на борде квилбоаров стоит 20.8, а по всему пулу
  тиров 1–4 — 11.6. Пул при этом фильтруется по НАЗВАННОМУ племени
  (`averagePoolValue` получил параметр расы, и племя вошло в ключ кэша),
  а на полном борде из ценности вычитается жертва: карта приходит в руку,
  и место ей освобождает продажа. Борд берётся тот, что будет К МОМЕНТУ
  ВЫБОРА, — сам миньон уже стоит (у «случайного соплеменника» он же
  и соплеменник), а жертва, освободившая место под него, уже продана;
  иначе одна и та же продажа обещалась бы дважды. Ветвь, которую оценить
  не берёмся, обнуляет выбор целиком: совет называет ОБЕ, потому что
  неоценённая ветвь не значит худшая.
- **«Increase your maximum Gold by N» — РЕНТА, а не монета** (part28).
  Предел поднимается навсегда, и потолка «десять» у него нет: в part27
  тег `RESOURCES` доходит до 19. Поэтому ценность — `N × оставшиеся ходы
  таверны × goldPointValue`, а не `N × goldPointValue`. Складывать
  с разовым `gold` в одно поле нельзя (`maxGold` отдельным полем
  `SpellEffect`): это приравняло бы «+1 золото сейчас» к «+1 золото
  до конца партии». Карт с такими словами в пуле шесть, и две из них —
  заклинания витрины (Strike Oil `BG28_805` тир 2, Timewarped Strike Oil
  `BG34_Treasure_932` тир 5), которые до этой правки были невидимы
  ЦЕЛИКОМ, как «Gain 2 free Refreshes» до part23.
- **Сколько ходов таверны осталось — ЗАМЕР, а не догадка**
  (`rules.remainingTavernTurns`, `npm run spike:horizon`). 25 партий
  датасета, 299 точек: длина партии 9–15 ходов таверны при медиане 12,
  на 7-м ходу впереди 5.3 хода, на 11-м — 2.2. Числа замерены У НАС
  и потому живут в НАШЕЙ нумерации ходов таверны, как `trinketOfferTurns`,
  а не переводятся из чужих таблиц (урок part20). Здоровье в таблицу
  не входит НАМЕРЕННО: связь есть (при hp 21–30 остаток 2.73 против 7.92
  при hp 31+), но с ходом она смешана — низкое hp бывает в основном
  поздно, — и на двухфакторную таблицу 25 партий не хватает. Цена
  допущения названа вслух и видна на самой part28: таблица обещала
  5.3 хода, игрок прожил 2. Поэтому совет обязан печатать это число
  словами («по золотому ещё 5.3 ходов таверны») — иначе игрок не может
  возразить.

- **Сила героя с ЦЕЛЬЮ читается полем `Target=` блока PLAY, а цель судится
  БОЕМ** (part29, «Lock and Load» Тавиша `BG22_HERO_000p_Alt`: «Remove
  a minion in the Tavern. When you have space next combat, fire it
  at a random enemy minion»). Сила активна и бесплатна (тега `COST` нет
  вовсе), нажимается каждый ход — 13 раз за партию, — и советник молчал
  про неё всю партию: бесплатные силы советовались только с текстом
  про обновление витрины (part13). Лог отдаёт всё: цель — поле `Target=`
  (миньон витрины, `player=11`), копия цели создаётся в `SETASIDE` под
  нашим контроллером (`TAG_SCRIPT_DATA_ENT_1` силы), заряд
  `TAG_SCRIPT_DATA_NUM_1` идёт 0 → 1 и обратно в ноль в начале боя,
  а сам миньон уходит в `REMOVEDFROMGAME`. Три следствия, и все три
  арифметика: ЦЕЛЬ судится `combatValue` (статы + ключевые слова + боевой
  эффект текста) — выстреленный миньон живёт ОДИН размен, и тир, племя,
  копии, экономика к нему не относятся; СТРЕЛЯЕМ тем, чего не купим —
  карта уходит из витрины насовсем, поэтому отбрасываются `affordable`
  лучших по обычной ценности, где `affordable` ограничено и золотом,
  и свободными слотами борда; БЕЗ СВОБОДНОГО СЛОТА совет молчит — это
  сказано в самом тексте силы. В пуле такая сила ОДНА: слова «remove
  a minion» среди 2147 карт типа `HERO_POWER` есть только у неё. Заморозку
  выстрел не бережёт (в part29 игрок сам стрелял по замороженной витрине),
  и запрета на это нет намеренно.
- **Заморозка ради «предложения дешевле покупки» обязана проверять
  СОБСТВЕННОЕ обещание — «два тела вместо одного»** (part29, ходы 5 и 7).
  Витрину держат ради заклинания, дающего миньона (part17), или продажного
  генератора (part25) ровно потому, что предложение стоит меньше трёх
  и занимает золото, которое иначе сгорит. Но сгорает оно не всегда: тел
  без предложения `⌊золото / 3⌋`, с предложением `1 + ⌊(золото − цена)
  / 3⌋`, и золото берётся ТОГО хода, который называет сам совет
  (`turnAffordingBoth` — на первом ходу таверны это третий). Пять золота —
  лишнее тело есть, шесть и семь — нет, восемь — снова есть. Дословно так
  и сказал игрок: «5 золота скорее всего последнее выгодное значение для
  его заморозки, дальше я уже смогу покупать два существа за 6 золота
  и существо + улучшение таверны за 7». Проверка общая для обеих веток —
  вопрос у них один. Планку `spellThresholdOf` (part27) и пороги по тиру
  правка не трогает: это дополнительное необходимое условие, а не новая
  шкала. Тем же счётом снялась заморозка ради River Skipper на part22
  (ход 7) — та, про которую игрок сказал «не вижу практического эффекта»
  и которая была записана первым кандидатом на снятие.
- **Цена бывает НЕ в золоте, и читается это тегом
  `BACON_COSTS_HEALTH_TO_BUY`, а не текстом** (part29, ход 9). Hasty
  Excavation `BG28_571` («Gain 1 Gold. This costs Health to buy instead
  of Gold», `COST=3`) сравнивался с золотом и был невидим дважды:
  «не по карману» при нуле и «даёт 1, а стоит 3» при деньгах. Тег ставит
  игра, и не только на карты со своей ценой в здоровье: Malchezaar
  `BG26_524` и Bazaar Dealer `BG28_905` на борде, наклейки
  `BG32_MagicItem_821`/`822`, тринкеты `BG30_MagicItem_701`
  и `BG35_MagicItem_152` делают платой за здоровье ЧУЖУЮ покупку — тот же
  урок, что с живыми ценами подъёма и обновления. **Курса «здоровье →
  золото» у нас нет и выдумывать его мы не станем**, поэтому ветка живёт
  ровно там, где курс не нужен: при миньоне «After your hero takes damage,
  rewind it» на борде (`healthRewindWords`, четыре карты пула — Soul
  Rewinder тира 2, Timewarped Rewinder тира 3, Ashen Corruptor
  и Timewarped Archimonde тира 5). Что триггер срабатывает именно на трату
  здоровья, показывает лог: `META_DATA Meta=SPEND_HEALTH Data=3`, броня
  героя 14 → 11 и тут же обратно 14. Без такого миньона совет молчит.
  На МИНЬОНАХ тот же тег тоже бывает, и там цена по-прежнему считается
  золотом — фикстуры нет, вслепую не вносим.
- **Копии под тройку игра считает и В РУКЕ, значит розыгрыш ставки её
  не улучшает** (part29, ход 19). Тег `BACON_PAIR_CANDIDATE` игра ставит
  на карту витрины и тогда, когда единственная наша копия лежит в руке:
  проверено прогоном по двум фикстурам — семь таких случаев (part29 ход 23
  `BG31_330`, part22 шесть штук), и ни одного «пара без копии». Отсюда две
  правки. Первая: бонус за копию — ценность ВЛАДЕНИЯ, и в очках розыгрыша
  он стоял вторым разом, да ещё и ПРИЧИНОЙ («вторая копия» в обосновании
  того, чтобы карту выставить). Вторая: ставка, занимающая ПОСЛЕДНИЙ
  свободный слот, разменивается на один бой — следующая покупка встретит
  полный борд продажей, и продана будет именно ставка, потому что копию
  под тройку берут не телом. Поэтому розыгрыш такой карты советуется,
  только когда её тело в бою (`combatValue`) стоит дороже ставки
  (`copiesBonus`); при равенстве ставка остаётся в руке — розыгрыш
  необратим, а рука не стоит ничего. Тройки это не касается: её собирают
  немедленно.
- **Сила «Discover a \<Tribe\>» — «даёт миньона», и Discover — это
  ВЫБОР** (part30, ход 1). Племя без слова «minion» читается той же
  `tribeMinionRace`, что выбор сил (part26) и ветви модальных миньонов
  (part28), — в общий `givesMinionWords` оно по-прежнему не вносится
  (та таблица кормит правила покупки и заморозки). Ожидание — по пулу
  племени тиров 1..своего, и у Discover оно НЕ среднее: три карты
  предложены, берётся одна, и считается ожидание лучшего из трёх
  порядковой статистикой (C(i−1,2)/C(n,3)) на той же шкале. Это
  арифметика, а не надбавка: средний мех тира 1 стоит 6.25 и проигрывает
  покупке 7.0, а выбор из Lullabot 5.0 и Cord Puller 7.5 — это 7.5,
  и игрок взял ровно Cord Puller. «Get a random \<Tribe\>» остаётся
  средним — там выбора нет; тёмный дар остаётся с `bonus: 0` (part24) —
  его пул искажён самим даром, и нижняя оценка там сознательная.
- **Статы «minions in the Tavern» ложатся на ВИТРИНУ, а не на наш борд**
  (part30, ход 7). Лог: блок PLAY с `Target=0`, энчанты — на сущностях
  `player=10`. Прежний разбор называл целью крупнейшего своего
  («→ на Gem Rat 4/4» на скриншоте) и приписывал борду статы, которых
  тот не получит. Признак — `buffsShop` (`buffsShopWords`); цель
  не называется, ценность — статы на число покупок по остатку золота
  (не больше размера витрины), при нуле золота честное молчание. Оценка
  нижняя: заморозка могла бы сохранить усиленных, покупки считаются
  по базовой тройке.
- **«Gain N Gold next turn» — золото ОТЛОЖЕННОЕ, и с живым оно
  не складывается** (part30, ход 9). Тот же урок, что предел золота
  в part28: разные величины — разные поля. `goldNextTurn` не попадает
  в `grantsGold`, и план не строит на него шагов этого хода — прежде
  «КУПИТЬ Careful Investment за 1 → ТЁМНЫЙ ДАР за 3» предлагалось при
  двух золотых. Покупка при этом советуется (чистыми +1) со словами
  «СЛЕДУЮЩИМ ходом»; для сравнения ветвей между собой (`branchScore`)
  отложенное золото идёт тем же курсом — там вопрос «какая ветвь»,
  а не «на что хватит».
- **Сгорающее золото уходит в обновление-СТОК — только под триггер трат
  на борде** (part30, ход 19). Условия переборные, не пороговые: золоту
  нет другого применения (`goldHasOtherUse`: покупка по живой цене,
  заклинание витрины, подъём, дар, платная сила), обновление платное
  и по карману, на борде миньон с триггером трат или обновления
  (`goldSinkTriggerWords`: «Whenever/After you spend … Gold», «After you
  Refresh»), витрина не заморожена и заморозка молчит. Очки малые
  (`goldSinkRerollValue`) — напоминание, а не приоритет. Границы part27
  целы: при нуле золота и бесплатном обновлении ветка не срабатывает
  (нечему сгорать), без триггера молчание остаётся — цель обязана быть
  названа.
- **Кровавые самоцветы молчат КОДОМ, а не удачей нулевых плейсхолдеров**
  (part30). Граница part28 («Blood Gems остались без цены») держалась
  на том, что плейсхолдеры без тегов давали нули; у Gem Day `BG31_893`
  ветвей-карт в снапшоте нет, текст родителя складывал обе ветви
  в «+2 статов» (класс ошибки part19) и советовал вечную прибавку
  будущим самоцветам как разовый бафф с целью. Теперь `bloodGemWords`
  возвращает разбору `null` — все 23 заклинания семьи. Проверка стоит
  ПОСЛЕ разбора ветвей CHOOSE_ONE: модальному миньону с одной самоцветной
  ветвью вторая остаётся видимой.
- **У заряда тёмного дара есть ЦЕНА — тело, которое он принёс бы позже**
  (part31). Зарядов три на партию (кнопка создаётся с `NUM_2=3`, живое
  число — `darkGiftCharges`), предложение растёт по ходам таверны
  до десятого (таблица part24) и дальше плоское. Пока дар мерился одним
  телом «сейчас», он стоял верхней строкой с первого хода, где по карману
  (part31: с хода 7; на скриншоте 14.4 против покупки 14.0), а игрок все
  три заряда нажал на 10–12-м ходах таверны. Правило без нового веса:
  ходов впереди — `remainingTurns` (замер part28), и если их с нынешним
  больше, чем зарядов, нажатие сейчас вытесняет самый ранний из последних
  ходов — `сейчас + (впереди + 1 − зарядов)`; цена спешки — разница тел
  того хода и этого (`averagePoolValue` по тирам таблицы, дробный ход —
  линейно). С десятого хода цена ноль, и совет говорит «сильнее
  предложение уже не станет — жать». Оговорки записаны в правиле:
  темповая цена тела (сейчас воюет больше боёв) не считается, горизонт
  без здоровья (part28). Причина обязана печатать и горизонт, и цену.
- **Миньон, приходящий В РУКУ, на полном борде вычитает жертву — везде,
  а не только у ветви part28** (part31, ход 13). «Discover a Tier 1
  minion» за 3 (A New Sprout, 7.1) стоял первым шагом плана при семи
  своих, где слабейший стоил 9.0. Вычет один (`handMinionVictim`):
  заклинание витрины «даёт миньона», сила героя «даёт миньона», ветвь
  без племени у модального миньона; планка — `sellMargin`, как у покупок
  на полном борде, иначе совет молчит. На неполном борде вычета нет.

### Отложено сознательно, с причинами

- **Статистика мест МИНЬОНОВ в ценность покупки — ЗАМЕРЕНО 15.08, не вносим**
  ([docs/cardstats.md](docs/cardstats.md)). Предрегистрация закоммичена
  до прогона; парный замер на 108 точках решения 13 партий билда 248348,
  4000 симуляций с зерном на кандидата, 405 кандидатов. Сигнала сверх наших
  одиннадцати слагаемых не найдено: **V2 = −0.078** при пороге +0.15
  (p = 0.87 по внутритировым перестановкам), при том что ветка V2 имела
  мощность 62–87% против истинной связи 0.15–0.20. Кросс-валидация
  независимо выбрала λ=0 в 12 фолдах из 13. Читать как «польза
  не показана», а НЕ «статистика вредит»: отрицательная Δcost = −1.16 п.п.
  лежит внутри собственной полосы нуля (5.3-й перцентиль), а вся её
  величина создана боями первого хода — без хода 1 она +0.27.
  Снапшот в git не кладётся, рантайм не тронут; дамп сохранён активом
  в `data/dataset/cardstats-dump.jsonl` — по нему любая будущая гипотеза
  «добавляет ли признак X сигнал» проверяется за секунды без симуляций.
- **Прогноз места в оверлей — ЗАМЕРЕНО 18.08, не вносим**
  ([docs/ml.md](docs/ml.md)). Первый замер фазы 6: гребневая регрессия
  на семи «своих» признаках против таблицы лидеров, LOGO по 23 партиям,
  предрегистрация до прогона. Вердикт НЕ ДОКАЗАНО: модель (1.874) хуже
  константы B0 (1.834) и сжатого места B2 (1.823), выигрыш у таблицы
  живёт только в ранних ходах и создан сжатием, а не признаками борда.
  Причина структурная: место — относительная величина, а признаки
  датасета — абсолютные (`lobby` вошёл в состояние после всех записанных
  партий). Перезамер при +10 новых партиях, первые кандидаты в признаки —
  относительные (hp против стола, тир против стола, место среди живых).
  Прибор готов: `npm run ml:eval`, детерминирован, 24 теста.
- **Цена ЗАРЯДА тёмного дара — ЗАКРЫТО 27.08 (part31).** Прежняя запись
  гласила, что зарядов три, нажатый рано отнимает лучший поздний,
  и этой альтернативной стоимости у нас нет. Теперь она есть и считается
  без нового веса — из горизонта партии (замер part28) и живого числа
  зарядов (см. «не переоткрывать»): советник умеет сказать «заряд лучше
  придержать» и называет цену спешки числом. Не считается по-прежнему
  ТЕМПОВАЯ цена (тело сейчас воюет больше боёв) — её нет ни у чего
  на нашей шкале.
- **`npm test` иногда возвращает ненулевой код при ВСЕХ зелёных тестах.**
  Ошибка «[vitest-worker]: Timeout calling "onTaskUpdate"»: воркер занят
  длинной синхронной работой (разбор 40-мегабайтного лога в `beforeAll`)
  и не отвечает репортёру. Появилось 17.08, когда советник подорожал.
  Живая сводка репортёра выключена (`summary: false`) — это снижает
  вероятность, но не лечит. Настоящее лечение — не держать поток
  двадцать секунд подряд, то есть читать фикстуры кусками; работа
  отдельная. Проверять надо строку «Tests N passed», а не код возврата.
- **Поглощение витрины КЛИЧАМИ, СПЕЛЛАМИ и триггерами не разобрано.**
  Активация закрыта (part24), но тот же эффект носят боевой клич миньона
  (Mind Muck `BG23_357`, тир 2: «Battlecry: Choose a friendly Demon.
  It consumes a minion in the Tavern to gain its stats» — это МИНЬОН,
  а не заклинание), заклинание витрины (Corrupted Cupcakes `BG28_607`,
  тир 5, съедает трёх) и триггеры на борде («After you play a Demon,
  consume…»).
  Их разбор — та же арифметика, но другая поверхность; вносить вместе
  с пунктами игрока значило бы смешать проверенное с непроверенным. Вопрос вынесен игроку.
- **Статистика мест для тёмных даров и композиций.** У Firestone есть
  comp-stats (средние места архетипов) и quest-stats (в текущем патче
  пуст) — не подключены. Герои и тринкеты подключены: там статистика
  отвечает на прямое «оценить не берёмся».
- **Активации со сложными эффектами.** Простые активации советуются
  (см. «не переоткрывать»): бафф-статы и «получить миньона». Кражи,
  поглощения, консервация статов («Living Prison») — эффекты без прямой
  цены в статах, совет их честно не оценивает.
- **Силы героя вне «даёт миньона», «бесплатно обновляет витрину» и «даёт
  заклинание таверны»** — урон, баффы, прочая экономика: совет не берётся
  судить, как и с тринкетами. Советуются сила, дающая миньона, бесплатная
  активная сила-обновление и платная сила-заклинание дешевле своей ценности
  (см. «не переоткрывать»).
- **Класс «сила платит за ДЕЙСТВИЕ» — 124 силы пула из 174, вносятся
  только с фикстурой** (замер 26.08, вопрос игрока «учитывал ли ты силу
  героя последней партии»). Из 174 сил пула советник не берёт ничего
  у 124; по факту — из 24 партий датасета сила не дала ничему в 14, и шесть
  из этих четырнадцати покрыты в обход текста (четыре героя тёмного дара
  идут кнопкой `darkGiftRule`, ещё две — выбором тринкета и стартовым
  телом). Остаются восемь партий, где сила определяла стиль, а советник
  о ней не знал. Из невидимых 27 платят за повторяющееся действие —
  «After you upgrade the Tavern, gain 2 Gold» (Everbloom), «At the start
  of your turn, gain a free Refresh» (Clairvoyance), «After you buy
  3 minions, get a Tavern Coin» (Verdant Spheres). Разобрать их разом
  нельзя: у нас нет ни одной партии за этих героев, а правило по тексту
  карты, которой не было ни в одном логе, — догадка под видом факта.
  Две силы, у которых фактура ЕСТЬ, внесены (порог тройки и статы
  за розыгрыш, см. «не переоткрывать»), и каждая закрыла ровно одну
  карту пула: 126 → 124. Числа покрытия закреплены тестом
  (`test/advisors/tavern/heroPowers.test.ts`), так что рост виден
  без пересчёта руками.

  Первый кандидат, когда фактура появится, — **`Stay Frosty`
  («Minions cost (2)»)**: цена миньона у нас константа `minionCost = 3`
  со скидкой тегом `BACON_REDUCE_BUY_COST` на самом миньоне, и если игра
  скидку туда не кладёт, на этом герое неверна цена КАЖДОГО совета.
  Проверяется одной партией за него. Цена обновления и цена подъёма
  таким героям не подвержены — они читаются живыми тегами кнопок.
- **Экономические эффекты тринкетов — ЗАКРЫТО 16.08 живыми ценами.**
  Прежняя запись гласила, что тринкеты, меняющие цену покупки и обновления,
  советник не учитывает. Оказалось, что моделировать их не нужно: игра сама
  считает итоговые цены и кладёт их в теги. Покупка (`BACON_REDUCE_BUY_COST`),
  подъём (`COST` кнопки), заклинания (`COST` сущности) читались и раньше;
  недостающей была цена обновления — она была константой из таблицы. Теперь
  и она живая, и советник видит бесплатные обновления, откуда бы те ни шли
  (см. «не переоткрывать»). Выбор тринкета отдельного правила по экономике
  не получил и не получит: 241 тринкет пула из 247 имеет статистику мест
  Firestone (остальные шесть — из режима Duos), а статистика и есть свёртка
  силы эффекта. Своей шкалы вместо неё не выдумываем — то же решение, что
  с героями.
- **`validTribes`.** Теги `BACON_SUBSET_*` висят на миньонах, а не на
  `GameEntity`, то есть означают принадлежность карты к племени, а не
  состав партии. На ТРИНКЕТАХ тот же тег называет племя эффекта — это
  используется в советах (part12), но состава партии по-прежнему не даёт.
- **`NaN` у статов спавнов.** Виден в логе симулятора вместе с
  «short-circuiting simulation». На него приходится единственный выброс
  калибровки. Нулями не лечится (см. выше).
- **Восемь воркеров вместо одного.** Один поднят в фазе 5 и снял главную боль:
  счёт не держит интерфейс. Восьмикратный запас потребует по снапшоту карт
  на воркер и своей схемы разбиения поиска — работа отдельная и пока
  не нужная.
- **Расстановка противника не перебирается.** Он тоже её выбирает; мы играем
  против того порядка, который видели в последнем бою с ним. Возраст этой
  картинки лежит в `staleTurns`.
- **Базовая цена покупки миньона — правило игры, не факт лога**: тега `COST`
  у миньонов витрины нет, тройка золотых лежит в `rules.ts`. Скидки при этом
  читаются (`BACON_REDUCE_BUY_COST`, см. «не переоткрывать») — неразобранной
  осталась только гипотетическая НАЦЕНКА: случаев дороже трёх в фикстурах
  не встречалось.
- **Таблица ключевых пар карт руками не пишется.** ТЗ её упоминает,
  но выдумывать пары значит вписать в правила чьё-то мнение под видом
  факта. Вместо таблицы — три читаемых из данных связи: племя
  (`perTribeMate` и `perTextTribeMate`), механика словами
  (`perTextMechMate`) и, с 15.08, ИМЯ карты в тексте
  (`perNamedCardMate`): Automaton Portrait называет Ancestral Automaton,
  связь двусторонняя, копии исключены, имена короче шести символов
  не ищутся, тексты приводятся к одиночным пробелам (переносы строк
  посреди имени — реальность снапшота). В пуле таких пар «миньон ↔
  миньон» десятки — все из опубликованных текстов.
- **Сравнение «телом против примагнитить» не делается.** Носитель теперь
  выбирается без дубля дара и называется всегда (part13), но что выгоднее
  на неполном борде — тело в слот или статы носителю — правила не судят:
  цены слота у них нет.
- **План розыгрыша продаёт не больше одного миньона.** Жертва считается
  против исходного борда; второй шаг через продажу продавал бы того же.
- **Цель баффа — «крупнейший свой», как у магнита.** Кому полезнее +1/+1
  или щит, правила не судят. Два исключения по фактам part15: бафф
  с провокацией обходит движков, нецелевое заклинание цели не называет
  (см. «не переоткрывать»).
- **Заклинания вне «золото» и «статы» не оцениваются.** Скидки («Отправные
  цены»), бананы в руку, раскопки-заклинания — эффекты без прямой цены;
  в совете по выбору они честно помечены «оценить не берёмся».
- **Полный оптимизатор трат хода не строится — ЗАМЕРЕНО 16.08.** Вместо него
  есть ПЛАН хода (`spend.ts`): цепочка тех же правил на гипотетических
  состояниях — верхний совет, состояние после него, снова верхний совет.
  Второй шкалы очков в нём нет намеренно: ценность подъёма в правилах
  СРАВНИТЕЛЬНАЯ (равна лучшей покупке плюс срочность, чтобы обойти её
  в списке), и складывать такие числа значило бы считать лучшую покупку
  дважды. С 17.08 у цепочки ОДНА развилка — когда она сама признала, что
  золото сгорает (part23, ход 5, см. «не переоткрывать»). Тем же вечером
  по решению игрока (part25) в развилку вошёл и ПОДЪЁМ — но не очками
  списка, а полем `standaloneScore`: двойной счёт лучшей покупки снят
  явно, и запрет на сложение по-прежнему соблюдён. Перебор корзин (`npm run validate:spend`, 17 партий): внутри формы
  — той же длины набора и с тем же решением о подъёме — цепочка совпадает
  с лучшим по бою в **64%** ходов при цене **5.97 п.п.** (28 ходов). После
  правки шкалы ходов (part20) на прежних 16 партиях было 75% при 6.59
  против 76% при 7.5 до правки — регресса нет, а состав сравнений
  сдвинулся: план перестал начинать ход ненужным подъёмом и чаще попадает
  в ту же форму, что лучший набор. Процент здесь чувствителен к шуму
  (критерий строгий, «не хуже лучшего», а на ничейных ходах разницу решает
  Монте-Карло), поэтому читать надо цену расхождения: она снижается.
  Весь разрыв «по всем наборам» (57%, 46 ходов)
  создаёт одно различие: план тратит золото целиком,
  а ближайшему бою выгоднее придержать часть и не ставить лишнее тело, —
  судить это ближайшим боем нельзя, он не видит ни экономики, ни следующих
  ходов. Выборка мала (28 ходов, решающих 13), порог заранее не объявлялся:
  это диагностика, а не доказательство. Перезамер после правок part21
  (18 партий): внутри формы 29 ходов, **69%**, цена **9.18 п.п.** Процент
  вырос, а цена — из-за ОДНОГО хода: part21 дала в сравнение ровно одну
  точку, ход 1, и та стоила 100 п.п.; без неё — 5.94 на 28 ходах, то есть
  прежние 5.97. Ход 1 — документированный другой режим (бой 1×1, исходы
  насыщены), выводов из него не делается. Перезамер после правок part23
  (развилка при сгорании) на тех же 18 партиях: внутри формы 24 хода,
  **63%**, цена **11.01 п.п.** — хуже по обоим числам, и это записано,
  а не спрятано. Читать надо вместе со строкой, которую отчёт печатает
  рядом: **«золота сгорает: у плана 1.07, у лучшего набора 2.83»**.
  «Лучший набор» по ближайшему бою сжигает втрое больше золота, чем наш
  план, — то есть мерка систематически награждает НЕтраченное золото,
  а правка сделана ровно затем, чтобы его не жечь. Претензия игрока
  и критерий сверки здесь смотрят в разные стороны, и приоритет у игрока:
  экономики и следующих ходов ближайший бой не видит. Плюс сравнение
  не like-for-like — развилка меняет первый шаг, а с ним и состав
  «внутри формы» (24 хода против 29). part23 в эту сверку не вошла вовсе:
  все её ходы отсеяны как «без боя следом» или «вне перебора».
  Перезамер после развилки с подъёмом (part25, 22 партии): внутри формы
  26 ходов, **65%**, цена **14.07 п.п.**; сгорает **у плана 1.49 против
  2.21** у лучшего набора. Читать надо строку про золото: разрыв сузился
  с обеих сторон, и это ровно то, ради чего правка делалась. Проценты
  не like-for-like — развилка меняет решение о подъёме, а от него зависит
  сам состав «внутри формы». Подъём из сверки не исчез: на ВТОРОМ ходу
  таверны он стоит всё золото (4 из 4), не жжёт ничего, и план его делает
  — расхождения part24 (ход 3, 74.5 п.п.) и part25 (ход 3, 100 п.п.)
  именно там, и оба про то, что ближайший бой наказывает вложение
  (на part25 этот подъём сделал сам игрок).
  Перезамер с part24 (21 партия): внутри формы 24 хода, **67%**, цена
  **15.06 п.п.**; сгорает у плана 1.56 против 2.82 у лучшего набора.
  Числа между прогонами не like-for-like — состав «внутри формы» каждый
  раз другой. Отдельно стоит запомнить ДВЕ точки самой part24: ходы 3
  и 7, где план поднял таверну, а лучший набор нет, — цена 75.5 и 41.5
  п.п. Ход 7 — тот самый, на который игрок показал скриншотом; ближайший
  бой систематически наказывает подъём (это вложение), но два таких
  расхождения подряд — лучший довод за пересмотр, какой у нас есть.
  Подробности и границы — в [docs/tavern.md](docs/tavern.md).
- **Состав племён в советах используется точечно.** Читается он теперь
  честно (см. «не переоткрывать»), но фильтрует пока только одно: тринкет
  для племени, невиданного в витринах партии, говорит об этом вслух.
  Остальные советы состав не сверяют — свои миньоны племени обычно
  доказывают его сами, а фантом возможен лишь от карт, полученных
  вне магазина.
- **Choose One — ЗАКРЫТО 17.08 разбором по ветвям** (part19). Прежняя запись
  гласила, что сумма статов складывает обе ветви и потому завышена. Теперь
  ветви читаются отдельными картами снапшота, эффект берётся от выбранной,
  и совет её называет. Осталась честная граница: РАЗДЕЛИТЬ равные по сумме
  ветви («+3/+1» и «+1/+3») нечем — замер разницы не нашёл (см. «не
  переоткрывать»), и совет называет обе.

### Следующий шаг

Двадцать третья партия (part31, 6-е место) дала два пункта, и оба
закрыты: цена придержанного заряда дара и жертва у миньона в руку
на полном борде. Открытые вопросы ИМЕННО по ней:

- **темповая цена дара не считается.** Правило вычитает разницу тел
  «сейчас» и «на вытесняемом ходу», но тело сейчас воюет больше боёв.
  На нашей шкале этой цены нет ни у чего; если дар станет придерживаться
  дольше, чем жмёт игрок, — первый подозреваемый. Разница тиров 4–5
  и 5–6 на шкале — два очка (17.7 против 19.5), то есть на 8–9-м ходах
  таверны дар уже почти не придерживается; если «ключевые существа»
  стоят игроку больше, крутить надо `minionValue` по тирам.
- **горизонт без здоровья** (оговорка part28) теперь стоит и под даром:
  на низком hp заряд дешевле, чем обещает таблица, и жать стоило бы
  раньше. Причина печатает горизонт словами — возражать есть чему.
- **продажа ради подъёма** — ход игрока на скриншоте (продал Rodeo
  Performer, поднял таверну за 10 при девяти золотых), которого совет
  не видит вовсе: подъём «не по карману», а `sellForGoldRule` продаёт
  только карту с ценностью в продаже. Фактура — одна партия, игрок
  не просил; вслепую не вносим.
- **«Discover» у заклинания витрины считается средним, а не лучшим
  из трёх** — расхождение с силой героя part30 записано: расширить
  порядковую статистику на `namedTierPool` значит сдвинуть и прокрутку
  Patient Scout (part25), это отдельный шаг со сверкой.
- **part31, как part27–part30, не в `CURRENT_BUILD_PARTS`**; числа
  `validate:spend` устарели ещё сильнее — дар в плане ранних ходов исчез
  у всех старых фикстур.

Двадцать вторая партия (part30, 3-е место) дала четыре пункта, и все
четыре закрыты: сила-Discover по племени, заклинание по витрине,
отложенное золото в плане, сток сгорающего золота. Открытые вопросы
ИМЕННО по ней:

- **список и план на ходу 1 расходятся.** Верхняя строка списка — «СИЛА
  ГЕРОЯ King of Mechs (7.5)», план — «КУПИТЬ Tusked Camper за 3»:
  гипотетический шаг силы непрозрачен, найденный миньон и банан на него
  плану не видны, и развилка наказывает цепочку силы за сгоревший золотой
  (7.5 − 3 < 7.0). Тот же класс, что список/план part29 (ход 7). Учить
  шаг силы приносить миньона-заготовку — выдумывать сущность
  в гипотетическом состоянии; вслепую не делаем.
- **«лучший из трёх» — оценка сверху.** Карты пула считаются
  равновероятными (копий снапшот не несёт), пул меньше трёх отдаёт
  максимум. Если сила станет навязчивой верхней строкой — крутить это.
- **выбор СИЛ героя мерит «Discover a \<Tribe\>» витриной, а не пулом
  племени** (part26, Нгуен) — не тронуто, чтобы не перемерять
  ранжирование той партии; сводить надо отдельным шагом со сверкой.
- **сток требует триггера на борде.** «Золото сгорит в любом случае» —
  довод игрока и без триггеров, но без них обновление не даёт ничего
  читаемого, и граница part27 оставлена. Если игрок скажет крутить
  и без триггеров — снимать условие, а не переписывать ветку.
- **флаг «сила нажата» на точке решения бывает поднят заранее** —
  порядок строк в блоке: флаг ставится на BLOCK_START, трата золота
  приходит строкой позже (part30, ходы 21 и 23; записано
  в docs/power-log.md). На советы влияет только в срезах-тестах.
- **числа `validate:spend` устарели ещё с правок part27–part29**,
  а `goldNextTurn` их трогает тоже (Careful Investment лежит в витринах
  десятка старых фикстур). Перезапуск — вместе с добавлением
  part27–part30 в `CURRENT_BUILD_PARTS`, отдельным шагом.
- **part30, как part27–part29, не в `CURRENT_BUILD_PARTS`.**

Двадцать первая партия (part29, 3-е место) дала пять пунктов, и все пять
закрыты: сила героя с целью, счёт лишнего тела у заморозки (два пункта),
цена в здоровье, ставка на тройку в последнем слоте. Открытые вопросы
ИМЕННО по ней:

- **список и план на ходу 7 расходятся видимо и противоречиво.** Верхняя
  строка списка — «СИЛА ГЕРОЯ Oozeling Gladiator 2/2», вторая —
  «ЗАМОРОЗИТЬ Oozeling Gladiator 2/2»: два совета про одну карту, и первый
  её уничтожает. План при этом связен (делает выстрел, заморозку
  не советует), и оверлей показывает план первой строкой. Связать их —
  значит учить заморозку выстрелу или наоборот; вслепую не делаем.
  Сам игрок в этот момент выстрелил именно Oozeling.
- **порядок «остатков» у выстрела — это наш порядок ПОКУПОК.** На ходах 7
  и 9 игрок стрелял не тем, что предлагает правило, и оба раза расхождение
  идёт от `minionValue` (отработавший генератор числится по тиру —
  открытый вопрос ещё с part27), а не от `combatValue`.
- **равенство 3.0 против 3.0 у ставки на тройку.** Ничью правило решает
  в пользу руки структурным доводом (розыгрыш необратим), а не замером.
  Если игрок увидит ход, где лишнее тело было нужнее ставки, чинить надо
  `combatValue`, а не запрет.
- **цена в здоровье у МИНЬОНОВ не разобрана.** Тег тот же, но фикстуры
  на такой случай нет: в шести партиях, где он встречается, он всюду
  на одной карте `BG28_571`. Ждём партию, где цену в здоровье назначает
  Malchezaar, Bazaar Dealer, наклейка или тринкет.
- **«перемотчик» считается бесконечным.** Текст ограничения не называет,
  и в part29 Soul Rewinder отработал на обеих покупках и на уроне боёв,
  выросши с 4/1 до 25/34. Если в какой-то партии окажется, что перемотка
  разовая или лимитированная, — это первый кандидат.
- **part29, как part27 и part28, не в `CURRENT_BUILD_PARTS`.** Добавление
  всех трёх обесценивает записанные числа сверок и замеров до полного
  перезапуска — это по-прежнему отдельный шаг.

Двадцатая партия (part28, 8-е место) дала один пункт, и он закрыт:
ветвь модального миньона называется в совете. Открытые вопросы ИМЕННО
по ней:

- **совет на ходу скриншота расходится с выбором игрока, и это надо
  ему подтвердить.** Наш счёт даёт Collect the Bounty 15.9 против
  Ensnare the Target 9.8; игрок выбрал квилбоара. Решает полный борд:
  карта идёт в руку и требует ВТОРОЙ продажи сверх той, которой
  освобождено место под самого ловушника. Не будь борд полон, квилбоар
  (20.8) выигрывал бы уверенно. Если игрок считает, что вторая продажа
  так не считается, править надо вычет жертвы, а не курс золота.
- **горизонт партии — среднее по 25 партиям без поправки на здоровье.**
  На самой part28 таблица обещала 5.3 хода, игрок прожил 2 (он шёл
  на 23 hp при обычных 31+ и пришёл восьмым). Здоровье в таблицу
  не вошло сознательно — оно с ходом смешано, — но именно здесь
  допущение сработало против нас: на низком hp предел золота стоит
  дешевле, чем обещает таблица. Первый кандидат на уточнение, когда
  партий станет заметно больше.
- **очки самого миньона ветвь не двигает.** Snare Trapper ценится телом
  и племенем, как прежде: ценность ветви в `minionValue` не входит,
  чтобы не устаревали сверки. Значит модальная карта по-прежнему может
  проиграть в списке покупок ту позицию, которую заслуживает эффектом.
  Вносить это надо вместе с перезамером `validate:tavern`
  и `validate:spend`, то есть отдельным шагом.
- **Blood Gems остались без цены.** Три модальных миньона пула
  (`BG30_123`, `BG31_320`, `BG36_341`) обещают кровавые самоцветы,
  и правил про них у нас нет — совет там честно называет обе ветви.
  Фактура появится с партией за квилбоаров, где они попадутся.
- **выбор ветви игроком (`SubOption`) в журнал действий не пишется.**
  Лог его отдаёт, и это готовое сырьё для фазы 6: «что игрок выбирал»
  против «что советовал советник» — тот же замер, что имитация покупок.
- **part28, как и part27, не в `CURRENT_BUILD_PARTS`.** Добавление обеих
  делает записанные числа сверок и замеров устаревшими до перезапуска
  всех сразу — это по-прежнему отдельный шаг.

Девятнадцатая партия (part27, 3-е место) дала три пункта, все три
закрыты структурно — планка заморозки, признак движка, цель обновления.
Открытые вопросы ИМЕННО по ней:

- **не стало ли заморозок в плане много.** Замер: 25 → 37 точек решения
  из 292 с заморозкой в плане (ради заклинания 15 → 24). Планка мягкая
  по построению — свежая сторона среднее, а не лучшее-из-N. Если игрок
  скажет «навязчиво», крутить свежую сторону (порядковая статистика
  по пулу — число вычислимое), а не возвращать среднюю доживающую.
- **«умолкает на третьем тире» — следствие арифметики, и игроку это
  надо подтвердить.** На part23 (ход 11) третий тир молчит, потому что
  свежая карта там стоит 10.7, а заклинание за 2 приносит тело первого
  тира (9.0); заклинание посильнее — лассо при богатой витрине — планку
  третьего тира взять может.
- **обновление при нуле золота остаётся, когда есть пара.** Игрок сказал,
  что совет бесполезен; совет остался с причиной «искать третью копию X».
  Если и это лишнее — убрать цель «соплеменник» или требовать
  `lateRerollTier`; вслепую не убираем.
- **отработавший генератор на борде числится по тиру.** Oozeling Gladiator
  3/3 с отработавшим кличем стоит в `ownValue` как тир 2 плюс тело,
  и кандидатом в продажу был Southsea Busker, а не он. Игрок продать
  собирался именно Oozeling. Фактуры на «клич отработал — тир не считать»
  одна карта; правило не вносится.
- **part27 — первая партия билда 250339, и она не в `CURRENT_BUILD_PARTS`.**
  Билд объявлен одной игрой с 248348 (`src/data/builds.ts`, параллельная
  сессия 26.08); добавление part27 в список делает записанные числа сверок
  и замеров устаревшими до перезапуска всех сразу — это отдельный шаг.

Восемнадцатая партия (part26, 4-е место) дала два пункта, оба закрыты
частично — и в обоих граница проходит по ДАННЫМ, а не по правилам:

- **половина сил героя по-прежнему «не берёмся»** — те, чей
  идентификатор героя не называет (`TB_BaconShop_HP_020` и родня, сто
  из 169). Путь к ним есть и найден в этой же партии: у сущности героя
  лежит тег `HERO_POWER` с dbfId его силы, то есть связь читается
  из ЛОГА, а не из имени карты. Собрать её по фикстурам — отдельная
  работа (таблица из данных, как `trinketOfferTurns`), и вносить её
  вместе с пунктами игрока значило бы смешать проверенное
  с непроверенным.
- **среднее место героя — прокси, и это надо проверить на игроке.**
  Число про целую партию того героя, а Нгуену сила достаётся на один ход.
  Если совет начнёт тянуть к силам «сильных героев» там, где ход требует
  другого, это увидится сразу.
- **порядок ставки «по тиру» стоит на ОДНОМ случае.** part26 его
  подтвердила (выиграл тир 4 против тира 3, а лидер таблицы проиграл),
  но это не замер. Накопить фактуру можно только партиями, где ставка
  сделана; в датасете она пока одна.
- **«Наемный детектив» остался без совета.** Его вопрос («какой из двух
  миньонов был у следующего противника») наши данные в момент вопроса
  не решают: на ходу 7 следующего противника мы не видели НИ РАЗУ,
  и это норма — до 13-го хода он почти никогда не из виденных
  (docs/position.md). Когда борд виден, ответ был бы фактом; ветку
  имеет смысл вносить вместе с замером, как часто это случается.

Семнадцатая партия (part25, 3-е место) дала два пункта, оба закрыты:
прокрутка через продажу и заморозка ради неё. Вместе с ними закрыт спор
о подъёме на шести золотых — решением игрока, а не догадкой. Открытые
вопросы ИМЕННО по ней:

- **список и план теперь расходятся заметно чаще.** На 4-м ходу таверны
  верхняя строка списка говорит «ПОДНЯТЬ ТАВЕРНУ», а план — «купить X,
  потом дар». Так и задумано (список ранжирует отдельные действия, план
  выбирает ход целиком), и оверлей показывает план первой строкой,
  но два соседних совета читаются как противоречие. Если игрок это
  подтвердит, честнее пометить подъём в списке словами «план тратит
  золото на борд», а не менять порядок.
- **запрет «мусорный подъём в развилку не идёт» проверяет только ЖАДНУЮ
  цепочку** (найдено аудитом диффа 19.08, правкой НЕ стало). `spendPlan`
  отказывается от развилки, когда в жадной цепочке стоит подъём без
  `standaloneScore`; но альтернативные цепочки строятся с запретом подъёма
  лишь тогда, когда подъём был у жадной, — значит мусорный подъём может
  войти ВНУТРЬ альтернативы вторым-третьим шагом и попасть в сумму
  цепочки ценностью, посчитанной как «лучшая покупка плюс срочность».
  Случая в фикстурах не нашлось, поэтому правило вслепую не меняется:
  сначала нужен ход, где это видно, — иначе правка пойдёт под видом факта.
- **канонический подъём part20 исчез, и это принято сознательно.** Там
  игрок поднялся сам и пришёл первым; теперь совета не будет. Если после
  нескольких партий окажется, что тир приходит слишком поздно, чинить
  надо не развилку, а таблицу кривой — там строка «тир 3 к 4-му ходу
  таверны» расходится с его собственной игрой (по датасету 25 партий он
  берёт тир 3 на 5–6-м ходу).
- **не станет ли тёмный дар слишком частым первым шагом плана.** Освободив
  4-й ход таверны от подъёма, развилка часто ставит туда дар (на part20,
  part24 и part25 — во всех трёх). Своего замера у дара нет, а зарядов
  три на партию (см. «отложено»).
- **заморозка ради продажного генератора видна и на part22 (ход 7)** —
  в партии, где игрок жаловался на ЛИШНИЕ заморозки. Карта другая
  (River Skipper, не лассо) и планка другая, но если совет снова
  покажется навязчивым — это первый кандидат.
- **скидка клича на заклинание витрины не читается.** «Battlecry: The
  next Tavern spell you buy costs (1) less» (Ominous Seer) — ровно на ней
  у игрока сошлась цепочка из трёх действий: лассо стоило 1 вместо 2.
  План этого не знает и цепочку из трёх шагов на шести золотых не
  построит. Фактура есть, правило — нет: вносить его вместе с пунктами
  игрока значило бы смешать проверенное с непроверенным.

Шестнадцатая партия (part24, 5-е место) дала четыре пункта: два закрыты
тогда же (золото заклинания в плане, поглощение витрины), ещё один —
подъём на шести золотых — закрыт 17.08 вместе с part25 решением игрока.
Остался один:

- **ЗАМОРОЗКА РАДИ ЗАКЛИНАНИЯ — ЗАКРЫТО 26.08 (part27), не разворотом,
  а починкой счёта.** История: part19 «совет исчез, верните»; part22
  «практического эффекта не вижу»; part23 «работает для ранней игры»;
  part24 «в ранней игре СНОВА не советует, хотя раунд назад советовало»
  (заклинание 8.5 против планки 9.79); part27 «снова перестало советовать
  на ранних ходах». Вопрос «на каком тире умолкать — на третьем или
  никогда?» игроку так и не пришлось решать: планка считала цену заморозки
  по СРЕДНЕЙ доживающей карте и не знала, что замороженная витрина
  дозаполняется свежими. С починкой (см. «не переоткрывать») на ходу 3
  part24 планка 6.81 против 8.48 — совет есть; на третьем тире (part23,
  ход 11) свежая карта 10.70 против 9.02 у тела первого тира — молчит. Вторая
  половина замечания остаётся: на ходу 3 план советует ПОДНЯТЬ ТАВЕРНУ,
  а следом — заморозку той же витрины (подъём добавляет слот, и заклинание
  в ней доживёт), то есть «памяти о вчерашней ставке» по-прежнему нет,
  но ставка и не отменяется.
- **ПОДЪЁМ НА ШЕСТИ ЗОЛОТЫХ — ЗАКРЫТО 17.08 решением игрока (part25).**
  Жалоба пришла третий раз подряд, вопрос был вынесен ему с числами,
  и он выбрал развилку: план сравнивает подъём с цепочкой, подъём идёт
  в сумму своей срочностью (`standaloneScore`), список не изменился.
  Теперь на part24 (ход 7) план играет розыгрыш → покупку → дар вместо
  подъёма с двумя сгоревшими. Осталась половина вопроса, которую правка
  не трогала: были ли те два Wrath Weaver «мусорной витриной» (5.5 при
  пороге 6) — по словам игрока, они хорошо синергировали с бордом.
  Если это повторится, чинить надо ЦЕННОСТЬ ПАРЫ, а не подъём.
- **не слишком ли часто советуется тёмный дар.** После таблицы он верхний
  совет на 16% точек решения и первый шаг плана на 12%. Числа скромные,
  но формулировка «тир 2 или 3 (12.0)» новая и в игре не проверена.
- **цена заряда дара.** Их три за партию, и модель этого не знает
  (см. «отложено»). Если игрок увидит совет жать дар там, где заряд
  стоило придержать, — это первый признак.

Пятнадцатая партия (part23, 7-е место) дала три пункта, все закрыты
структурно. Открытые вопросы ИМЕННО по ней — и первый из них главный,
потому что правка отменяет прежнее правило:

- **ОТМЕНЁННОЕ ПРАВИЛО part19 надо подтвердить игроком.** Заморозка ради
  заклинания теперь живёт только на первом тире, и совет, который игрок
  просил вернуть в part19 («исчез после подъёма»), больше не появляется.
  Основание — его же слова из part23 («работает для ранней игры») плюс
  жалоба part22 на тот же совет, и починка планки, сравнивавшей числа
  с разной начинкой. Если он считает иначе, нужен его довод против
  АРИФМЕТИКИ (свежая карта своего тира против средней доживающей),
  а не против совета: правится тогда планка, а не тир.
- **не стал ли план менять первый шаг слишком охотно.** Развилка
  срабатывает, только когда жадная цепочка сама признала сгорание,
  и подъём не трогает; но верхняя строка советов и первый шаг плана
  теперь могут расходиться, и это игрок увидит первым делом.
- **вес тёмного дара.** Он плоский (8 очков) и по описанию механики
  ведёт себя наоборот правде: подталкивает жать РАНО, хотя предложения
  тем сильнее, чем позже. Своего замера у нас нет и ближайшим боем его
  не сделать. Спросить игрока: жать ли дар раньше или позже, и не мешает
  ли совет «ТЁМНЫЙ ДАР за 3» на вторых-третьих ходах таверны.
- **заклинание бесплатных обновлений оценено по номиналу.** Два
  обновления по цене кнопки минус цена заклинания. Обновление стоит
  своего золота лишь тогда, когда его есть на что тратить, — если совет
  начнёт всплывать не к месту, это увидится сразу.

Четырнадцатая партия (part22, 1-е место) дала пять пунктов. Три были
закрыты структурно тогда же (рука как позиция, сила героя в ценности
покупки, удвоитель механики), четвёртый — заморозка лассо — закрыт
17.08 вместе с part23 (см. выше). Остался один:

- **не слишком ли дорого встала сила героя.** River Skipper на ходу 1
  поднялся с 5.5 до 9.0 и обошёл дракона — как игрок и просил, — но
  ближайший бой в сверке наказал этот выбор на 50 п.п. (1/1 против 1/4).
  Это документированная оговорка про экономику, и партию игрок выиграл;
  но если совет начнёт тянуть к мусорным телам ради героя, это увидится
  первым.
- **читается ли «свой удвоитель на борде» в оверлее.** Формулировка новая
  и игроком в игре не проверена.

Тринадцатая партия (part21, 2-е место) дала два пункта, и оба закрыты:
цель заклинания теперь считается по выгоде магнита, а не по размеру тела,
заклинания руки вошли в ценность покупки. Третий вопрос той же партии
(провокация на носителе ралли) закрыт ЗАМЕРОМ с отрицательным ответом.
Открытые вопросы ИМЕННО по этой партии:

- **достаточно ли «польза не показана» про провокацию на ралли.** Замер
  дал −0.241 п.п. при пороге 0.359 — знак в сторону опасения игрока, но
  различить нельзя: у 60 точек из 78 обе ветви дали тождественный исход.
  Если игрок вспомнит или сыграет партию, где провокация на носителе ралли
  стоила боя, — нужен ЭТОТ ход фикстурой, и порог будет пересмотрен
  по фактуре. Вслепую правило не вносится.
- **видно ли в оверлее, ПОЧЕМУ сменилась цель.** Совет теперь пишет
  «усиление на нём останется навсегда» — это новая формулировка, и она
  ещё не проверена игроком в игре.
- **пять магнитов из семи молчат.** Копия заклинания (Zesty Shaker),
  кровавый камень соседям, усиление витрины и миньона в руке числом
  не мерятся, и цель у них выбирается по телу. Если игрок увидит промах
  на одном из них — это фактура для отдельного правила.

Двенадцатая партия (part20, 1-е место) дала один пункт — «очень часто
советует улучшить таверну», — и он оказался про шкалу ходов, а не про
порог или место (кривая шла вдвое быстрее партии). Правка одна и
в одном месте, замер частоты стал постоянной командой
(`npm run spike:level`). Открытые вопросы ИМЕННО по этой партии:

- **читается ли теперь причина подъёма.** Совет называет ход таверны
  («при ожидаемых 4 к 8-му ходу таверны»), а шапка оверлея по-прежнему
  печатает наш `turn` («ход 15»). Два номера рядом могут путать; если
  игрок это подтвердит, честнее показывать в шапке ход таверны, а наш
  счётчик оставить внутри — но менять вид без его слова не стоит.
- **не стало ли подъёма СЛИШКОМ мало.** После правки подъём — верхний
  совет в 30% точек решения (был 50%), и в этой партии только на втором
  и четвёртом ходах таверны. Сама кривая (тир 3 к четвёртому ходу, тир 4
  к седьмому, тир 6 к одиннадцатому) — чужая таблица, сверенная с игрой
  на глаз, а не наш замер: если игрок скажет, что темп занижен, правится
  таблица, а не шкала.
- **порог здоровья и место в таблице.** Опасение «на 7 месте могу
  не успеть вернуться» правкой не закрыто и правилом не стало: та же
  партия его опровергает (седьмое место при 22 hp, подъём не сделан,
  первое место в итоге). Если случай «советник погнал подниматься
  на остатках» найдётся, нужна фикстура с этим ходом — тогда порог
  перестанет быть плоским по фактуре, а не по мнению.

Одиннадцатая партия (part19, 1-е место) — четыре пункта, три закрыты
правилами (заморозка ради заклинания переживает подъём, ветви «Choose
One» названы, аура на чужих не идёт в жертву), четвёртый закрыт счётом
без кода (ралли вторым номером — перебор подтвердил совет). Открытый
вопрос ИМЕННО по этой партии: устраивает ли ответ «ветви равны» у
модального заклинания — замер разницы между «+3/+1» и «+1/+3» не нашёл
(−1.530 п.п. при пороге 1.547), и если у игрока есть довод в пользу одной
из них, его надо превращать не в мнение, а в правило с фактурой (например,
«на носителе провокации/щита — здоровье»): такой признак читается
из состояния и проверяется тем же замером.

Девятая партия (part17, 1-е место) — четыре пункта, из них два вопроса:
заклинание витрины, дающее миньона, теперь видно и советнику (покупка
и заморозка), провокация не уходит на токен и на кандидата в продажу,
заморозка при полном борде проходит планку покупки, склеенный золотой
вариант текста больше не удваивает числа. Про расстановку кода
не потребовалось — ответ оказался «борд дорос до 100%», и это закреплено
тестом. Дальше — продолжать цикл: следующая партия проверит сегодняшние
правки. Открытые вопросы к игроку прежние: не мешают ли частые честные
«менять нечего» от цели-поля; полезна ли строка «по бою» и её
формулировки (в part16 она дважды честно говорила «бой выигрывается
любой покупкой» против устаревших целей). Новый вопрос из part17:
устраивает ли цель провокации «крупнейший из остальных» — Fortify теперь
идёт на Wildfire Elemental 8/3, а не на токен. И вопросы от 16.08: читается
ли ПЛАН ХОДА первой строкой лучше, чем прежний одиночный совет, и не мешает
ли он — план занимает место одной из трёх строк оверлея.

Десятая партия (part18) дала три пункта, все про план, все закрыты
структурно: продажа-ради-траты вошла в план, раннее платное обновление
из него ушло, синергия по механике стала двусторонней. Следующая партия
проверит эти правки — и заодно первый вопрос про план останется открытым.

На вопрос игрока «выгрузить ли из hsreplay, что делают герои и карты»
отвечено: данные о том, что делает карта, уже есть — снапшот Firestone
несёт текст, механики и племена каждой карты, и все пять промахов part15
были не от нехватки данных, а от правил, которые эти поля не читали
(текст Titus лежал в снапшоте, «of each type» у сервиза — тоже).
Статистика мест уже берётся снапшотом Firestone (`npm run update:bgstats`,
герои и тринкеты); у hsreplay открытого API нет, и новых данных он бы
не добавил.

По базе знаний JeefHS (docs/jeefhs.md) игрок ответил на все вопросы:
тьюторинг тринкетов подтверждён (напоминание внесено), приоритет
сильного нейтрального тринкета подтверждён с примерами (ранжирование
эффективным местом внесено), порог подъёма по здоровью жалоб не вызывал
(не тронут). Единственный хвост — таблица активаторов: частично
устарела, ждём от игрока список актуальных карт, вслепую не вносится.

Что ещё осталось незакрытым:

- **досчёт покупок в живом режиме считает против виденных бордов,
  а не против фактического борда боя** — знание оффлайн-сверки живому
  режиму недоступно по построению. Давность картинки подписывается;
  насколько живой досчёт помогает на деле, покажет обратная связь
  игрока со строк «по бою»;
- **окно оверлея в среде разработки не запускалось** — бинарник Electron
  не скачался. У игрока работает; вид покрыт тестами;
- **фаза 6, ML — НАЧАТА 18.08, два замера сделаны** (docs/ml.md):
  предсказание места (`npm run ml:eval`, 18.08) и имитация покупок
  (`npm run ml:imitation`, 19.08) — оба НЕ ДОКАЗАНО, в советник
  и оверлей ничего не внесено (см. «Отложено сознательно»). Дорожная
  карта «от прогноза места к действиям» — в docs/ml.md; журнал действий
  пишется в датасет с 19.08. Перезамер обоих при +10 новых партиях —
  они первые принесут `lobby` (относительные признаки) и ослабят второй
  контур петли (советник, дорабатывавшийся по тем же партиям).

## Настройка логирования — обязательна, иначе данных не будет

Два файла в **разных** местах, это легко перепутать:

**`%LOCALAPPDATA%\Blizzard\Hearthstone\log.config`** — что логировать:

```ini
[Power]
FilePrinting=true
Verbose=true
```

**`<каталог установки игры>\client.config`** — предел размера лога:

```ini
[Log]
FileSizeLimit.Int=1000000
```

Без второго клиент обрывает **всё** файловое логирование на 10 000 КБ и больше
не возобновляет: полная партия BG весит ~40 МБ и не влезает. Настройка читается
один раз при старте клиента. Файл лежит рядом с игрой, поэтому обновление
Hearthstone может его снести — `ensureLogSizeLimit` проверяет и чинит.

## Что известно про формат Power.log

**Полная фактура — в [docs/power-log.md](docs/power-log.md).** Там всё с
доказательствами из фикстур: числа, цитаты строк, что проверено человеком,
что скриншотом, а что остаётся гипотезой. При работе с парсером читать оттуда,
а не вспоминать.

Ниже только то, на чём ломаются чаще всего. Каждый пункт стоил отдельного бага,
и все они дают **не падение, а тихо неверный результат**:

1. **UTF-8 без BOM, переводы строк смешанные.** Обычные строки CRLF, баннер
   обрезки — одиночные LF. Разбивать по `/\r?\n/`.
2. **`entityName` может содержать вложенные скобки** —
   `[entityName=UNKNOWN ENTITY [cardType=INVALID] id=255 …]`, около 10% всех
   дескрипторов. Разбирать позиционно по именам полей, не по закрывающей скобке.
3. **Канал-источник — `GameState.DebugPrintPower`.** `PowerTaskList` дублирует
   его событие в событие; применять оба — применить теги дважды.
4. **`BLOCK_END` есть не у каждого `BLOCK_START`.** Вложенность считать
   по отступу, а не по парности.
5. **Дескриптор в `TAG_CHANGE` показывает состояние ДО изменения.** Зону
   и позицию брать только из явных тегов, иначе мёртвые миньоны воскресают.
6. **`HIDE_ENTITY` несёт смену зоны и начинается не с `TAG_CHANGE`.**
   Там же: **`SHOW_ENTITY` называет сущность через `Entity=`, а `FULL_ENTITY`
   через `ID=`** — разбор, знающий одну форму, теряет 1349 раскрытий карт
   за партию и оставляет их без `cardId`.
7. **Отбирать сущности белым списком** (`CARDTYPE=MINION`), а не «всё кроме».
8. **Одинаковые теги приходят на разные сущности с разным смыслом.** `TURN`
   на `GameEntity` — ход партии, на игроке — его счётчик, вдвое меньше.
9. **Своего игрока определять по ненулевому `GameAccountId`**, не по номеру:
   `PlayerID` меняется между партиями (в фикстурах 4 и 3 у одного человека).
10. **Золото — это `RESOURCES` минус `RESOURCES_USED`.** Прямого тега
    на остаток нет.
11. **Следующий противник известен заранее** — тег `NEXT_OPPONENT_PLAYER_ID`
    в таверне, проверено 18 совпадений из 18. Кто именно скрыт за чужим слотом,
    определяется через тег `PLAYER_ID` его героя, а не по подписи слота:
    та в поздних боях остаётся «Бармен Боб». Тег
    `BACON_CURRENT_COMBAT_PLAYER_ID` — это НЕ противник, а сам игрок.
12. **Борд противника снимать в начале боя**, по первому блоку `ATTACK`:
    к выходу в таверну его миньоны уже убраны из `PLAY`.

## Как размечать новые фикстуры

Видео бесполезно — ассистент его не читает. Работает так:

1. Игрок делает 4–6 **скриншотов** в конце фазы таверны, на разных ходах.
2. Кладёт в `data/screenshots/` (в git не коммитятся, см. `.gitignore`).
3. Ассистент прогоняет `npm run demo:phase1` на логе и сверяет сам.
4. Совпавшее идёт в `partN.expected.json` с пометкой `verifiedBy: скриншот`.

Скриншоты снимаются в середине фазы, а демо печатает её конец — расхождение
в единицу здоровья это норма, а не баг. Проверять то, что от момента не зависит:
состав, порядок, атаку, золото, тир.

Пометки в `expected.json` различаются по силе: `подтверждено человеком` —
игрок видел это своими глазами в игре; `скриншот` — сверено по картинке.
