* text=auto

# Фикстуры Power.log должны храниться байт-в-байт: игра пишет UTF-8 без BOM с CRLF.
# Без -text git нормализует переводы строк, и парсер в тесте увидит не то,
# что реально писал клиент. См. docs/power-log.md.
*.log -text
